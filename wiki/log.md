# Wiki Log

Append-only record of wiki operations — ingests, queries filed back, lint passes, scaffolding. **Never edit existing entries**; only append new ones at the bottom.

Entry format (consistent prefix keeps the log greppable — `grep "^## \[" log.md | tail -5`):

```
## [YYYY-MM-DD] <op> | <subject>
1–3 sentences on what happened.
```

Operations: `scaffold`, `ingest`, `query`, `lint`, `decision`, `task`, `bug`, `requirement`, `roadmap`.

---

## [2026-06-11] scaffold | Two-domain wiki + raw/ layer created
Created the `wiki/` knowledge base in the two-domain form (`knowledge/` + `work/`) with index/log MOC, conventions, and per-family lifecycle docs. Relocated ground-truth docs into the immutable `raw/` layer. Skill rewiring and `.docs/` content migration deferred to follow-up tasks.

## [2026-06-11] scaffold | UAT un-nested, per-family active indexes added
UAT promoted to its own work family at `work/uat/` (own lifecycle, statuses `pending|in-progress|passed|failed|skipped|trashed`). Every work family gained an `index.md` listing only active items; the home index now links to family indexes instead of listing work items. `trashed` added as a terminal status for tasks and UAT (`/task-trash` flips status — no file moves).

## [2026-06-14] scaffold | Archive subdirs and log rotation added
Added `archive/` subdirectory (with `archive/index.md`) to all 6 work families. Updated `lifecycle.md` files to clarify that active files are never moved but terminal items may be moved to `archive/` by `/wiki-archive`. Added two new skills: `/wiki-archive` (batch-moves terminal items) and `/wiki-rotate-log` (rotates log.md to dated segment files at ~400 entries). Updated `conventions.md` with §5 Archiving and §6 Log rotation. The "files are never moved" rule is now "active files are never moved" — link safety is preserved because links use stable IDs not paths.

## [2026-07-06] ingest | Research: Improving the LLM Wiki tooling
Ingested from `raw/research/wiki-tooling-improvements/index.md` (+ `sources.md`). Key claims: Claude Code's native Auto Memory has no bridge to this repo's wiki; independent gist reimplementations converged on a hot-cache session-handoff file, provenance tagging, and multi-writer locking, none of which this repo has yet; dedicated agent-memory frameworks (Mem0/Zep/Letta/Hindsight) solve a different problem than this wiki and shouldn't be adopted as a default. 5 entity pages touched (1 person, 4 tools), 4 concept pages touched, 1 new source summary page.

## [2026-07-06] roadmap | ROADMAP-001 created — Improve wiki tooling based on 2026-07-06 research
Created `wiki/work/roadmaps/ROADMAP-001-wiki-tooling-improvements.md` (5 phases, 11 inline items, all pending task-file creation via `/roadmap-next`). Phases: audit/fix skill-template drift, documentation bridge (Auto Memory vs. wiki), session-continuity hot cache, provenance/confidence tagging, and deferred concurrency-safety locking. Derived from the same-day research ingest above. Owner: David Taylor.

## [2026-07-07] ingest | Research: Serena MCP server disconnects mid-session
Ingested from `raw/research/serena-mcp-disconnect/index.md` (+ `sources.md`). Key claims: the Serena health-tracking hook (`5752eed`) `pkill`s a live Serena process whenever a tool call returns an error its classifier doesn't recognize, on the false premise that a responded error means the process is hung — reproduced live via `ps aux` and the corrupted state file; a second unsynchronized-write race in the same hook can leave enforcement stuck "on" even after the process is confirmed dead, hard-blocking the Read/Bash fallback too. 1 new component entity page (Serena Health-Tracking Hook — first entry in `entities/components/`), 1 new concept page (Responded Error Proves Liveness), 1 new source summary page. Flagged a contradiction in `Wiki Multi-Writer Safety`, which had cited this same hook as the fail-open pattern to imitate; that page is now `confidence: ambiguous`.

## [2026-07-06] task | TASK-010 Update /wiki-ingest to populate the confidence field on new/updated knowledge pages
Created task TASK-010 (ROADMAP-001 Phase 3, Provenance & Quality): teach `lib/skills/wiki-ingest/SKILL.md` to set `confidence: extracted|inferred|ambiguous` on the summary/entity/concept pages it writes, per the convention TASK-009 activated in `wiki/conventions.md` §4. depends_on TASK-009 (done). Roadmap linking left to the orchestrator.

## [2026-07-06] task | TASK-001 Audit lib/skills for stale README.md-style family-index references
Created task TASK-001: verify which skills in `lib/skills/` wrongly assume a `wiki/work/<family>/README.md` file (structured table/template) instead of this repo's actual `index.md` + `lifecycle.md` convention. Preliminary grep already found 9 likely offenders including `roadmap-create`, `primer`, `tackle`, and `roadmap-next` itself. Linked to ROADMAP-001 Phase 0.

## [2026-07-06] task | TASK-002 Audit lifecycle.md files and skill templates for stale completed/ references
Created task TASK-002: confirm no lifecycle.md or skill/template still references the old `wiki/work/<family>/completed/` convention instead of `archive/`. Preliminary sweep already found zero drift — every hit either correctly states the absence of `completed/` or belongs to `migrate-wiki.md`'s historical `.docs/` mapping table. Linked to ROADMAP-001 Phase 0.

## [2026-07-06] task | TASK-003 Fix roadmap-create and other skills drifting from the index.md/lifecycle.md convention
Created task TASK-003 (depends on TASK-001): rewrite 9 skills — `roadmap-create` (heaviest offender), `primer`, `task-audit`, `bug-file`, `power-mode`, `roadmap-next`, `task-update`, `tackle`, `bug-close` — that wrongly assume a `wiki/work/<family>/README.md` file instead of the real `index.md` + `lifecycle.md` split. Linked to ROADMAP-001 Phase 0.

## [2026-07-06] task | TASK-004 Add Auto Memory vs. wiki division-of-responsibility note to CLAUDE.md
Created task TASK-004: add a short "Auto Memory vs. this wiki" subsection to CLAUDE.md's LLM Wiki section (and its source template) documenting which facts belong in Anthropic's per-user, cross-project Auto Memory vs. this repo's per-project, git-versioned wiki. Linked to ROADMAP-001 Phase 1.

## [2026-07-06] task | TASK-005 Add Optional tooling pointer (qmd, Hindsight) to CLAUDE.md
Created task TASK-005 (depends on TASK-004, same file): add an "Optional tooling" subsection to CLAUDE.md cross-linking the qmd and Hindsight entity pages, each with a one-line "status for this repo" (qmd deferred until wiki scale grows; Hindsight deferred until observed cross-subagent memory pain). Does not touch immutable raw/llm-wiki.md. Linked to ROADMAP-001 Phase 1.

## [2026-07-06] task | TASK-006 Add wiki/hot.md template to lib/scripts/templates/wiki/
Created task TASK-006: scaffold a copy-once `hot.md` session-handoff template into `lib/scripts/templates/wiki/` (500-word cap, Key Recent Facts / Recent Changes / Active Threads structure) and seed this repo's own `wiki/hot.md` as the first real instance. Blocks TASK-007 and TASK-008, which wire skills to write/read it. Linked to ROADMAP-001 Phase 2.

## [2026-07-06] task | TASK-007 Update /wiki-ingest (and other wiki-writing skills) to refresh wiki/hot.md
Created task TASK-007 (depends on TASK-006): add a Step 8 to `/wiki-ingest` that regenerates `wiki/hot.md` in full, then extend the same refresh call to `/roadmap-create`, `/task-add`, `/decision-finalize`, `/req-finalize`, and `/wiki-query`'s file-back-as-page path only. Linked to ROADMAP-001 Phase 2.

## [2026-07-06] task | TASK-008 Update /primer to read wiki/hot.md first, before Serena memories
Created task TASK-008 (depends on TASK-006 for the file to exist, and TASK-003 since both edit lib/skills/primer/SKILL.md — TASK-003's README.md-drift fix must land first): add a Step 0 to /primer that reads wiki/hot.md before Serena memory discovery, no-op if the file doesn't exist. Linked to ROADMAP-001 Phase 2.

## [2026-07-06] task | TASK-009 Activate confidence: extracted|inferred|ambiguous in wiki/conventions.md
Created task TASK-009: move `confidence` from conventions.md's reserved-keys list to an active, documented frontmatter key with three values (extracted/inferred/ambiguous), scoped to knowledge pages only. Teaching /wiki-ingest and /wiki-lint to use it is explicitly out of scope (separate roadmap items beyond this batch's 9-item cap). Linked to ROADMAP-001 Phase 3.

## [2026-07-06] task | TASK-001 through TASK-009 executed and closed
Ran all 9 upgraded ROADMAP-001 tasks to completion via parallel `/tackle`-style execution across 3 dependency waves. Wave 1 (TASK-001, 002, 004, 006, 009): confirmed 9 skills drifting to the old README.md convention, confirmed zero `completed/`-vs-`archive/` drift, added the Auto Memory vs. wiki note to CLAUDE.md, scaffolded `wiki/hot.md` + its copy-once template, activated the `confidence` frontmatter key. Wave 2 (TASK-003, 005, 007): rewrote all 9 drifting skills to the real `index.md`/`lifecycle.md` convention, added the Optional-tooling (qmd/Hindsight) pointer to CLAUDE.md, wired a canonical Hot Cache Refresh procedure into `/wiki-ingest` and referenced it from `/roadmap-create`, `/task-add`, `/decision-finalize`, `/req-finalize`, and `/wiki-query`'s file-back path. Wave 3 (TASK-008): added a Step 0 to `/primer` reading `wiki/hot.md` before Serena memories. All 9 task files are `status: done` and removed from `wiki/work/tasks/index.md` per its active-items-only contract. 3 roadmap items beyond the original 9-task upgrade cap (Phase 3 items 2–3, Phase 4 item 1) remain inline placeholders for a future `/roadmap-next` pass.

## [2026-07-06] roadmap | ROADMAP-002 created — Live HTML dashboard for wiki/work families
Created `wiki/work/roadmaps/ROADMAP-002-wiki-work-dashboard.md` (5 phases, 6 inline items, all pending task-file creation via `/roadmap-next`). Persists a plan-mode design session for a zero/near-zero-dependency, always-current dashboard that polls each family's `index.md`/`archive/index.md` client-side via `fetch({cache: 'no-store'})`, served by a builtin-only Node static server — chosen over LLM-regenerated snapshots and Claude-Code-hook-driven regeneration, both rejected for failing the "always current" requirement (they only update when Claude runs, missing edits from humans/git/other tools). Phases: static server, dashboard client, CLI/distribution wiring, docs, manual verification. Owner: David Taylor.

## [2026-07-06] uat | UAT-009 UAT: Activate confidence: extracted|inferred|ambiguous in wiki/conventions.md
Generated UAT-009 for TASK-009 with 5 test cases covering the activated `confidence` frontmatter convention in wiki/conventions.md §4: base-key row present, all three values documented, removal from the reserved-keys list, knowledge-pages-only scoping, and forward-only/no-backfill note. Created 0 repeatable unit tests: none — all cases are documentation-content assertions against a prose convention file with no automatable business-logic contract and no markdown-content test harness in the repo.

## [2026-07-06] uat | UAT-004 UAT: Add Auto Memory vs. wiki division-of-responsibility note to CLAUDE.md
Generated UAT-004 for TASK-004 with 5 test cases covering the new `### Auto Memory vs. this wiki` subsection: heading present in CLAUDE.md, placed after the Navigation paragraph and before Wiki operations, carries both rule-of-thumb bullets, identical heading present in the source template `lib/scripts/templates/CLAUDE-wiki.md`, and the subsection body stays byte-for-byte in sync between the two files (diff). Created 0 repeatable unit tests: none — all cases are static markdown-content assertions with no runtime surface and no test harness in the repo.

## [2026-07-06] uat | UAT-002 UAT: Audit lifecycle.md files and skill templates for stale completed/ references
Generated UAT-002 for TASK-002 with 5 test cases re-verifying the audit's zero-drift finding: no `completed` in any `wiki/work/*/lifecycle.md`, the three `completed/` hits in `lib/skills/` all assert the subdir's absence, the one `lib/scripts/templates/wiki/` hit is generic prose, no live `/completed` path exists in `lib/` outside `migrate-wiki.md`, and `migrate-wiki.md`'s `completed/` mentions are all historical `.docs/`-scoped. Created 0 repeatable unit tests: none — repo has no unit-test runner, so each deterministic check is an embedded `grep` command runnable by `/uat-auto`; the fifth case is a semantic scope judgment left to human verification.

## [2026-07-06] uat | UAT-006 UAT: Add wiki/hot.md template to lib/scripts/templates/wiki/
Generated UAT-006 for TASK-006 with 4 test cases: template `hot.md` carries the required hot-cache structure (frontmatter, `# Hot Cache` H1, regeneration note, and the three sections), this repo's `wiki/hot.md` is seeded with real ROADMAP-001 content (no `YYYY-MM-DD` placeholder), `sync-wiki-scaffold.sh` delivers `hot.md` into a fresh target, and the copy-once semantics preserve a customized target `hot.md` across a re-sync. Created 0 repeatable unit tests: repo has no shell test harness, so each case is an embedded deterministic command runnable by `/uat-auto`.

## [2026-07-06] uat | UAT-006 passed (auto) · TASK-006 done
Archived UAT-006 → uat/archive/ and TASK-006 → tasks/archive/. All 4 tests passed headlessly: the `hot.md` template carries the required hot-cache structure, this repo's seeded `wiki/hot.md` has real ROADMAP-001 content, and `sync-wiki-scaffold.sh` both delivers `hot.md` to a fresh project and preserves a customized copy on re-sync (copy-once).

## [2026-07-06] task | TASK-011 Update /wiki-lint to flag knowledge pages with weak or unset confidence provenance
Created task TASK-011 (ROADMAP-001 Phase 3, Provenance & Quality): add a `### 2.10 Provenance / confidence (LOW)` check to `lib/skills/wiki-lint/SKILL.md` that surfaces `knowledge/` pages whose `confidence` is `inferred`/`ambiguous` (weak provenance) or entirely unset (untagged — informational, defaults to `extracted` per convention), per the field TASK-009 activated in `wiki/conventions.md` §4. Parallel-safe with TASK-010 (edits wiki-ingest, not wiki-lint). Numbered TASK-011 after a concurrent TASK-010 collision with the wiki-ingest sibling task. Roadmap linking left to the orchestrator.

## [2026-07-06] uat | UAT-001 UAT: Audit lib/skills for stale README.md-style family-index references
Generated UAT-001 for TASK-001 with 5 test cases: 3 Manual (Audit Findings section present/complete, confirmed-drifting list accurate, not-drift exclusions correctly categorized) + 2 Edge (template layer scaffolds no family `README.md`; convention documents `index.md`+`lifecycle.md` with no `README.md` family role). Note: the downstream fix (TASK-003) has already remediated the drift in the working tree, so accuracy verification of this historical audit relies on human/`git`-history judgment — the accuracy tests are Manual by design. Created 0 repeatable unit tests: audit deliverable is prose findings and the repo has no markdown-assertion test harness.

## [2026-07-06] uat | UAT-009 skipped → TASK-009 done · both archived
UAT skipped for "Activate confidence: extracted|inferred|ambiguous in wiki/conventions.md". Its 5 auto-run tests all fail-closed to human verification (documentation-content assertions with no machine oracle), but the implementation was manually confirmed present and correct in `wiki/conventions.md` §4 — so UAT was skipped rather than walked. Task marked done. Archived UAT-009 → uat/archive/ and TASK-009 → tasks/archive/; checked off TASK-009 in ROADMAP-001 Phase 3 (roadmap stays active — other Phase 0–4 items remain).

## [2026-07-06] uat | UAT-004 passed (auto) · TASK-004 done
Archived UAT-004 → uat/archive/ and TASK-004 → tasks/archive/. All 5 tests passed: the `### Auto Memory vs. this wiki` subsection is present once in CLAUDE.md (line 217), ordered after the Navigation paragraph and before Wiki operations, carries both rule-of-thumb bullets, has an identical heading in the source template `lib/scripts/templates/CLAUDE-wiki.md`, and its body is byte-for-byte in sync between the two files. Verified via Serena `search_for_pattern` + `Read` rather than the embedded shell commands, which the repo's Serena-first hook blocks for markdown reads. Checked off TASK-004 in ROADMAP-001 Phase 1 (roadmap stays active — other Phase 0–4 items remain).

## [2026-07-06] uat | UAT-002 skipped → TASK-002 done · both archived
UAT skipped for "Audit lifecycle.md files and skill templates for stale completed/ references". Its /uat-auto run passed 4/5 deterministic search-based tests (zero-drift finding independently re-confirmed via Serena); the 5th test fail-closes to human verification (a semantic scope judgment on the same already-verified data, not a defect) — so UAT was skipped rather than walked. Task marked done. Archived UAT-002 → uat/archive/ and TASK-002 → tasks/archive/; checked off TASK-002 in ROADMAP-001 Phase 0 (roadmap stays active — other Phase 0–4 items remain).

## [2026-07-06] uat | UAT-007 UAT: Update /wiki-ingest (and other wiki-writing skills) to refresh wiki/hot.md
Generated UAT-007 for TASK-007 with 4 test cases: `/wiki-ingest` defines the canonical `## Step 8: Refresh the Hot Cache` procedure (targets `wiki/hot.md`, regenerate-not-append), its frontmatter description advertises the refresh, the four wiki-writing skills (`roadmap-create`, `task-add`, `decision-finalize`, `req-finalize`) reference the shared procedure by name rather than restating it, and `/wiki-query` wires the refresh only on the file-back path (explicitly never on the read-only answer path). Created 0 repeatable unit tests: all cases are skill-definition markdown-content assertions with no automatable business-logic contract and no markdown test harness in the repo.

## [2026-07-06] uat | UAT-007 passed (auto) · TASK-007 done
Archived UAT-007 → uat/archive/ and TASK-007 → tasks/archive/. All 4 tests passed headlessly via embedded grep oracles: `/wiki-ingest` defines the canonical `## Step 8: Refresh the Hot Cache` (targets `wiki/hot.md`, regenerate-not-append), its description advertises the refresh, the four wiki-writing skills reference the shared procedure by name, and `/wiki-query` scopes the refresh to the file-back path only. Checked off TASK-007 in ROADMAP-001 Phase 2 (roadmap stays active — TASK-008/010 and other items remain).

## [2026-07-06] uat | UAT-001 skipped → TASK-001 done · both archived
UAT skipped for "Audit lib/skills for stale README.md-style family-index references". Its /uat-auto run recorded all 5 tests as fail-closed (4 human-judgment audit-content reviews + 1 deterministic template-scan whose grep command the Serena-first hook blocks — its claim, zero README.md in lib/scripts/templates/wiki/, was independently re-confirmed via Serena). No defects, just no machine oracle for audit-content review — so UAT was skipped rather than walked. Task marked done. Archived UAT-001 → uat/archive/ and TASK-001 → tasks/archive/; checked off TASK-001 in ROADMAP-001 Phase 0 (roadmap stays active — TASK-003, 005, 007, 008, 010, 011 and Phase 4 remain).

## [2026-07-06] uat | UAT-011 UAT: Update /wiki-lint to flag knowledge pages with weak or unset confidence provenance
Generated UAT-011 for TASK-011 with 6 test cases covering the new `### 2.10 Provenance / confidence (LOW)` check in `lib/skills/wiki-lint/SKILL.md`: section heading present, scoped to `wiki/knowledge/` only (excludes `wiki/work/`), distinguishes weak (`inferred`/`ambiguous`) from untagged (missing key) tiers, untagged framed as an informational non-defect + whole check report-only (consistent with conventions.md §4 "omitting means extracted"), cross-references conventions.md §4, and documentation-only scope (no frontmatter backfill). Created 0 repeatable unit tests: all cases are skill-spec markdown-content assertions with no automatable business-logic contract and no markdown test harness in the repo.

## [2026-07-06] uat | UAT-003 UAT: Fix roadmap-create and other skills drifting from the index.md/lifecycle.md convention
Generated UAT-003 for TASK-003 with 6 test cases: 5 deterministic search-based (primary regression gate — zero `wiki/work/<family>/README.md` hits across lib/skills/; roadmap-create retargets to roadmaps/index.md; task-audit writes the dependency graph to its own dependency-graph.md and never index.md/README.md; bug-file/bug-close/task-update point schema/spec to lifecycle.md; primer Step 4 targets tasks/index.md and drops the table-bootstrap) + 1 Manual (residual README.md mentions are all benign non-drift). All 5 deterministic claims were pre-verified via mcp__serena__search_for_pattern at generation time; their embedded grep commands will fail-closed under /uat-auto because the Serena-first hook blocks grep on code paths. Created 0 repeatable unit tests: all cases are skill-definition markdown-content assertions and the repo has no markdown test harness.

## [2026-07-06] uat | UAT-005 UAT: Add Optional tooling pointer (qmd, Hindsight) to CLAUDE.md
Generated UAT-005 for TASK-005 with 5 deterministic tests: the `### Optional tooling` subsection is present in CLAUDE.md, links both the qmd and Hindsight entity pages, carries each tool's deferred-adoption status line ("correctly deferred" / "hold off adopting as default"), both linked entity pages resolve on disk, and the pointer is absent from both the immutable `raw/llm-wiki.md` and the shipped `lib/scripts/templates/CLAUDE-wiki.md` (repo-local by decision — avoids dead links in fresh projects). All 5 pre-verified via Serena at generation time. Created 0 repeatable unit tests: all cases are documentation-content assertions and the repo has no markdown test harness.

## [2026-07-06] uat | UAT-010 UAT: Update /wiki-ingest to populate the confidence field on new/updated knowledge pages
Generated UAT-010 for TASK-010 with 7 test cases covering the confidence-population change to `lib/skills/wiki-ingest/SKILL.md`: Step 3/4 templates carry `confidence: extracted`, Step 5's inline frontmatter list includes `confidence`, the shared "Choosing confidence" note defines all three values, the note scopes confidence to `knowledge/` pages (work artifacts use `status`), a CRITICAL RULE mandates the field and couples `ambiguous` to the contradiction callout, no `/wiki-lint` claim was introduced (inverted absence check), and the definitions match `wiki/conventions.md` §4 (no drift). Created 0 repeatable unit tests: all cases are skill-spec markdown-content assertions with no automatable business-logic contract and no markdown test harness in the repo (`package.json` `test` is a stub).

## [2026-07-06] uat | UAT-011 skipped → TASK-011 done · both archived
UAT skipped for "Update /wiki-lint to flag knowledge pages with weak or unset confidence provenance". Implementation added the `### 2.10 Provenance / confidence (LOW)` check to `lib/skills/wiki-lint/SKILL.md` (weak tier = `inferred`/`ambiguous`; untagged tier = missing key, informational; report-only; scoped to `wiki/knowledge/`; cross-refs conventions.md §4). Its /uat-auto run recorded all 6 tests fail-closed — content/scope assertions whose grep-on-markdown oracles the Serena-first hook blocks; every claim was independently re-confirmed present via Serena, so no defect. Task marked done. Archived UAT-011 → uat/archive/ and TASK-011 → tasks/archive/; checked off TASK-011 in ROADMAP-001 Phase 3 (roadmap stays active — TASK-003, 005, 008, 010 and Phase 4 remain).

## [2026-07-06] uat | UAT-003 skipped → TASK-003 done · both archived
UAT skipped for "Fix roadmap-create and other skills drifting from the index.md/lifecycle.md convention". Implementation (already applied to the working tree) rewrote all 9 formerly-drifting skills off `wiki/work/<family>/README.md`: reads retargeted to the family `index.md`, schema/spec pointed at `lifecycle.md`, task-audit's dependency graph moved to its own `wiki/work/tasks/dependency-graph.md`, and power-mode's task-row removal now handled by UAT-CORE closure. Its /uat-auto run recorded all 6 tests fail-closed — 5 deterministic search-based (incl. the primary regression gate: zero `work/<family>/README.md` hits across lib/skills/) whose grep-on-code oracles the Serena-first hook blocks, each independently re-confirmed TRUE via `mcp__serena__search_for_pattern`; + 1 Manual scope judgment on residual benign README mentions. No defects. Task marked done. Archived UAT-003 → uat/archive/ and TASK-003 → tasks/archive/; checked off TASK-003 in ROADMAP-001 Phase 0 — Phase 0 now fully complete (roadmap stays active — TASK-005, 008, 010 and Phase 4 remain).

## [2026-07-06] uat | UAT-005 passed (auto) · TASK-005 done
Archived UAT-005 → uat/archive/ and TASK-005 → tasks/archive/. All 5 deterministic tests passed headlessly (evidence via Serena search, which the repo's markdown hook mandates over the embedded grep): the `### Optional tooling` subsection exists in CLAUDE.md, links both the qmd and Hindsight entity pages, carries each tool's deferred-adoption status line, both linked entity pages resolve on disk, and the pointer is correctly absent from the immutable `raw/llm-wiki.md` and the shipped `CLAUDE-wiki.md` template. TASK-005 already checked off in ROADMAP-001 Phase 1 — Phase 1 now fully complete (roadmap stays active — TASK-008, TASK-010 and Phase 4 remain).

## [2026-07-06] uat | UAT-008 UAT: Update /primer to read wiki/hot.md first, before Serena memories
Generated UAT-008 for TASK-008 with 6 test cases covering the new `## Step 0: Read the Hot Cache` in `lib/skills/primer/SKILL.md`: Step 0 heading present, Step 0 ordered strictly before `## Step 1: Discover & Read Memories` (the core ordering guarantee), Step 0 locates+reads `wiki/hot.md` via `mcp__serena__find_file`, no-ops silently when the file is absent, frames itself as a first-pass primer that does not replace Serena memory reading, and stays independent of TASK-003's fixed Step 4 task-index check. Created 0 repeatable unit tests: all cases are skill-spec markdown-content assertions with no automatable business-logic contract and no markdown test harness in the repo.

## [2026-07-06] uat | UAT-010 skipped → TASK-010 done · both archived
UAT skipped for "Update /wiki-ingest to populate the confidence field on new/updated knowledge pages". Implementation added `confidence: extracted` to the Step 3/4 frontmatter templates and to Step 5's inline list in `lib/skills/wiki-ingest/SKILL.md`, a shared "Choosing `confidence`" note (three values aligned verbatim-in-meaning with `wiki/conventions.md` §4, scoped to `knowledge/` pages), and CRITICAL RULE 8 mandating the field and coupling `ambiguous` with the contradiction callout. Its /uat-auto run recorded all 7 tests fail-closed — 6 grep-on-markdown content oracles the Serena-first hook blocks + 1 manual semantic-equivalence check; every claim was independently re-confirmed present via `mcp__serena__search_for_pattern` (lines 49, 79, 55-58, 91, 138; no `wiki-lint` reference), so no defect. Task marked done. Archived UAT-010 → uat/archive/ and TASK-010 → tasks/archive/; checked off TASK-010 in ROADMAP-001 Phase 3 (roadmap stays active — TASK-008 and Phase 4 remain).

## [2026-07-06] uat | UAT-008 skipped → TASK-008 done · both archived
UAT skipped for "Update /primer to read wiki/hot.md first, before Serena memories". Implementation (already applied) added `## Step 0: Read the Hot Cache` to `lib/skills/primer/SKILL.md`, ordered before `## Step 1: Discover & Read Memories` — it finds `hot.md` via `mcp__serena__find_file`, reads `wiki/hot.md` as a first-pass primer, skips silently when absent, and explicitly does not replace the Serena memory step; independent of TASK-003's fixed Step 4 (task index → `wiki/work/tasks/index.md`). Its /uat-auto run recorded all 6 tests fail-closed — content/ordering assertions whose grep-on-markdown oracles the Serena-first hook blocks; the core ordering (Step 0 at line 34 precedes Step 1 at line 41) and every other claim were independently re-confirmed via Read/Serena, so no defect. Task marked done. Archived UAT-008 → uat/archive/ and TASK-008 → tasks/archive/; checked off TASK-008 in ROADMAP-001 Phase 2 — Phases 0–3 now fully complete (roadmap stays active — only the deferred Phase 4 advisory-locking item remains).

## [2026-07-06] task | TASK-012 Build wiki-dashboard-server.js zero-dependency static file server
Created task TASK-012: build a zero-dependency Node static file server serving the dashboard client and the project's `wiki/` tree with no-cache headers and port fallback. Linked into ROADMAP-002 Phase 1.

## [2026-08-13] ingest | Automating Obsidian and Plugin Setup in the Bootstrap Scripts
Ingested from raw/research/obsidian-setup-automation/index.md. Key claims: Obsidian app + Dataview/Graph Link Types/Breadcrumbs plugins are both fully automatable (brew/winget/flatpak for the app; GitHub Releases API + manifest.json-driven plugin id + community-plugins.json for the plugins), fitting this repo's existing guarded/sticky-preference install pattern; /wiki-lint's graph-view guidance improves for free but /task-audit does NOT (bespoke Depends-on blockquote, not rel:: typed links). 5 entity pages touched/created (obsidian, dataview, graph-link-types, breadcrumbs-plugin updated; new component page bootstrap-guarded-install-pattern created), 1 concept page touched (typed-wiki-links).

## [2026-07-06] task | TASK-013 Build dashboard.html self-contained live dashboard client
Created task TASK-013: build a single self-contained HTML file that polls each `wiki/work/` family's `index.md`/`archive/index.md` and renders a live view. Parallel-safe with TASK-012. Linked into ROADMAP-002 Phase 2.

## [2026-07-06] task | TASK-014 Wire dashboard command into bin/cli.js
Created task TASK-014: add a `dashboard` command to `bin/cli.js` that launches TASK-012's server. Depends on TASK-012. Linked into ROADMAP-002 Phase 3.

## [2026-07-06] task | TASK-015 Sync dashboard.html into projects as an always-refresh scaffold file
Created task TASK-015: wire `sync-wiki-scaffold.sh` to always-refresh `dashboard.html` into target projects rather than copy-once. Depends on TASK-013. Linked into ROADMAP-002 Phase 3.

## [2026-07-06] task | TASK-016 Document the dashboard command in README.md, lib/scripts/README.md, and CLAUDE.md
Created task TASK-016: document the new `bootstrap dashboard` command across the three usual doc sites. Depends on TASK-012 through TASK-015 (needs the final implemented command surface). Linked into ROADMAP-002 Phase 4.

## [2026-07-06] task | TASK-017 Manually verify dashboard liveness and edge cases
Created task TASK-017: manual verification pass for liveness, live-update behavior, and edge cases (empty state, port fallback, path traversal). Noted that ROADMAP-002's referenced plan-mode verification checklist was never persisted to the repo, so the checklist was reconstructed from the roadmap's stated goal. Depends on TASK-016. Linked into ROADMAP-002 Phase 5.

## [2026-07-06] uat | UAT-012 UAT: Build wiki-dashboard-server.js zero-dependency static file server
Generated UAT-012 for TASK-012 (ROADMAP-002 Phase 1) with 11 test cases: 2 deterministic static checks (`node --check`, exec-bit + shebang) and 9 self-contained headless integration tests (each spawns the server as a child on a dedicated test port 4381–4389, asserts, kills the child, exits 0/1) covering markdown serving with `no-store`/`no-cache`/`Expires:0` + `text/markdown` content-type, 404 on missing file, 403 on encoded `../` traversal to `package.json`, 405 on non-GET, graceful 404 for `/` while `dashboard.html` is absent (TASK-013 pending — flips to 200 later), 404 on a directory path, default-port startup log, `--port` honored, and EADDRINUSE `port+1` fallback. Created 0 repeatable unit tests: `parseArgs`/`contentType`/`noCacheHeaders`/traversal check are pure but the module exports nothing and calls `listen()` at import, and the repo has no JS test runner (`package.json` `test` is a stub) — promoting them needs an out-of-scope source refactor (`module.exports` + `require.main` guard), recorded as a gap/follow-up.

## [2026-07-06] uat | UAT-013 UAT: Build dashboard.html self-contained live dashboard client
Generated UAT-013 for TASK-013 (ROADMAP-002 Phase 2) with 13 test cases: 5 deterministic STATIC checks (no external CDN/font/stylesheet refs, inline `<script>` compiles via `new Function`, exactly six `section.panel[data-family]` panels, `cache: 'no-store'` + `POLL_INTERVAL_MS = 5000` present, the three header hooks `#last-updated`/`#refresh-btn`/`#status-banner`), 3 deterministic EDGE checks that extract `parseIndexMarkdown` from the HTML via `node -e` and exercise it (standard bullet → all four fields, `_(none yet)_`/prose → `[]`, bare bullet → null summary/status), and 5 UI checks needing the TASK-012 server + a browser (six panels render live counts, manual refresh re-fetches + restamps, ~5s poll reflects a live wiki edit without reload, a 404 family degrades to empty state, dark theme under `prefers-color-scheme: dark`). Created 0 repeatable unit tests: `parseIndexMarkdown` is deterministic and promotable but embedded in the single self-contained HTML file (TASK-013's zero-build/zero-dep/single-file constraint) with no module export, and the repo has no wired JS test runner — a permanent unit test needs extracting the parser (violates the single-file constraint) or new test infrastructure, recorded as Blocked on the three EDGE cases; the `node -e` UAT commands are the deterministic repeatable checks meanwhile.

## [2026-07-06] uat | UAT-012 passed (auto) · TASK-012 done
Archived UAT-012 → uat/archive/ and TASK-012 → tasks/archive/. All 11 tests passed headlessly (Node v26): `node --check` clean, exec-bit + `#!/usr/bin/env node` shebang present, default-port startup log, `--port` honored, EADDRINUSE `port+1` fallback, `/wiki/index.md` served 200 with `no-store`/`no-cache`/`Expires:0` + `text/markdown`, 404 on missing file, 403 on encoded `../` traversal to `package.json`, 405 on non-GET, and 404 on a directory path. UAT-EDGE-004 was retargeted at run time from the graceful-404-when-absent branch to the present-file branch (200 `text/html`) because TASK-013 landed `lib/scripts/templates/wiki/dashboard.html` (24 KB) concurrently — the dashboard route now correctly serves it. Checked off TASK-012 in ROADMAP-002 Phase 1 (roadmap stays active — Phases 2–5 remain).

## [2026-07-06] uat | UAT-013 skipped (auto) → TASK-013 done · both archived
Ran UAT-013 via /uat-auto: 8 of 13 tests passed headlessly and 5 UI tests fail-closed (uat-auto does no browser automation). The 8 deterministic passes: `dashboard.html` has zero external CDN/font/stylesheet references, its inline `<script>` compiles via `new Function`, exactly six `section.panel[data-family]` panels are present, `cache: 'no-store'` fetch + `POLL_INTERVAL_MS = 5000` are wired, the three header hooks exist, and `parseIndexMarkdown` (extracted from the HTML via `node -e`) correctly parses a standard bullet into `{title,path,summary,status}`, treats `_(none yet)_`/prose as `[]`, and parses a bare bullet with null summary/status. The 5 fail-closed UI tests (live panel counts, manual-refresh restamp, ~5s live-poll, 404-family empty-state, dark theme) require a running TASK-012 server + browser and are owned by TASK-017's manual verification pass — so per orchestrator direction they don't block closure. UAT-013 marked skipped, TASK-013 marked done; archived UAT-013 → uat/archive/ and TASK-013 → tasks/archive/; checked off TASK-013 in ROADMAP-002 Phase 2 (roadmap stays active — Phases 3–5 remain). Serena MCP was intermittently disconnected during the run; file ops fell back to Read/Edit/Write.

## [2026-07-06] uat | UAT-015 UAT: Sync dashboard.html into projects as an always-refresh scaffold file
Generated UAT-015 for TASK-015 with 3 test cases covering fresh delivery of dashboard.html, always-refresh overwrite of a stale copy, and a copy-once regression check on project-owned index.md. Created 0 repeatable unit tests: none (no shell test harness; tests are rsync/filesystem integration).

## [2026-07-06] uat | UAT-014 UAT: Wire dashboard command into bin/cli.js
Generated UAT-014 for TASK-014 with 4 test cases: help-text lists `dashboard` + port hint (CLI-001), `dashboard` wired in the SCRIPTS map to `wiki-dashboard-server.js` (CLI-002), CLI launches the server and serves the dashboard client over HTTP (INT-001), and a positional port arg is forwarded through so the server binds the override not the default 4317 (EDGE-001). Created 0 repeatable unit tests: blocked — repo has no test framework (placeholder `test` script, zero devDependencies, no test files); INT/EDGE cases are live-server integration and not unit-promotable regardless. Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash.

## [2026-07-06] uat | UAT-015 passed (auto) · TASK-015 done
Archived UAT-015 → uat/archive/ and TASK-015 → tasks/archive/. Verified sync-wiki-scaffold.sh delivers dashboard.html on fresh sync, always-refreshes a stale copy, and preserves copy-once project-owned files (index.md). Checked off TASK-015 in ROADMAP-002 (still active; TASK-014/016/017 open).

## [2026-07-06] uat | UAT-014 passed (auto) · TASK-014 done
Archived UAT-014 → uat/archive/ and TASK-014 → tasks/archive/. All 4 tests passed headlessly: `dashboard` + port-hint appear in cli.js help text, the SCRIPTS map wires `dashboard` → `wiki-dashboard-server.js`, `node bin/cli.js dashboard 4411` launches the server and serves the `<title>Wiki Dashboard</title>` client over HTTP, and a positional port arg is forwarded through (4412 bound, default 4317 down). Checked off TASK-014 in ROADMAP-002 Phase 3 — roadmap stays active (Phases 4–5, TASK-016/017, remain). Note: cli.js spawns the server via execFileSync, so killing the CLI wrapper orphans the child server — cleaned up two leftover processes with pkill during closure. Repo has no unit-test framework, so the two deterministic CLI checks are recorded Blocked on a missing harness. Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash. Concurrent TASK-015 closure had mangled the TASK-014 row in tasks/index.md (joined to TASK-016 with no newline); repaired while removing the TASK-014 row.

## [2026-07-06] uat | UAT-016 UAT: Document the dashboard command
Generated UAT-016 for TASK-016 with 5 test cases (4 content-verification greps across README.md, lib/scripts/README.md, CLAUDE.md; 1 edge check that the stale "forthcoming/TASK-015" placeholder row was removed). Docs-only task, so no runtime tests and no repeatable unit tests created — repo has no markdown-content test harness. Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash.

## [2026-07-06] uat | UAT-016 passed (auto) · TASK-016 done
Archived UAT-016 → uat/archive/ and TASK-016 → tasks/archive/. All 5 content-verification tests passed headlessly: README.md CLI Entry Point enumerates `dashboard` and the Run Locally block shows the default port 4317 + `4400` override example; lib/scripts/README.md's CLI-facing table row for wiki-dashboard-server.js now documents port 4317 + `bootstrap dashboard 4400` override (stale "forthcoming, TASK-014 / owned by TASK-015" placeholder gone); CLAUDE.md Setup Workflow carries the `dashboard [port]` bullet. Checked off TASK-016 in ROADMAP-002 Phase 4 — roadmap stays active (Phase 5 / TASK-017 verification remains). Docs-only task, no repeatable unit tests (no markdown-content harness). Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash. Dashboard feature files are all still untracked (feature not yet committed), so archival used plain `mv` rather than `git mv`.

## [2026-07-06] uat | UAT-017 UAT: Manually verify dashboard liveness and edge cases
Generated UAT-017 for TASK-017 with 7 test cases (3 API, 4 EDGE) covering server bind + dashboard serve, live family-data matching index.md ground truth, no-cache headers, live-edit reflection without restart, empty-state/404 degradation, EADDRINUSE port fallback, and path-traversal rejection (raw + encoded + sibling-prefix). All CLI-drivable via curl/node; no browser required. 0 repeatable unit tests created — 2 promotable-in-principle cases (parser empty-input, path-traversal containment) marked Blocked: repo has no JS unit-test harness (npm test is a stub, no framework/test dir), rest are inherently live-server/socket checks. Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash.

## [2026-07-06] uat | UAT-017 passed (auto) · TASK-017 done
Archived UAT-017 → uat/archive/ and TASK-017 → tasks/archive/ (plain mv — dashboard feature files still untracked/uncommitted). 6 of 7 tests auto-passed headlessly against a live server on 4420/4421: server bind + dashboard.html serve (200, six family panels), roadmaps active data matched index.md ground truth (2 items), no-cache headers (Cache-Control: no-store + Pragma + Expires) on 200 GET, empty-state/404 degradation, EADDRINUSE port fallback (second instance bound 4421), and path-traversal rejection (raw → 403, no /etc/passwd leak). The 7th test (UAT-EDGE-001 live-edit reflection) has no single-command form so uat-auto fail-closed it as manual; it was resolved with a human walk verdict because it had already been manually verified this session with recorded before/after evidence (TASK-999 edit/revert, file restored). Completing TASK-017 checked off ROADMAP-002 Phase 5 — all 6 phases now done, so ROADMAP-002 flipped to status: done and archived → roadmaps/archive/. ROADMAP-002 fully complete. Serena MCP disconnected this session; file ops used Read/Edit/Write/Bash.

## [2026-07-06] task | TASK-018 Upgrade dashboard.html dark mode to a full explicit-override toggle
Created task TASK-018 (renumbered from an initial TASK-012, which collided with an ID already used by the archived wiki-dashboard tasks): upgrade the dark mode shipped by the now-archived ROADMAP-002 (bare `@media (prefers-color-scheme: dark)` swap only) to the full tri-state toggle (`localStorage` explicit choice > OS preference > default) designed during a `/research` plan-mode session and persisted at `raw/research/dark-mode-theme/index.md`. Filed as a standalone follow-up since ROADMAP-002 had already shipped and closed by the time the research/plan was approved.

## [2026-07-06] task | Fixed ID-collision bug: renumbered ROADMAP-002 dashboard tasks/UATs from 001-006 to 012-017
User flagged that the ROADMAP-002 dashboard tasks were numbered TASK-001 through TASK-006, directly colliding with the pre-existing archived ROADMAP-001 tasks of the same numbers — proof visible in `wiki/work/tasks/archive/` holding two files per number. Root cause: `/task-add` Step 4 explicitly instructed *not* to scan `wiki/work/tasks/archive/` when determining the next available number ("Do not look in completed/ or trashed/ subdirectories — files never move"), a stale assumption predating this repo's archive-on-completion convention. The same anti-pattern was found in `roadmap-create` (Step 2 and Step 5), `decision-create` (Step 2), and `req-create` (Step 2) — only `bug-file` already scanned both active and archive directories correctly. Fixed all four skills to scan `<family>/` **and** `<family>/archive/` before computing `max + 1`. Renumbered the 6 affected task files, 6 UAT files, and all cross-references (frontmatter `depends_on`/`blocks`/`parallel_safe_with`/`uat`, body prose, the archived `ROADMAP-002` file's phase links, both archive `index.md` tables, and this log) from TASK/UAT-001..006 to TASK/UAT-012..017. `lib/scripts/templates/wiki/dashboard.html`'s one self-referential source comment (citing its own build task) was updated from TASK-002 to TASK-013; the file's illustrative parser-test fixture strings ("TASK-001 — Build server" as a generic example ID) were left untouched since they're synthetic test data, not real cross-references.

## [2026-07-06] task | TASK-019 Teach dashboard parseIndexMarkdown to parse archive/index.md table rows
Created task TASK-019: extend the dashboard's client-side `parseIndexMarkdown` to recognize the pipe-table format archive `index.md` files use (`| [[ID]] | Title | Final Status | Archived |`), so every family's Archived count stops rendering 0. Standalone follow-up to the archived dashboard feature (ROADMAP-002), first noted during TASK-017 verification. Handles the escaped-pipe edge case in TASK-009's archived title.

## [2026-07-06] task | Dashboard UI/UX rework: hash-routed multi-page client · TASK-018 done · TASK-019 done
Playwright assessment of the live dashboard found broken item links (index-relative hrefs 404ing against `/`), archive counts stuck at 0, clipped/wrapping row layout, and a favicon 404. Rewrote `lib/scripts/templates/wiki/dashboard.html` as a hash-routed single page (server untouched): `#/` overview with per-family summary cards (active count + status-keyword breakdown badges + archived count), `#/<family>` pages with two-line rows and color-coded status badges, and `#/<family>/<file>.md` item pages with an escape-first client-side markdown renderer (frontmatter grid, headings, checkbox lists, tables, code). Absorbed and completed TASK-019 (archive pipe-table parsing incl. escaped-pipe handling — Tasks now shows 17 archived) and TASK-018 (tri-state dark-mode toggle: localStorage > OS preference, persisted across reload, header button outside re-rendered regions). Verified via Playwright at desktop + 375px: no console errors, no horizontal overflow, toggle persistence, archive counts matching ground truth. Both tasks removed from the active index; UAT skipped in favor of the recorded in-session browser verification.

## [2026-07-27] task | TASK-020 Convert brave-search MCP setup to the Docker MCP image
Created task TASK-020: replace the npx-based brave-search MCP registration (`npx -y @modelcontextprotocol/server-brave-search`) in `lib/scripts/install-mcps.sh` and `lib/scripts/setup-project.sh` with the Docker MCP registry image (`docker run -i --rm --name brave-search-mcp -e BRAVE_API_KEY docker.io/mcp/brave-search`). No docker-availability guard, no npx fallback (user decision); fixed container name for Docker Desktop identification, with the concurrent-session name-conflict trade-off documented in the task.

## [2026-07-28] task | TASK-020 updated — persistent-container + docker exec design from research
Reworked TASK-020 per `raw/research/brave-mcp-single-docker-container/index.md`: the `docker run -i --rm --name` plan (which collided on concurrent sessions) is replaced by a `sh -c` wrapper that lazily creates/starts a persistent named container (`brave-search-mcp`, `--entrypoint sleep`, `--init`, no `--rm`) and `docker exec`s `node dist/index.js` per session, with the API key forwarded at exec time. New user decision: force `--scope user` in `_add_brave` so no project entry shadows the single global server. Verification extended with a two-concurrent-session test and a no-baked-key inspect check.

## [2026-07-28] uat | UAT-020 UAT: Convert brave-search MCP setup to a single global Docker container
Generated UAT-020 for TASK-020 with 9 test cases: 5 CFG/static (wrapper string present once in install-mcps.sh, forced user scope, zero live npx references, setup-project.sh echo parity + image-update note, bash -n both scripts) and 4 runtime (smoke test with one named container + exec'd server process, two-concurrent-session sharing, stopped-container revival via the docker start branch, no-baked-key docker inspect). Created 0 repeatable unit tests: repo has no shell test harness (`package.json` test is a stub) — 3 promotable-in-principle CFG cases recorded as Blocked; runtime cases are live Docker/Claude-CLI integration and not unit-promotable.

## [2026-07-28] roadmap | ROADMAP-003 created — Single-process (shared HTTP) MCP servers
Created `wiki/work/roadmaps/ROADMAP-003-single-process-mcp-servers.md` (6 phases 0–5, 6 inline items, all pending task-file creation via `/roadmap-next`). Persists the approved plan derived from `raw/research/mcp-one-process-per-user/`: brave-search → HTTP-mode Docker container (`--restart unless-stopped`, port 8941) superseding TASK-020's exec-wrapper (Phase 0 closes it via `/uat-skip` with a supersession note); playwright → native launchd LaunchAgent on port 8931 (globally-installed package, never npx-under-launchd); shared plumbing (`mcp_matches`/`wait_http_up`/upgrade detection in `register_optional_mcp`); docs alignment incl. the stale "1 req/sec" brave claims; local-machine migration doubling as runtime UAT. Context7 already remote HTTP (no change); serena deferred by user decision; separate declarative MCP config file considered and rejected (provisioning is behavior, not data). Owner: David Taylor.

## [2026-07-28] task | TASK-021…025 created from ROADMAP-003 via /roadmap-next
Upgraded all six ROADMAP-003 inline items to task links. Phase 0 links the existing TASK-020 (closure via /uat-skip — no new task). Created five new tasks: TASK-021 shared plumbing (constants, `mcp_matches`/`wait_http_up`, `register_optional_mcp` upgrade detection, read-EOF fix; blocks 022/023), TASK-022 brave-search → HTTP-mode container (supersedes TASK-020's exec-wrapper; docker-info guard, old-container inspect migration, prefix-assignment `-e` export fix), TASK-023 playwright → launchd LaunchAgent (globally-installed package + direct node invocation, `LimitLoadToSessionType Aqua`, idempotent bootstrap; darwin-only, stdio fallback elsewhere), TASK-024 docs alignment (epilogue, README, CLAUDE.md, stale "1 req/sec" contradiction at build-mcp-guide.sh:100, playwright stub, project-scope shadowing hint), TASK-025 local-machine migration doubling as runtime UAT (upgrade-path exercise, two-concurrent-session process-count assertions, reboot survival, idempotency). Dependency chain: 021 → {022 ∥ 023} → 024 → 025. Task creation confirmations were covered by the approved plan (plan-mode ExitPlanMode, 2026-07-28) rather than re-asked per task.

## [2026-07-28] uat | UAT-020 skipped → TASK-020 done · both archived

UAT skipped for "Convert brave-search MCP setup to a single global Docker container". Task marked done. Archived UAT-020 → uat/archive/ and TASK-020 → tasks/archive/. Reason: the exec-wrapper implementation was real and statically verified (5/5 static checks), but the design is superseded before runtime UAT by ROADMAP-003 Phase 2 / TASK-022 (brave-search → single HTTP-mode Docker container); supersession note (`superseded_by::[[TASK-022]]`) added to TASK-020. ROADMAP-003 Phase 0 checkbox flipped.

## [2026-07-28] uat | UAT-021 UAT: Shared plumbing for single-process HTTP MCP servers
Generated UAT-021 for TASK-021 with 11 test cases: 2 CFG/static (bash -n both scripts; constants-once + 3-arg call-site check, Serena-pre-verified), 7 deterministic shell-functional tests using hermetic `claude` PATH shims (wait_http_up dead-port failure and 404-counts-as-up, mcp_matches fixed-string match/no-match, register_optional_mcp skip paths and stdio→http upgrade path, port-constant defaults and env-override URL derivation), and 2 EDGE EOF-stdin runs of the full script (brave read-EOF guard isolated; requirement-level full-script resilience). Created 0 repeatable unit tests: repo has no shell test harness (`package.json` test is a stub) — 9 promotable cases recorded as Blocked. Discovered gap encoded per test-integrity rules: `_add_context7`'s `read -r CONTEXT7_API_KEY` (install-mcps.sh:108) lacks the `|| true` EOF guard, so UAT-EDGE-002 is expected to fail until the guard is extended.

## [2026-07-28] uat | UAT-021 passed (auto) · TASK-021 done
Archived UAT-021 → uat/archive/ and TASK-021 → tasks/archive/. All 10 tests passed headlessly: bash -n gate, constants-once/3-arg call-site static check (re-verified via Serena after the hook blocked the embedded grep), wait_http_up dead-port + 404-counts-as-up, mcp_matches match/no-match, register_optional_mcp skip + stdio→http upgrade paths via hermetic claude shims, port defaults 8941/8931 + env-override URL derivation, and both EOF-stdin full-script runs — UAT-EDGE-002's expected-to-FAIL note proved stale (the `_add_context7` read guard was fixed post-generation; script now completes with exit=0 with no keys at all). ROADMAP-003 Phase 1 checkbox flipped; roadmap stays active (Phases 2–5 open).

## [2026-07-28] uat | UAT-022 UAT: brave-search → single HTTP-mode Docker container
Generated UAT-022 for TASK-022 with 11 test cases: 4 CFG/static (bash -n both scripts; _add_brave flow content + 4-arg call site; setup-project.sh new-form echo block; zero exec-wrapper/8080 remnants in lib/scripts — all Serena-pre-verified) and 7 hermetic shell-functional/EDGE tests using PATH shims for `claude` AND `docker` (+ `curl`/`sleep`) with invocation logs: docker-down graceful skip, fresh create proving prefix-assignment forwards the read (unexported) key with exact run/registration args, sleep-path rm+recreate migration, node-path start-not-recreate, wait_http_up WARNING branch, end-to-end stdio→http upgrade via full script run, and unknown-entrypoint conservative fall-through. Created 0 repeatable unit tests: repo has no shell test harness (`package.json` test is a stub) — 7 promotable cases recorded as Blocked (precedent UAT-020/021). Live-docker runtime cases explicitly deferred to TASK-025 per the task.

## [2026-07-28] uat | UAT-022 passed (auto) · TASK-022 done
Archived UAT-022 → uat/archive/ and TASK-022 → tasks/archive/. All 11 tests passed headlessly: bash -n gate; three CFG content checks re-verified via Serena after the hook blocked the embedded greps (_add_brave docker guard/migration case/prefix-assigned run/HTTP registration + 4-arg call site; setup-project.sh new-form echo lines 51-52; zero exec-wrapper/8080 remnants under lib/scripts); and all 7 hermetic shim tests exactly matched their expected logs — docker-down graceful skip with no registration, fresh create proving the unexported read key reaches the container env via prefix assignment (key=test-key-123, 127.0.0.1:8941:8941 both sides), sleep-path rm+recreate migration, node-path start-not-recreate idempotency, endpoint-down WARNING branch still exiting 0, end-to-end stdio→http upgrade (remove-then-add) with context7/playwright merely skipped, and unknown-entrypoint conservative fall-through. ROADMAP-003 Phase 2 checkbox flipped; roadmap stays active (Phases 3-5 open).

## [2026-07-28] uat | UAT-023 UAT: playwright → native launchd LaunchAgent HTTP server
Generated UAT-023 for TASK-023 with 13 test cases: 2 CFG/static (bash -n; darwin gate + single-npx-occurrence + plist-key + darwin-only `playwright_expected` check, Serena-pre-verified) and 11 hermetic shell-functional/EDGE tests using PATH shims for `npm`/`launchctl`/`claude`/`curl` (+ `uname`/`sleep` where needed) with HOME overridden to a temp dir and `npm root -g` faked to a temp tree with a stub cli.js: darwin fresh-install happy path (install → plist → bootout+bootstrap → forced-user http registration → listening), generated-plist content audit (node-direct ProgramArguments, port 8931, KeepAlive.SuccessfulExit=false, ThrottleInterval 30, Aqua, logs, WorkingDirectory, zero npx), unchanged+loaded no-op, unchanged+unloaded bootstrap-only, changed-plist bootout+bootstrap+rewrite, non-darwin stdio fallback via uname shim (incl. empty `playwright_expected`), Bootstrap-failed-5 hint path (return 0, registration skipped), generic bootstrap failure (warning + registration + diagnostics), npm-install failure, missing cli.js, and node-missing-from-PATH guards. Created 0 repeatable unit tests: repo has no shell test harness — 11 promotable cases recorded as Blocked (precedent UAT-020/021/022). Live launchd/browser runtime explicitly deferred to TASK-025 per the task.

## [2026-07-28] uat | UAT-023 passed (auto) · TASK-023 done
Archived UAT-023 → uat/archive/ and TASK-023 → tasks/archive/. All 13 tests passed headlessly: bash -n gate; the CFG structural check re-verified via Serena after the hook blocked the embedded grep (darwin gate at 158/276, single npx occurrence at 160 outside the plist heredoc, LimitLoadToSessionType/ThrottleInterval/SuccessfulExit keys, darwin-only `playwright_expected` assignments); and all 11 hermetic PATH-shim tests matched their expected markers — darwin fresh-install happy path (npm install → plist → bootout+bootstrap → forced-user `--transport http` registration → listening), full generated-plist content audit (node-direct ProgramArguments with port 8931, KeepAlive.SuccessfulExit=false, ThrottleInterval 30, Aqua, dual log paths, WorkingDirectory, zero npx — verified by direct Read of the rendered plist after the hook blocked greps on it), unchanged+loaded no-op, unchanged+unloaded bootstrap-only, changed-plist bootout+bootstrap+rewrite, non-darwin stdio fallback with empty `playwright_expected`, Bootstrap-failed-5 hint (rc 0, no registration), generic bootstrap failure (warning + registration + diagnostics), and npm-install/missing-cli.js/node-missing guards. ROADMAP-003 Phase 3 checkbox flipped; roadmap stays active (Phases 4-5 open).

## [2026-07-28] uat | UAT-024 UAT: Docs & guide alignment for single-process HTTP MCP servers
Generated UAT-024 for TASK-024 with 9 test cases covering doc-content assertions (setup-project.sh epilogue, README diagrams/env/troubleshooting, CLAUDE.md manual-setup, straggler skill fixes), two hermetic behavioral runs (build-mcp-guide.sh output, install-mcps.sh shadowing hint + negative control), and a repo-wide stale-claim sweep. Created 0 repeatable unit tests: none (no shell test harness — precedent UAT-020…023).

## [2026-07-28] uat | UAT-024 passed (auto) · TASK-024 done
Archived UAT-024 → uat/archive/ and TASK-024 → tasks/archive/. All 8 tests passed headlessly: five Serena-verified doc assertions (setup-project.sh epilogue rotation/kickstart/port-override story with zero exec-wrapper remnants; README tech row + Docker/launchd/npx architecture and sequence diagrams; README port-env rows, baked-in BRAVE_API_KEY rotation story, and both endpoint-not-answering troubleshooting entries; CLAUDE.md brave :8941 / playwright launchd :8931 bullets with the corrected 50 req/sec parallel figure; decision-create + research-company straggler rate-limit fixes), two hermetic behavioral runs (build-mcp-guide.sh emitted the corrected Quick-Reference row + launchd stub note with zero stale claims; install-mcps.sh shadowing hint fired echo-only for a project-scoped playwright entry and stayed silent in the negative control), and the repo-wide stale-claim sweep (zero 1/sec-sequential/exec-wrapper hits; only the known-permissible mermaid-flowchart :8080 example). ROADMAP-003 Phase 4 checkbox flipped; roadmap stays active (Phase 5 / TASK-025 open).

## [2026-07-30] ingest | Research: gitignored wiki dirs vs tool visibility
Ingested from raw/research/gitignored-wiki-tool-visibility/index.md. Key claims: gitignoring .serena/raw/wiki blinds Serena (GitignoreParser mirrors .gitignore when ignore_all_files_in_gitignore: true; negation escape broken upstream #600) and Claude Grep; .git/info/exclude has identical git semantics but is invisible to Serena (source-verified, file_system.py:176) — adopted as the delivery mechanism instead of .gitignore entries. 1 entity page created (Serena), 1 concept page created (Git-Ignore Tool Visibility).

## [2026-07-30] task | TASK-031 Tier 3 — adopt /sandbox
Created task TASK-031, completing the three-tier model. Closes the one hole the hook tier structurally cannot reach: a Bash-capable agent writing `~/.claude/settings.json` from a **script file**. `interpreter-indirection-guard.js` catches `node -e`, `protected-write-guard.js` catches `>` redirects, `claude-settings-guard.js` catches the file tools — but `node write-settings.js` reaches the file and no hook parses inside a script. Grounded in `raw/research/bypass-mode-enforcement/index.md` §5, which sources to the official docs that *"the sandbox automatically denies write access to Claude Code's settings.json files at every scope … so a sandboxed command can't modify its own policy"*, with symlink resolution since v2.1.210 — exactly the residual risk recorded in that hook's header and both READMEs. The tiers compose rather than overlap: Read/Edit deny rules and `sandbox.filesystem` **merge into one boundary**, so TASK-026's A1/A2 entries strengthen the sandbox. **Step 1 measures breakage before step 2 decides scope** — `install-global.sh` is the interesting case, since it writes `~/.claude/` by design and would fail inside a sandbox that denies settings writes at every scope (likely acceptable: a human runs it, not an unattended agent), and MCP-server impact must be *verified* rather than assumed since Serena backs the mandated navigation path. Step 4 is the acceptance criterion and is a single falsifiable claim: a `node <file>` script writing settings.json **must fail** sandboxed and **must still succeed** unsandboxed, so the test demonstrates the sandbox rather than an unrelated error. Recorded footguns carried forward: do **not** set `disableBypassPermissionsMode` (breaks two skills and three setup scripts), `allowManagedPermissionRulesOnly` (nullifies all 116 deny entries), or `allowManagedHooksOnly` (disables every hook this repo installs). Limits to be documented plainly rather than discovered: sandbox covers Bash and its children only; `~/.aws`/`~/.ssh` stay readable unless configured; the proxy does not terminate TLS; native Windows is unsupported.

## [2026-07-30] task | TASK-030 User preferences
Created task TASK-030: a two-level preference store so skills stop taking consequential actions nobody agreed to — `/git-commit` bumps `package.json` on every commit regardless of whether the project versions that way, and `/research` always writes to `raw/research/`. Global defaults at `~/.claude/bootstrap-preferences.json`, per-project override at the repo root and **git-excluded** (machine-local answer, not a team decision — same reasoning as the `.serena/`/`raw/`/`wiki/` exclusions). Prompted **once during the skill sync** (`install-global.sh`), per user correction — not lazily on first skill run — so skills stay declarative and only read. Keys are **four-state** (`unset`/`true`/`false`/`ask`). "Never asked" must be distinguishable from "asked, declined" or a decline re-prompts forever — and `ask` ("I want to be asked every time") is a *settled answer whose content is keep-asking*, not an unanswered one. `unset` and `ask` both produce a prompt but from different actors at different times: the sync settles `unset` once; the skill prompts on `ask` every run and never persists that answer. Storable values are `true`/`false`/`"ask"`; `unset` is represented by absence alone, so deleting a line is how a question is re-opened (`--unset`). The sync prompt is therefore three-way (yes/no/ask-each-time), which `prompt_yn` cannot express — `prompt_scope` is the multi-choice precedent. Five keys in v1: `gitCommit.autoVersionBump`, `research.persistToRaw` (with a per-run "not this one" override, so a single report can be discarded without changing the default), `gitignore.offerSectionUpdates`, `gitCommit.autoPush` (default off — it turns a local action into a published one), `uatGenerate.promoteTests`. **One open design decision recorded as step 1:** the sentinel block's canonical form is currently *exactly* three entries, enforced by the normalizer shipped in TASK-029 — adding the prefs file either extends canonical form (test churn, all exclusions in one place) or sits outside it (no churn, but bootstrap-owned exclusions split across two locations). Recommendation: extend it. Also noted while scanning: TASK-018 and TASK-019 are `done` on disk but were never archived and are absent from the index — pre-existing drift, worth a `/wiki-archive` sweep.

## [2026-07-30] fix | Removed the bootstrap-claude carve-out from claude-settings-guard.js
Closed a self-permission hole found by using it. While clearing a UAT test artifact, a plain `Edit` call rewrote the live `~/.claude/settings.json` from inside this repo — the carve-out working exactly as specified, and visibly too broad. **The premise behind it was wrong:** it existed because "this repo manages those settings", but the repo writes them via `node merge-settings-deny.js` *inside* `install-global.sh` — a Bash subprocess no `PreToolUse` hook or permission rule ever observes. Nothing here ever needed the Edit *tool* on those paths, so the exception cost real safety and bought nothing. The block is now **unconditional**; `isBootstrapRoot`/`findBootstrapRoot` and the marker constants are deleted as dead code. **Honest limit recorded in the header and both READMEs:** this guards the file *tools* only — `node write-settings.js` still reaches them, since no hook parses inside a script file. It is a guardrail, not a boundary; `/sandbox` remains the only real containment. **Three tests rewritten rather than deleted** — the shapes that formerly earned the exception (marker pair present, nested under a marker root, a differently-named genuine checkout) now assert **deny**, making them the most sensitive canaries for the carve-out creeping back; a fourth asserts the reason no longer advertises an exception that does not exist. Deny entries deliberately left out of `settings-deny.json`: the merge is additive-only with no removal path, and the hook already covers `Write`/`NotebookEdit`, which `Edit(...)` rules never reach. 108/108 green. Also in this pass: `merge-gitignore.sh` now **prints the actual lines** each section would add before asking (a bare "3 new line(s)?" asked for consent to something invisible), and a single opening gate replaces per-section prompting for users who want no `.gitignore` changes at all — the `.git/info/exclude` offer stays outside that gate, so declining still lets the sentinel repair run.

## [2026-07-30] uat | UAT-028 + UAT-029 passed (walk) · TASK-028 + TASK-029 done
Closed both via `/uat-walk`. **UAT-028 21/21, UAT-029 16/16**; all four files archived, both tasks `done`, both active indexes now empty.
**UAT-028 `SESSION-003`** — the three prescribed commands (`rm -rf ~`, `cat .env`, `$(curl …)`) were **not** run literally; each is safe only if the control under test works, so running them to test it is circular. Step 1 was substituted with `bash -c "crontab -l"` — `Bash(crontab *)` is also a deny-list entry, so the path is identical with nothing at stake. **This settled the residue the auto-run flagged:** the returned message was the *hook's* (``Blocked inside `bash -c` — `crontab -l` matches the permission deny rule `Bash(crontab *)` ``), not the harness's, so a deny-listed command inside `-c` really is judged by the hook. Steps 2 and 3 ran as written and matched verbatim.
**UAT-028 `SESSION-004`** — user added `Bash(kubectl delete *)` to their own `~/.claude/settings.json`; **no installer ran in between**; `bash -c "kubectl delete pod api-7"` blocked naming the rule, `bash -c "kubectl get pods"` **executed** (reached the network, `connection refused`) — stronger evidence than a success, since it proves the binary ran. Runtime-read property confirmed end-to-end on the real settings file.
**Incidental live confirmation:** removing that rule afterward was done with the plain `Edit` tool and **succeeded** — the first real-world exercise of `claude-settings-guard.js`'s bootstrap-claude carve-out (marker-file identified, not path name). It also surfaced that the carve-out is broader than needed: the repo's own settings writes go through `node merge-settings-deny.js` *inside* `install-global.sh`, i.e. Bash subprocesses no PreToolUse hook ever sees — so the Edit-tool exception is not required for the repo to function, and the in-code comment asserting otherwise is wrong. Closing it is pending.
**UAT-029 `MANUAL-001`** — passed on **a second machine**, which is stronger than any fixture: that machine held a genuine pre-existing broken repo (three paths in `.git/info/exclude`, no sentinel — Case A, arrived at naturally). The user ran the update script there, `merge-gitignore.sh` took the unprompted pure-repair branch, and **autocomplete was restored**. Full chain observed in the wild: real broken state → shipping code path repairs it → picker re-includes → `@` completes. The earlier in-repo attempt explicitly did **not** count toward this verdict — bootstrap-claude has no sentinel and all three dirs are git-tracked (12/54/138), so its completion came from the base `rg --files` listing and would have worked with the built-in picker.

## [2026-07-30] uat | UAT-028 + UAT-029 re-run after install — 32 pass / 3 open, 0 product defects
Re-ran both UATs with `uat-auto` after `install-global.sh` was run (user-consented). **UAT-028: 19 pass / 2 fail** (was 18/3). **UAT-029: 13 pass / 1 fail** (was 11/3, three previously blocked). `npm test` 108/108. **Zero product defects across both** — every remaining failure is a safety or environment limit.
**UAT-028.** `SESSION-002` now passes end-to-end: the three one-liners were issued as real Bash tool calls in a live session and each executed. `SESSION-003` **deliberately not executed** — it prescribes issuing `rm -rf ~`, `cat .env`, and `$(curl …)` for real, and each is safe *only if* the control under test works, so running them to test it is circular. Evidence gathered read-only instead (the installed hook's three reasons confirmed verbatim), plus live matcher routing proven at zero risk via `bash -c "bash -c \"bash -c 'true'\""` — payload `true`, harmless even if allowed — which the installed guard intercepted with `nested 3 levels deep`. **A genuine residue was identified:** `Bash(rm -rf ~*)` is itself a deny-list entry, so in a live session the harness may block that case *before* the hook runs and render its own message rather than the hook's — and the hook's phrasing is exactly what the case pins. Unsettleable without issuing the command. `SESSION-004` needs the user to add and remove a rule in their own `~/.claude/settings.json`; not done. `SESSION-001` **inverted by design** (the installed hook now allows `bash -c "echo hi"`); left at `[x] Pass` with a dated note, since the verdict vocabulary has no "intentionally superseded" token and the marker correctly records what was true when made. The Scope section's now-stale precondition prose was corrected separately, original kept inline for provenance.
**UAT-029.** `INSTALL-003` passed satisfied-by-prior-run — installed picker md5 `fd71f63b…` is **byte-identical to the current template**, so the post-fuzzy-fix copy is what shipped. `INSTALL-004` passed against the installed copy directly. **Every previously-passing case was re-run rather than trusted**, because `file-suggestion.sh` changed underneath them; all 11 still pass. `MANUAL-001` recorded **FAIL, deliberately not a clean pass**: the user's live `@wiki/ho` confirmation succeeded, but this repo has no bootstrap sentinel and `.serena/`/`raw/`/`wiki/` are git-**tracked** here (12/54/138 files), so the completion came from the base `rg --files` listing — the observable passed while the re-inclusion mechanism was never exercised. Closing it requires one human observation in a scratch fixture *with* a sentinel; repeating it in this repo will not. Two stale-wording items noted without editing: `UNIT-001` says 106/106 (now 108/108 — its real assertion, `fail 0`, holds) and `EXCLUDE-008` says "four hot files" where five are correct (that fixture never excludes `secret/`). Security properties re-verified against the live template after the fuzzy change: `-v`, `.*`, `*`, `[`, `^`, `$`, `(`, `)`, `\`, `--help`, `$(touch pwned)` all return zero, exit 0, no `pwned` created.
**Neither task can close.** Both UATs stay `in-progress`; nothing archived. `.git/info/exclude` md5-identical before and after.

## [2026-07-30] uat | UAT-028 run headlessly — 18 pass / 3 blocked / 0 fail
Ran UAT-028 with `uat-auto`. **18 PASS, 3 BLOCKED, 0 real defects.** `node --test test/command-class-hooks.test.js` → 64 pass / 0 fail (the pinned file-scoped count); `npm test` 106/106. Perf held under concurrent load: common path **49.6 ms/call** (inside the ~60 ms threshold), interpreter path **262 ms/call**, ratio 5.3× — matching the task's recorded 46/270 ms. **The runner did not trust a green `node:test` line on its own** — a passing test with an empty body is indistinguishable from a real one, so it read the test source and counted the assertions behind every case (32 spelling-pair assertions, 5 sibling sentinels, the empty-stderr fail-closed pin, 14 malformed-stdin shapes, …); all matched except three clerical miscounts. **3 documentation inaccuracies found, all internal to the UAT document, judged PASS rather than FAIL** because every behavior the Steps enumerate verifies correct and marking them FAIL would send someone to fix a non-problem: UNIT-005 says "12 allows" but asserts 11; EDGE-005 says "7 decisions" but holds 6 (and its prose says "the two `echo` rows" then lists three); EDGE-006 step 3's `WebFetch` pass-through assertion lives in a sibling test the prescribed `--test-name-pattern` does not match (green in the full-file run). Each recorded inline beside its verdict. **3 BLOCKED pending the user re-running `install-global.sh`** — `UAT-SESSION-002/003/004`; the underlying logic is green against fixtures in UNIT-003/004/006/010. `UAT-SESSION-001` confirmed the precondition by firing the *installed* hook and getting the pre-change blanket-deny text, and is expected to **invert** to `allow` after re-install — that inversion is the signal the other three have unblocked, not a regression.

## [2026-07-30] uat | UAT-029 run headlessly — 11 pass / 3 blocked / 0 fail
Ran UAT-029 with `uat-auto`. **11 PASS, 3 BLOCKED, 0 FAIL, 0 INCONCLUSIVE; zero real defects and zero documentation inaccuracies** — every prescribed command produced its documented output verbatim, including both pty idioms and the `.gitignore` pre-seeding, and nothing hung. `npm test` 106/106 in 28.3s. The 8 `UAT-EXCLUDE-*` cases each **reproduced the bug before asserting the fix** (no-sentinel showed only `src/hotsrc.txt`; split above/below showed `.serena/hotcache.txt` + `src/hotsrc.txt`), then verified the canonical block, idempotency by md5, the stranded-entry two-line note appearing once and not on re-run, `raw/private/` surviving the `raw/` scrub, `640` mode preservation, and the non-interactive early exit. **3 BLOCKED pending explicit consent to run `install-global.sh`** — it mutates `~/.claude/settings.json` via an additive merge with no removal path, so `UAT-INSTALL-003` and its two dependents (`-004`, and the manual `@`-autocomplete case) were not run; verified afterward that `~/.claude/file-suggestion.sh` is still absent. `UAT-EDGE-002` landed on its documented "resolved negative" branch — the malformed control again produced identical silence, re-confirming the `claude --debug -p` channel is dark and adds no evidence about the settings key. One forced deviation: a `bash -c '…'` step was blocked by the live interpreter guard and routed through a script file with identical semantics, byte-identity confirmed by md5. This repo's `.git/info/exclude` md5-identical before and after; all fixtures under one `mktemp -d`, removed.

## [2026-07-30] uat | UAT-029 UAT: Ship fileSuggestion @-autocomplete restoration
Generated UAT-029 for TASK-029 with 16 test cases (1 unit-suite, 8 `UAT-EXCLUDE-*` normalization, 4 `UAT-INSTALL-*`, 2 `UAT-EDGE-*`, 1 manual). Promoted 19 assertions into `test/`: a new `test/file-suggestion.test.js` (18 tests — sentinel re-inclusion, both bug-reproduction states, scoping vs a user's own exclusions, comment-terminated block, scrambled order, `raw/private/` non-substring, hostile queries, the 15-cap, the `rg`→`git ls-files`→`find` fallback chain, installed-location invocation, exec bit) plus one in `test/settings-deny.test.js` asserting all 116 deny entries survive the `fileSuggestion` key merge and a second install is byte-identical. Suite 106/106 green. **Two findings:** (1) the proposed cheap automated complement — `claude --debug -p` to confirm the settings key parsed — **does not work**; an A/B against a deliberately malformed `fileSuggestion` produced identical silence on both streams, so the channel cannot distinguish "accepted" from "silently ignored". Recorded as `UAT-EDGE-002`, a resolved negative, rather than a false pass; integration confidence rests entirely on the one manual case. (2) `merge-gitignore.sh` normalization is **not unit-testable** — it exits without a tty at `:40-43`, so `node:test` can never reach it; the 8 exclude cases drive it through `script(1)`, and the harness only runs unattended because the scratch repo's `.gitignore` is seeded from the shipped template, making every section prompt a no-op so the exclude prompt is the only one that fires. Every command was executed verbatim before being written down; each Expected Result is transcribed from observed output.

## [2026-07-29] task | TASK-028 Interpreter guard — recursive re-evaluation
Created task TASK-028: replace `interpreter-indirection-guard.js`'s blanket deny of `bash -c`/`node -e`/`python3 -c` with recursive re-evaluation — extract the payload, unquote one level, and re-run it through the sibling guards plus the deny list, blocking only what would have been blocked directly. Supersedes TASK-027 step 2's deny-outright decision. The overturning argument: the hook's own documented escape hatch (write the script to a file and run it) is also its complete bypass, so it was never an adversarial control — which dissolves the "obfuscation defeats payload inspection" objection while leaving the friction cost real.

## [2026-07-29] task | TASK-027 Tier-2 PreToolUse hooks
Created task TASK-027: four small PreToolUse hooks gating command classes deny rules cannot express — interpreter indirection (`bash -c`), package-install consent (with the `oraios/serena` allowlist deny provably cannot express), absolute-path invocation (`/bin/rm`), and redirects/env-injection into protected paths. Depends on TASK-026. Also corrects the false "deny is not enforced under bypassPermissions" rationale where it appears in hook source comments.

## [2026-07-29] task | TASK-026 Audit and harden the canonical settings deny list
Created task TASK-026: research current agent sandbox-escape vectors, then audit lib/scripts/templates/settings-deny.json and add out-of-project write protection (file-tool deny rules) plus hardened Bash patterns, compatible with the additive merge flow.

## [2026-07-29] ingest | Research: Agent Sandbox-Escape Vectors Relevant to Claude Code
Ingested from `raw/research/agent-sandbox-escape-vectors/index.md` (TASK-026 step 5, ingest 1 of 3). Key claims: Claude Code permissions are a CLI-enforced string-matching layer, not an OS boundary, so a deny rule blocks a literal spelling and never a capability; only `Edit(path)`/`Read(path)` rules are consulted (`Write(...)` is accepted then silently ignored) and a single leading slash in user settings anchors at `~/.claude/`, not `/`; nine persistence vectors (shell profiles, launchd/cron, `curl | sh`, `.env` source-gap, git `core.fsmonitor`, `DYLD_*`/`LD_PRELOAD`, settings self-modification, `osascript`, symlink escape) resolve to a three-tier deny → hook → sandbox model. 4 entity pages touched (2 new tools, 1 new component, Serena updated with the `execute_shell_command` surface), 3 new concept pages, 1 new source page. One claim flagged as contradicted by the later bypass-mode report.

## [2026-07-29] ingest | Research: Deny rules vs. PreToolUse hooks
Ingested from `raw/research/deny-rules-vs-hooks/index.md` (TASK-026 step 5, ingest 2 of 3). Key claims: Claude Code decomposes compound commands into subcommands *before* matching — proven by the fact that approving `git status && npm test` saves a rule for `npm test` — so a bare `Bash(sh)` deny catches the target of `curl x | sh` while any pipe-containing pattern can never fire, and **no startup warning exists** for such a dead rule; `permissions.ask` is a first-class tier that cannot be silenced by an allow rule, `bypassPermissions`, a hook returning `allow`, or sandbox auto-allow, which makes it — not a hook — the right mechanism for package-install consent, since deny has no yes-path and no rule can carry an allowlist exception; the widely-taught "deny `npm install *` + allow `npm install --ignore-scripts`" pattern is therefore broken. 3 entity pages touched (1 new component `bootstrap-claude-hooks`, permission-system and deny-list updated), 2 new concept pages + 2 updated, 1 new source page.

## [2026-07-29] ingest | Research: Which Claude Code controls survive --dangerously-skip-permissions
Ingested from `raw/research/bypass-mode-enforcement/index.md` (TASK-026 step 5, ingest 3 of 3). Key claims: deny rules, ask rules, PreToolUse hooks, and the sandbox are **all enforced under `bypassPermissions`** — only `permissions.allow` goes inert — so the premise that blocked TASK-026 was false; the control bypass actually destroys is Claude Code's **built-in protected-path list** (`.claude`, `.gitconfig`, `.zshrc`, `.zshenv`, `.npmrc`, `.mcp.json`, …), which is Prompted in default/acceptEdits, Denied in dontAsk, and **Allowed** under bypass — making the deny list's self-modification and shell-profile groups worth *more* under bypass, not less; managed settings are self-applicable via `sudo` (not MDM-only), but `allowManagedPermissionRulesOnly` and `allowManagedHooksOnly` would nullify this repo's own rules and hooks. 4 entity pages touched (permission-system, sandbox, deny-list, hooks), 1 new concept page + 3 updated, 1 new source page. **Two contradictions flagged:** (1) this report supersedes `agent-sandbox-escape-vectors` §C7's claim that bypass "still gates writes to `.claude`" — callout carried on `knowledge/concepts/permission-mode-control-survival.md`, with the source page marked `confidence: ambiguous`; (2) an unresolved disagreement between this report (hook) and `deny-rules-vs-hooks` (`permissions.ask`) on the mechanism for headless package-install consent — callout on `knowledge/concepts/consent-requires-a-yes-path.md`. Also recorded: this repo's `lib/hooks/README.md:14-19` and `:311-312` state the opposite error (deny not enforced under bypass) and need correcting.

## [2026-07-29] uat | UAT-026 UAT: Audit and harden the canonical settings deny list
Generated UAT-026 for TASK-026 with 13 test cases covering the three checks TASK-026 explicitly deferred (per-subcommand pipeline decomposition via `echo … | sh` with a `bash` control; the two-token `Bash(git stash:*)` startup-warning question with a `Write(...)` control; and merge behavior against a scratch settings file), plus template integrity (118 entries, the 36 legacy entries byte-identical at indices 0–35, zero `Write(...)` rules, zero duplicates), a workflow regression batch (`bash -n`, `node script.js`, `python3 -m`, `npm install -g`, `git status` must all stay permitted), and the two doc-correction assertions. Every runtime test builds a throwaway project-scoped `.claude/settings.json` — the real `~/.claude/settings.json` is never merged into, because `Edit(~/.claude/settings.json)` is unremovable once installed. Created 1 repeatable unit test file: `test/settings-deny.test.js` (13 assertions, `node:test`, zero dependencies), wired to `npm test`.

## [2026-07-29] uat | UAT-026 passed (auto) · TASK-026 done
Archived UAT-026 → uat/archive/ and TASK-026 → tasks/archive/. All 13 tests passed headlessly, settling the three checks TASK-026 deferred: **Claude Code does decompose a pipeline and match each subcommand independently** — in a project denying only `Bash(sh)`, `echo … | sh` was blocked ("Permission to use Bash with command sh has been denied") while the `bash` control ran and created its marker, validating the 10 bare-interpreter entries and re-confirming deny is enforced under `--dangerously-skip-permissions`; the two-token `Bash(git stash:*)` form drew **no** startup warning while the `Write(...)` positive control did fire on stderr, closing the Step 2 residual uncertainty; and the merge proved additive, idempotent (byte-identical re-run), creating, and fail-safe on a malformed target (exit 0, file untouched). Template integrity confirmed at 116 entries / 93 Bash / 11 Edit / 12 Read / 0 Write, 36 legacy entries byte-identical at indices 0–35, zero duplicates, no fetcher or installer rules. All five workflow commands stayed permitted under the full list. The real `~/.claude/settings.json` was never merged into — verified unchanged by hash before and after.

## [2026-07-29] task | TASK-025 runtime UAT executed · pending-uat · ROADMAP-003 Phase 5 flipped
Cross-referenced TASK-025 against TASK-026/027 first: annotated the reboot-survival step (TASK-027's planned `absolute-path-guard.js` bare-basename `launchctl` block would deny `kickstart` — hook should match destructive subcommands only) and the idempotency step (`BRAVE_API_KEY` env prerequisite given the `Read(**/.env)` deny + planned `source .env` block); set `parallel_safe_with: [TASK-027]`. Then ran Step 2 live with two concurrent sessions: all four checks PASS — exactly one `brave-search-mcp` container and one `@playwright/mcp/cli.js` process with zero per-session brave/playwright stdio children, live `brave_web_search` + `browser_snapshot` succeeded, both endpoints answered within 1s of `docker restart` + `launchctl kickstart -k` with no re-registration, and the installer re-run reported "already installed, skipping" for all three MCPs without restarting anything. Evidence recorded in the task file; ROADMAP-003 Phase 5 (its last open item) checked off; task flipped to `pending-uat`.

## [2026-07-29] uat | UAT-025 skipped → TASK-025 done · both archived · ROADMAP-003 done + archived

UAT skipped for "Migrate this machine's MCP registrations to the single-process design (runtime UAT)". Task marked done. Archived UAT-025 → uat/archive/ (skeleton created skipped — the task was itself the runtime UAT; live evidence in its `## Verification Evidence` section) and TASK-025 → tasks/archive/. With Phase 5 flipped, all ROADMAP-003 checkboxes are `[x]`: roadmap `status: done`, removed from the active index, archived → roadmaps/archive/. No decision references TASK-025.

## [2026-07-29] uat | UAT-027 UAT: Tier-2 PreToolUse hooks — gate command classes deny rules cannot express
Generated UAT-027 for TASK-027 with 23 test cases (20 unit-backed, 3 session-blocked) covering the six new command-class hooks and the shared `command-parse.js` helper, which had zero runtime verification at task close. Created 1 repeatable unit test file: `test/command-class-hooks.test.js` — 55 test cases / ~330 assertions, `node:test`, zero deps, following `test/settings-deny.test.js`; suite total is now 69 tests. Every hook's positives, negatives, allowlist, marker-spoof, and documented-gap behavior verified passing, including the `oraios/serena` carve-out, the bootstrap-claude marker pair (spoofed and half-marker directories correctly rejected; a differently-named directory carrying both markers correctly accepted), `Write`/`MultiEdit`/`NotebookEdit` coverage on `~/.claude/settings.json`, and the `.env` guard on both the Bash and Serena surfaces. **One defect found**: `readHookInput` (`lib/hooks/lib/command-parse.js:17-27`) calls `handler(data)` outside a `try/catch`, so a JSON `null` payload crashes five of the six hooks with exit 1 — violating the fail-open contract that file's own header states. `env-content-read-guard.js` survives only because it wraps its handler locally. One `try/catch` in the helper fixes all five; the assertion is written against the contract, so `npm test` is 64 pass / 5 fail until it lands. Two documented "known false positives" were also found to be stated inaccurately in source comments (`echo "bash -c foo"` and `echo "add it with >> ~/.zshrc"` both allow — the quote character defeats the match); the code is correct, the prose is not, and both spellings are now pinned.

## [2026-07-29] uat | UAT-027 run (auto) — 23 passed, 3 session-blocked · not archived
Re-ran UAT-027 headlessly after the `readHookInput` fix landed. `npm test` is now **69 pass / 0 fail** (was 64/5), and the defect the previous run found is confirmed closed at `lib/hooks/lib/command-parse.js:29` — `handler(data)` is now wrapped in its own `try/catch`. Verified independently of the suite with a 228-case probe harness (0 flagged): all six hooks exit 0 on eight malformed payloads (`null`, `42`, `"hello"`, `[]`, `{}`, garbage text, empty stdin, `{"tool_name":123}`) — 48 combinations, none emitting output; deny still fires (`/bin/rm -rf ~`) and allow still passes (`rm build/out.js`); helper exports exactly `deny`/`readHookInput`/`splitSegments`/`tokenize` and all six hooks pass `node --check`. Both prose-vs-code discrepancies confirmed empirically and are **documentation defects, not test failures**: `echo "bash -c foo"` allows while unquoted `echo bash -c foo` denies (so `TASK-027` step 2 and `lib/hooks/README.md` are wrong), and `echo "add it with >> ~/.zshrc"` allows while the unquoted form denies (so the `protected-write-guard.js:50-53` example is not an instance of the class it illustrates). The three `(session)` cases stay unrun — they need `PreToolUse` matcher wiring in `~/.claude/settings.json` that this UAT deliberately does not perform — and are recorded with their preconditions quoted, so the file keeps blocking markers and is **not** archived; UAT-027 → `in-progress`, TASK-027 unchanged. Also found: the UAT's own Summary table undercounts — the file contains **26** test cases (23 unit-backed + 3 session), not the 23 it claims. Nothing was written to `~/.zshrc`, `~/.claude/`, or `~/Library/LaunchAgents/`; `install-global.sh` was not run.

## [2026-07-29] ingest | Research: git exclude vs Claude Code @ autocomplete
Ingested from `raw/research/git-exclude-at-autocomplete/index.md` (+ `sources.md`). Key claims: `.git/info/exclude` blinds Claude Code's `@` file picker (and ripgrep-class tools — rg documents honoring `$GIT_DIR/info/exclude`; verified by local experiment and anthropics/claude-code#5657), and recent versions suggest only git-*tracked* files (#40082/#45012), so no git-side layout (untracked, nested embedded repos — #15192) restores autocomplete; the documented `fileSuggestion` settings key replaces the picker with a custom command and is the escape hatch — recommendation is to keep `info/exclude` and ship a sentinel-scoped `fileSuggestion` script + settings merge. 1 new source page, 1 new tools entity (Claude Code @ File Picker), 1 concept page corrected (Git-Ignore Tool Visibility — resolved contradiction: "visible to agents" narrowed to Serena; prior source page carries a superseded_by callout).

## [2026-07-30] uat | UAT-028 UAT: Rework interpreter-indirection-guard from blanket deny to recursive re-evaluation
Generated UAT-028 for TASK-028 with 21 test cases (17 unit-backed, 1 perf, 1 read-only precondition, 3 session-blocked). Created 8 new repeatable unit tests in `test/command-class-hooks.test.js` (~87 assertions); that file goes 56 → **64 pass / 0 fail** (whole-suite total not pinned — `test/settings-deny.test.js` is under concurrent TASK-029 development and its count moves; `npm test` was green at 106/106 after the change). The pre-existing 6 interpreter tests judge the guard's *output*; the 8 added here judge its *machinery*, because re-evaluation has a failure mode reading the verdict cannot detect — `readHookInput`'s catch exits 0 (= allow), so a throw escaping `askSibling` would silently flip fail-closed to fail-open while every output-level assertion still passed. Each new case runs the guard from an `fs.mkdtemp` copy of the hook directory with exactly one thing about the install broken, and requires a **deny**: a missing sibling, non-JSON stdout, a non-zero exit, a sibling that never answers (2s budget), and an unreadable or malformed deny list — all with empty stderr, which is what actually pins fail-closed. Also newly pinned: every one of the five `SIBLING_GUARDS` is really spawned and read (unique-tagged denying stubs — dropping one from the list was previously invisible, since `bash -c "cat .env"` would still deny via the deny list); the synthesized envelope carries the *extracted* payload plus the session `cwd`; the deny list is read from disk at call time in both layouts, proved by a fixture where `rm -rf ~` **allows** because it is absent from that list; the four `patternToRegExp` semantics (word boundary, `:*` suffix, exact match, mid-pattern wildcard) reproduced through `bash -c`; `cwd` changing the verdict for the identical `bash -c "echo x > .zshrc"` ($HOME denies, repo allows); and the counterweight — a totally broken install still allows `npm test` and a `Read` call, so fail-closed stays scoped to interpreter commands. **Precondition established, not assumed**: `~/.claude/hooks/interpreter-indirection-guard.js` was fired directly and confirmed to still be the pre-change blanket-deny build (denies `bash -c "echo hi"`), so the 3 session cases are genuinely blocked — `install-global.sh` was **not** run and `~/.claude/` was not modified. Measured cost on the repo copy: ~45 ms/call with no interpreter vs ~264 ms with one, matching the task's recorded 46/270 ms.

## [2026-07-29] task | TASK-029 Ship fileSuggestion @-autocomplete restoration
Created task TASK-029: restore @ autocomplete for the info/exclude-hidden dirs via a `fileSuggestion` custom-picker script (sentinel-scoped re-inclusion), a `--set-key` generalization of merge-settings-deny.js wired into install-global.sh, and narrowing of the over-broad "invisible to the tools" prose in merge-gitignore.sh / templates/gitignore / READMEs. parallel_safe_with TASK-027, TASK-028. (Filed as TASK-028, renumbered same-day: a concurrent session claimed 028 for the interpreter-guard task.)

## [2026-07-29] uat | UAT-027 passed (auto) · TASK-027 done
Archived UAT-027 → `uat/archive/` and TASK-027 → `tasks/archive/`. All **26** cases now pass: the 23 unit-backed cases via `npm test` (**69 pass / 0 fail**), and the three `(session)` cases that the two prior runs recorded as blocked. Those became runnable because the wiring was done independently between runs — `install-global.sh` was run and the `PreToolUse` blocks were registered in `~/.claude/settings.json` (117 deny entries). **UAT-INT-001**: all five Bash guards fire live under the `Bash` matcher, each returning its own `permissionDecisionReason` rather than a generic denial, while the negatives (`npm test`, `bash -n lib/scripts/lib.sh`, plain `rm -f`) still run — the `/tackle` static gate is intact in a live session. **UAT-INT-002** (the one that mattered most): the Serena half of `env-content-read-guard.js` is **not silently inert** — `search_for_pattern` and `get_symbols_overview` on `.env`, and the `paths_include_glob: '**/.env'` scoping form, were all blocked as live MCP calls, while `search_for_pattern` on `package.json` returned normally. The `Bash`-only-matcher symptom (Bash blocks, Serena passes) did not occur. **UAT-INT-003**: `~/.claude/hooks/lib/command-parse.js` exists at the nested path — the rsync did not flatten it — and all six installed hooks exit 0 on a benign payload with no `MODULE_NOT_FOUND`. The hooks also fired on the executing agent's *own* commands mid-run (`node -e`, `bash -c`, `cat .env`, `tail` on markdown), which is stronger evidence of live routing than any probe. Two payloads were made inert to honour the no-side-effects constraint (a nonexistent package name for the install gate; `false >> ~/.zshrc`, which appends zero bytes) — neither changes which hook rule is exercised. Nothing was written to `~/.zshrc`, `~/.claude/`, or `~/Library/LaunchAgents/`; `~/.claude/settings.json` was read but never modified. No fixes were applied (`uat-auto`, not `uat-auto-plus`); none were needed.

## [2026-07-31] roadmap | ROADMAP-004 created — Resilient hook install + automated settings.json hooks wiring
Created `wiki/work/roadmaps/ROADMAP-004-resilient-hook-install.md` (3 phases, 7 inline items, all pending task-file creation via `/roadmap-next`). Motivated by a new machine where an interrupted `setup` left global hooks missing and `update` never recovered them: the MCP step runs before the hook install unguarded under `set -e`, and settings.json hook registration is a manual README paste. Phases: wiring engine (`settings-hooks.json` template + `merge-settings-hooks.js` with "template owns its blocks" semantics + tests), install flow (reorder/guard `install-global.sh` + `lib.sh`), docs & release. Implementation plan approved at `~/.claude/plans/ok-now-parallel-cerf.md`. Owner: David Taylor.

## [2026-07-31] task | TASK-032–038 created — ROADMAP-004 materialized (power-mode)
Created seven tasks from ROADMAP-004's inline placeholders in one parallel batch with pre-assigned IDs (avoiding the concurrent-numbering race that renumbered TASK-029): TASK-032 settings-hooks.json template extraction, TASK-033 merge-settings-hooks.js ("template owns its blocks"), TASK-034 test/settings-hooks.test.js (invariants + merge coverage), TASK-035 install-global.sh reorder/guard/wire, TASK-036 lib.sh run_project_sync guard, TASK-037 docs sweep, TASK-038 fake-HOME verification + release. Dependency chain: 032→033→{034∥035}→037→038, with 036 independent. All roadmap items upgraded to task links; execution proceeding wave-by-wave under /power-mode.

## [2026-07-31] uat | UAT-032 generated for TASK-032 (settings-hooks.json template extraction)
Generated `wiki/work/uat/UAT-032-settings-hooks-template.md` with 6 test cases (5 `UAT-FILE-*` static-inspection cases + 1 `UAT-EDGE-*` scope-boundary case) covering: bare top-level key shape, byte-for-byte structural equivalence with the README's inline JSON block, preservation of the `mv-absolute-path-block.js` `if: "Bash(mv *)"` filter, the 18-script hooks↔template bijection (`serena-usage-tracker.js` counted twice), npm-tarball inclusion, and confirmation that no merge script or README rewrite leaked into this task's scope. All 6 commands were run and pass as of generation. No repeatable unit test file was created — every assertion is deferred to TASK-034's `test/settings-hooks.test.js`, which explicitly owns these invariants, per this run's scoping constraint against duplicating that suite.

## [2026-07-31] uat | UAT-036 generated for TASK-036 (run_project_sync reorder + guard)
Generated `wiki/work/uat/UAT-036-lib-sh-mcp-guard.md` with 3 test cases (1 unit-backed, 2 live/static edge cases) covering the reordered, guarded `run_project_sync()`. Created 5 repeatable unit tests in new file `test/run-project-sync.test.js` (sources the real `lib.sh` against stubbed sub-scripts): install-global.sh precedes install-mcps.sh, a failing install-mcps.sh only warns and does not abort under `set -euo pipefail`, the four downstream steps still run afterward, a successful install-mcps.sh prints no warning, and — the scope check — a failure in a *different* step (sync-wiki-scaffold.sh) still aborts, proving only install-mcps.sh is guarded. Full suite green (113/113). **UAT-EDGE-001 executed live during generation**, closing the task's own deferred verification step: a throwaway "fake repo" (real `lib/scripts` copy with `install-mcps.sh` stubbed to exit 1; `lib/skills`, `lib/hooks`, `raw/` symlinked back to the real repo) run against a scratch `$HOME` and scratch project, with Serena registered locally for that scratch project path. Result: exit 0, the warning printed, `install-global.sh` preceded the failing MCP step in the output, and all of hooks/skills/`settings.json`/wiki-scaffold/`CLAUDE.md`/`mcp-tools.md`/`.serena/project.yml` were produced despite the guarded failure. UAT-EDGE-002 statically confirms both real callers (`setup-project.sh`, `update-project.sh`) still invoke `run_project_sync "$PROJECT_DIR" "$SCRIPT_DIR"` unchanged. Nothing was written to the real `~/.claude` or `~/.claude.json`.

## [2026-07-31] uat | UAT-036 passed (auto) · TASK-036 done
Archived UAT-036 → uat/archive/ and TASK-036 → tasks/archive/. All 3 tests were already `[x] Pass` at resolution (UAT-UNIT-001 via the 5-case `test/run-project-sync.test.js` suite, UAT-EDGE-001's live scratch-`$HOME` end-to-end run, and UAT-EDGE-002's static caller check), so `/uat-auto` proceeded straight to closure with no re-execution needed. Checked off TASK-036 in ROADMAP-004 Phase 2 (roadmap stays active — TASK-032/033/034/035/037/038 remain).

## [2026-07-31] uat | UAT-032 passed (auto) · TASK-032 done
Archived UAT-032 → uat/archive/ and TASK-032 → tasks/archive/. All 5 tests passed headlessly: the template's top-level keys are exactly `SessionStart`/`PreToolUse`/`PostToolUse`/`PostToolUseFailure` with no wrapping `hooks` key, its content is byte-for-byte structurally identical to the README's inline JSON block, `mv-absolute-path-block.js` retains its `if: "Bash(mv *)"` filter, the 18-script hooks↔template bijection holds (`serena-usage-tracker.js` counted twice, all others once), and `npm pack --dry-run` confirms the template ships in the tarball (187 files total). Scope-boundary edge case also confirmed: no `merge-settings-hooks.js` yet, README inline JSON block untouched. Checked off TASK-032 in ROADMAP-004 Phase 1 (roadmap stays active — TASK-033/034/035/037/038 remain).

## [2026-07-31] uat | UAT-033 generated for TASK-033 (merge-settings-hooks.js block-ownership merge)
Generated `wiki/work/uat/UAT-033-merge-settings-hooks.md` with 10 test cases (6 `UAT-CLI-*` merge-contract cases + 4 `UAT-EDGE-*` safety/edge cases) covering: fresh create with default `--source` resolution, no-`hooks`-key create preserving sibling keys and tab indentation, byte-identical idempotent no-op with no `.tmp-` residue, missing-block/missing-entry append with `UserPromptSubmit` untouched, drift adoption of a renamed pure-owned matcher, in-place replacement of a drifted owned entry (`if: "Bash(mv *)"` restored at the same index), relocated-into-mixed-block warn-without-write, the compound empty-placeholder-block scenario, 4 malformed-input warn-skip-exit-0 sub-cases, and the same-basename foreign path outside `.claude/hooks/` staying foreign. All commands were dry-run at generation: 9 of 10 pass; **UAT-EDGE-002 is a confirmed known-FAIL** — when one template block is fully relocated and another block carries a real change in the same run, an empty `{"matcher":"Grep","hooks":[]}` placeholder block is written to disk (repro embedded in the test; routed to the fix loop, not fixed here). Commands use classic `function(){}` style because `serena-bash-grep-block` false-positives on `=>` followed by a `.js` string. No repeatable unit test file created — every assertion is reserved for TASK-034's `test/settings-hooks.test.js` per the parallel-wave scoping constraint. Every invocation used `--target`/`--source` scratch files; the real `~/.claude/settings.json` was never touched.

## [2026-07-31] uat | UAT-033 passed (auto) · TASK-033 done
Archived UAT-033 → uat/archive/ and TASK-033 → tasks/archive/. All 10 tests re-run fresh and passed headlessly, verifying merge-settings-hooks.js's full contract: fresh create with default `--source` resolution, sibling-key/tab-indent preservation, byte-identical idempotent no-op with no `.tmp-` residue, missing-block/entry append with `UserPromptSubmit` untouched, drift adoption, in-place owned-entry replacement, relocated-into-mixed-block warn-without-write, all 4 malformed-input warn-skip cases, and same-basename foreign paths staying foreign. The generation-time known-FAIL UAT-EDGE-002 (empty `{matcher, hooks: []}` placeholder persisted on a compound run) now **passes** — the deferred-block-push fix holds against the exact confirmed repro. Checked off TASK-033 in ROADMAP-004 Phase 1 (roadmap stays active — TASK-034/035/037/038 remain).

## [2026-07-31] uat | UAT-034 generated for TASK-034 (test/settings-hooks.test.js meta-verification)
Generated `wiki/work/uat/UAT-034-settings-hooks-tests.md` with 6 test cases (3 `UAT-CLI-*` + 3 `UAT-EDGE-*`). Because TASK-034's deliverable *is* a test suite, this UAT is meta-verification: exact-count green run (`npm test` 134/134/0, with glob pickup proven by arithmetic — 113 from the four sibling files + 21 from `settings-hooks`), per-file counts and coverage of the plan's required case list by test name (all 21 fragments incl. the 18-hook bijection, the three-writer sequence, and both deferred-push fix cases), an independent from-scratch recheck of the 18-hook bijection, two vacuity probes with temporarily-broken fixtures (3 targeted template/hooks-dir breaks → exactly the 3 matching invariant tests fail, 18 green; a no-op `process.exit(0)` merge-script stub → exactly the 14 merge cases fail, 7 invariants pass — both restored byte-identical and re-run green in the same command), and a runtime safety check that a full `npm test` leaves the real `~/.claude/settings.json` stat-identical (never read). All six commands dry-run and pass at generation. Corrected TASK-034's implementation note: the file contains 21 tests (7 + 14), not 20 (7 + 13). No new unit test file — the deliverable under verification is the unit suite itself.

## [2026-07-31] uat | UAT-035 generated for TASK-035 (install-global.sh reorder/guard/wire)
Generated `wiki/work/uat/UAT-035-install-global-reorder.md` with 6 test cases (4 `UAT-CLI-*` + 2 `UAT-EDGE-*`) covering the reordered `install-global.sh`: the six-step order as observed in output (hooks → skills → deny merge → hooks-wiring merge → fileSuggestion → MCPs last), fake-HOME `settings.json` ending up with deny + hooks wiring + fileSuggestion all present, idempotent re-run (three "already up to date" outcomes, byte-identical settings, no restart nudges), preserved `--skip-mcps` skip, the missing-`lib/hooks` stderr-warning-and-continue path (run from a copy with the dir renamed), and MCP-failure resilience (stubbed failing `install-mcps.sh` without `--skip-mcps`: warning on stderr, exit 0, all local installs intact). Created 6 repeatable unit tests in new file `test/install-global.test.js` (runs the real script + real merge scripts from a scratch template copy with a stubbed `install-mcps.sh` and scratch HOME, modeled on `test/run-project-sync.test.js`) — all 6 pass. Every live UAT command is scoped to a `mktemp -d` scratch: `HOME` is always overridden and the real `install-mcps.sh` is never executed.

## [2026-07-31] uat | UAT-034 passed (auto) · TASK-034 done
Archived UAT-034 → uat/archive/ and TASK-034 → tasks/archive/. Re-run of the single pending case UAT-CLI-001 after its stale-arithmetic amendment (suite grew 134→140 when `test/install-global.test.js` landed post-generation): `npm test` is exactly 140/140/0 with glob pickup proven by arithmetic (119 from the five sibling files + 21 from `settings-hooks`), and a fresh stat snapshot confirmed the run left the real `~/.claude/settings.json` untouched (size 8808, mtime identical). The five previously-recorded passes (exact per-file counts/coverage-by-name, independent 18-hook bijection recheck, both vacuity probes, and the EDGE-003 safety check) stand unchanged. Checked off TASK-034 in ROADMAP-004 Phase 1 — Phase 1 complete; roadmap stays active (TASK-035/037/038 remain).

## [2026-07-31] uat | UAT-035 passed (auto) · TASK-035 done
Archived UAT-035 → uat/archive/ and TASK-035 → tasks/archive/. All 6 tests ran headlessly against scratch template copies with a stubbed `install-mcps.sh` and an overridden fake HOME (real `$HOME` and real MCP install never touched), verifying the TASK-035 reorder: six banners in the new order with MCPs last, fake-HOME settings.json carrying 116 deny entries + hooks wiring (PreToolUse et al.) + fileSuggestion with hooks/skills/picker installed, byte-identical idempotent re-run with all three "already up to date" outcomes and no restart nudges, `--skip-mcps` fully skipping the MCP step, missing-`lib/hooks` warning on stderr with the run continuing to completion, and a failing `install-mcps.sh` warning on stderr while exiting 0 with all local installs intact. Checked off TASK-035 in ROADMAP-004 Phase 2 — Phase 2 complete; roadmap stays active (TASK-037/038 remain).

## [2026-07-31] uat | UAT-037 generated for TASK-037 (automated hooks-wiring docs)
Generated `wiki/work/uat/UAT-037-hooks-wiring-docs.md` with 10 test cases (8 `UAT-DOC-*` + 2 `UAT-EDGE-*`), all static verification for the docs-only task: header/closing-reminder automation claims vs the shipped `install-global.sh`, pointer-replaces-inline-JSON (zero wiring-JSON copies in the README, link target exists), merge-semantics prose vs `merge-settings-hooks.js` (incl. the documented opt-out limit from finding 2), matcher-gotcha prose byte-checked against `templates/settings-hooks.json`, restart-reminder claim verified true against the post-fix case pattern `*'hooks wiring: created'*|*' applied'*` with no doc quoting the old broken pattern, `lib/scripts/README.md` rows vs recomputed step order in `install-global.sh`/`run_project_sync`, 4-events/18-scripts bijection recomputed from disk, root `CLAUDE.md` bullet sweep, residual stale-phrasing sweep (11 hits, all negations or unrelated), and raw/-untouched scope containment. All 9 scripted commands dry-run green at generation. Created 0 repeatable unit tests: every deterministic assertion is already covered by `test/settings-hooks.test.js` (bijection, 4-event, matchers) and `test/install-global.test.js` (six-step order, restart-reminder-on-applied) — 141 tests total.

## [2026-07-31] uat | UAT-037 passed (auto) · TASK-037 done
Archived UAT-037 → uat/archive/ and TASK-037 → tasks/archive/. All 10 static-verification tests passed headlessly: lib/hooks/README.md header and closing reminder now describe automated wiring via merge-settings-hooks.js merging templates/settings-hooks.json (manual pasting retired), the inline wiring JSON is replaced by a single pointer to the template (zero duplicate copies), merge-semantics prose matches shipped code incl. the opt-out re-append limit, matcher-gotcha prose is byte-consistent with the template, the restart-reminder claim is true of the fixed case pattern with no doc quoting the old broken one, lib/scripts/README.md rows and root CLAUDE.md bullets match the recomputed install-global.sh/run_project_sync step order (local steps first, MCPs last and guarded), the 4-events/18-scripts bijection recomputed true from disk, the stale-phrasing sweep returned only the 11 expected negation/unrelated hits, and nothing under raw/ was touched. Checked off TASK-037 in ROADMAP-004 Phase 3 — roadmap stays active (TASK-038 remains).

## [2026-07-31] release | TASK-038 verification green + v2.17.0 release commit
Ran TASK-038, ROADMAP-004's acceptance gate, entirely against scratch state: npm test 141/141 green (settings-hooks 21, install-global incl. the restart-reminder-on-applied regression, run-project-sync); fake-HOME install-global.sh --skip-mcps delivered all 18 hooks + lib/, 59-skill mirror, and a settings.json with 116 deny entries, template-identical hooks wiring, and fileSuggestion, with a byte-identical idempotent re-run printing all three "already up to date/set" lines; forced MCP failure (stubbed install-mcps.sh in a repo copy) at both entry points warned on stderr and continued — install-global.sh exited 0 with all local installs intact, and setup-project.sh's run_project_sync still reached sync-wiki-scaffold/build-mcp-guide/bootstrap-serena; the missing-lib/hooks path warned and completed; npm pack --dry-run ships settings-hooks.json + merge-settings-hooks.js (188 files, no raw/ leakage). Bumped 2.16.0 -> 2.17.0 and cut the [minor] release commit on main containing the full ROADMAP-004 body of work. No git push, no npm publish (left to the user). TASK-038 -> pending-uat.

## [2026-07-31] uat | UAT-038 UAT: Fake-HOME end-to-end verification of resilient hook install + hooks wiring, and the minor release
Generated UAT-038 for TASK-038 with 6 test cases (all UAT-CLI-*), confirmation-of-record for the v2.17.0 release gate: commit b85cbe9 on main with the exact [minor] style + both trailers, HEAD exactly one unpushed commit ahead of origin/main with a clean code tree, package.json 2.17.0 in tree and in the commit, npm test fully green (141 at the release commit, 144 now), tarball shipping settings-hooks.json + merge-settings-hooks.js with zero raw/research / raw/companies / PDF leakage (188 files), and a live fake-HOME idempotency spot re-run (scratch HOME only, byte-identical settings.json, all three no-op lines). Deliberately does not re-run the full verification matrix — that lives in the repeatable suite. Created 1 repeatable unit test file: test/npm-pack-contents.test.js (3 tests promoted from the tarball case; suite 141 -> 144).

## [2026-07-31] uat | UAT-038 passed (auto) · TASK-038 done
Archived UAT-038 → uat/archive/ and TASK-038 → tasks/archive/. All 6 CLI tests passed headlessly: release commit b85cbe9860ff on main with the exact [minor] Release 2.17.0 subject and both trailers verbatim; HEAD=main=b85cbe9 exactly one unpushed commit ahead of origin/main (7908676) with the working tree dirty only in this UAT's own bookkeeping files; package.json 2.17.0 in tree and in the commit; npm test 144/144 green (0 fail, 0 cancelled); npm pack --dry-run ships settings-hooks.json + merge-settings-hooks.js + settings-deny.json at 188 files with zero landing-zone/PDF leakage; live scratch-HOME idempotency spot re-run — both installer runs exit 0, settings.json byte-identical (matching SHA-256), all three no-op lines in run2, scratch dir discarded, real $HOME untouched. Checked off TASK-038 in ROADMAP-004 Phase 3 — every roadmap box now [x]; roadmap left in place for orchestrator archival.

## [2026-07-31] archive | ROADMAP-004 — completed, moved to archive
All 7 items checked (TASK-032–038, each through the full tackle → uat-generate → uat-auto pipeline under /power-mode, same-day create-to-complete). Status → done; moved to wiki/work/roadmaps/archive/ROADMAP-004-resilient-hook-install.md via git mv; active index row removed; archive index row appended. Shipped as release 2.17.0 (b85cbe9, unpushed): MCP-failure-proof install ordering + automated settings.json hooks wiring, tests 108→144. Two defects found and fixed by the pipeline itself: the merge's empty-placeholder-block write (UAT-033 EDGE-002) and the restart-reminder case pattern never matching the applied path (TASK-037 doc sweep).

## [2026-08-06] lint | 23 issues found, 4 fixed
Full lint pass: 2 HIGH, 3 MEDIUM, 18 LOW. Fixed — TASK-018 and TASK-019 (both `status: done`, stranded in the active tasks dir since 2026-07-06) `git mv`'d to `tasks/archive/` with rows appended to the archive index; the non-vocabulary typed link `supersedes-claim-in::[[gitignored-wiki-tool-visibility]]` in `knowledge/concepts/git-ignore-tool-visibility.md` normalized to `supersedes::`; orphaned `wiki/hot.md` given an inbound link from the home index header. Clean on family-index drift, two-domain violations, ID/filename mismatch, UAT↔task cross-links, stale frontmatter, and archive-index↔file parity (tasks now 36/36, uat 34/34, roadmaps 3/3). Left open by design: 3 contradiction callouts (two documented as resolved/superseded, one genuinely open — `ask` rule vs. hook for headless package consent, `permission-mode-control-survival.md:45`), 10 never-ingested raw sources (5 `raw/research/` dirs plus `llm-wiki.md`, `llm-wiki-2.md`, `design-principles.md`, `harness-notes.md`, the case-study PDF), 5 weak-provenance pages (1 `inferred`, 4 `ambiguous`), and 11 untagged-provenance pages. Log at 385 lines — under the 500-line rotation threshold.

## [2026-08-06] skill | /wiki-query auto-ingests relevant raw sources on a coverage miss
Replaced `/wiki-query`'s dead-end "no coverage → suggest /wiki-ingest" branch with a real fallback (new Step 4): diff `raw/` against the `sources:` back-links in `knowledge/sources/` to find un-ingested files, rank them cheaply against the question (slugs + keyword search + headings, never full reads), announce the picks, then run the full `/wiki-ingest` procedure with its Step 2 checkpoint downgraded to a non-blocking announcement. Capped at 3 sources and one re-answer per invocation so a query can never cascade into an ingest loop; `raw/research/<slug>/` and `raw/companies/<slug>/` count as one candidate each. Old Step 4 renumbered to 5, keeping the "never on the read-only answer path" hot-cache guarantee an archived UAT greps for. Descriptions synced in CLAUDE.md (3 rows), `templates/CLAUDE-wiki.md`, and `lib/skills/README.md`; installed globally.

## [2026-08-06] roadmap | ROADMAP-005 created — preference store, absorbing TASK-030
Created ROADMAP-005 (4 phases, 14 items) from an approved plan at `~/.claude/plans/ensure-that-all-of-luminous-simon.md`. Exploration established there is no preference store anywhere in the repo: stickiness is inferred from side effects, so accepting a prompt stops it while declining re-asks forever across all ~17 installer prompts — worst case the Playwright conflict menu, which re-asks even after being answered because options 1 and 2 leave indistinguishable states. Only `sync-wiki-scaffold.sh:171` behaves correctly, because both its answers write a sentinel. Mid-planning the roadmap was found to overlap TASK-030 (created 2026-07-30), which had already designed the skill-consent half and did it better in one respect — a four-state model (`unset`/value/`false`/`ask`) where `ask` means "keep prompting, don't persist", a distinction the new plan had missed. TASK-030 is absorbed rather than duplicated: its four-state model and five consumers are adopted, the store moves to ROADMAP-005 Phase 1, and TASK-030 narrows to its steps 3–4 (sync prompt pass + consumer wiring) with `part_of: ROADMAP-005`. Four conflicts reconciled and recorded in both files — store renamed to `bootstrap-prefs.json`/`bootstrap-prefs.js` under `.claude/`; `gitCommit.autoVersionBump` becomes `gitCommit.versionBump` with `auto`/`confirm`/`never` (making `confirm` that key's `ask` state); a `consumer` schema field splits installer-asked from sync-asked keys; and TASK-030's "the project file must never be committed" requirement is **superseded** by a new `prefs.gitTracking` three-way choice (`.gitignore` / `.git/info/exclude` / neither). Also verified the store is unguarded — `claude-settings-guard.js` covers only `~/.claude/settings*.json` and `~/.claude/hooks/`. No implementation started; no commit.

## [2026-08-06] task | TASK-039 Add extensive inline comments to the hook scripts
Created TASK-039: targeted why-not-what commenting pass over `lib/hooks/`. A survey of all 21 files found the problem is unevenness, not volume — the command-class guards are already 35–50% comments with headers that near-verbatim restate their README sections, while the Serena-first hooks sit at 4–18% while carrying the densest heuristics (~40 regexes in `serena-bash-grep-block.js` alone). So the task is heavy on nine thin files, gap-fill only on the six dense guards, and records a deliberate, `lib/hooks/`-scoped suspension of the repo's no-comments default. Comments-only: three likely defects surfaced by the survey (`mv-absolute-path-block.js` using `process.cwd()` not `data.cwd` and omitting `|` from its segment split; the duplicated `.env` predicate) are annotated and routed to `/bug-file` rather than fixed inline.

## [2026-08-06] bugs | BUG-0001–0008 filed from the TASK-039 commenting pass
Opened the bugs family (previously empty). Four enforcement-bypass bugs filed individually because each needs its own fix and regression test: BUG-0001 `serena-first-guard.js:73` non-code path denylist is unanchored, so `mydocs/`, `myknowledge-vault/`, `applogs/` silently skip Serena enforcement (the Glob guard's `NON_CODE_PATH` was anchored for exactly this; this side was missed); BUG-0002 a brace glob mixing code and non-code extensions fails open — `**/*.{ts,json}` matches the pre-filter but the extension re-extraction finds nothing, leaving `ext === ''` → `exit(0)`; BUG-0003 the zero-width bypass `SYMBOL_ZERO_WIDTH` exists to close is still open on Grep (Glob sets `stripZeroWidth`, Bash strips in `clean`, Grep does neither and `rejectRegexSpecials` does not catch it); BUG-0004 `serena-pre-delegation.js` emits `decision: 'warn'`, undocumented for PreToolUse — suspected no-op that would make the hook forced-explorer/worktree-only, confirmation path recorded. Three original divergences: BUG-0005 `mv-absolute-path-block.js` uses bare `process.cwd()` where all five sibling guards prefer `data.cwd`; BUG-0006 the same file omits `|` from its segment split — investigated and **not** a bypass (args span the pipe, so the traced evasion cases still deny), the real effect is arg-bleed causing false positives on piped `mv`; BUG-0007 `isBlockedEnvFile` duplicated byte-for-byte across `env-file-guard.js` and `env-content-read-guard.js` with the warning on only one copy, already flagged in `lib/hooks/README.md:522-525`. BUG-0008 consolidates the ten remaining message-only, dead-code, and latent findings (wrong-phase `sed -i.bak`, `indexOf('ls')` target extraction, the `.docs/` allowlist note that contradicts `isExemptDir`, three dead-code items, `exit(2)`-after-write truncation, the `alreadyRead` `nextReadNum` ordinal, the pre-lock gate snapshot, the `glob`+`kebabComponents` trap, ungated bare `uvx`, and the two documented false positives). No `.js` file was modified.

## [2026-08-06] docs | TASK-039 closed out — commenting standard recorded in lib/hooks/README.md
Completed the targeted, comments-only why-not-what pass over `lib/hooks/`: no behaviour changed anywhere, and the scope was deliberately asymmetric — heavy on the nine thin Serena-first hooks whose inline comments *are* their only documentation, gap-fill only on the six command-class guards whose README sections already carry the argument. Added a `## Commenting standard` section to `lib/hooks/README.md` recording the `lib/hooks/`-scoped suspension of the repo's no-comments default (five comment classes, the mandatory per-file header block, `serena-usage-tracker.js:4-32` as the calibration target), noted the same exception in CLAUDE.md's `lib/hooks/` bullet, and repaired the `.js:<line>` citations the pass itself invalidated (`env-file-guard.js` `:39`→`:81` and `:6-13`→`:48-55`; `serena-bash-grep-block.js` `:126`/`:160`/`:189`→`:312`/`:393`/`:454`). The pass surfaced 16 suspected defects, filed as BUG-0001–0008, plus one correction to the standard itself: fail-**closed** is not a property of the command-class guards as a group — the shared `readHookInput` and the two hand-rolled stdin handlers all exit 0 on a throw, so only `interpreter-indirection-guard.js` genuinely fails closed; the rest fail open on infrastructure and block only on an ambiguous match.

## [2026-08-06] task | TASK-040 Canonical preference key registry — bootstrap-prefs-schema.json
Created TASK-040, the first of ROADMAP-005 Phase 1: the single canonical description of every bootstrap preference key at `lib/scripts/templates/bootstrap-prefs-schema.json`, read by three surfaces (the `bootstrap-prefs.js` helper's `--list` and generated README, the installer scripts' sticky prompts, and `/bootstrap-config`) so they cannot drift. 19 entries — 14 static `consumer: "installer"` keys plus two wildcard patterns, and 5 `consumer: "skill"` keys from TASK-030. Three design calls made while writing it. **Dynamic keys get a wildcard form** (`guides.*`, `gitignore.section.*`) with `"dynamic": true` and exact-match-then-wildcard lookup, because enumerating the optional guides and the eight `templates/gitignore` section titles would hard-code them in a second place — precisely the drift the file exists to stop; section titles are slugified (lowercase, non-alphanumeric runs → `-`) since they are free-text banner comments containing spaces and slashes. **`gitignore.review` is dropped** — the plan's name for the master review gate and TASK-030's `gitignore.offerSectionUpdates` are one prompt, and the behavior-naming one wins. **`default` encodes today's behavior, not the desired behavior** (`research.persistToRaw` → `true`, `gitCommit.autoPush` → `false`, `gitCommit.versionBump` → `"auto"`, every installer key → `null`), and is schema metadata never written to a values file, since writing a default would convert an unanswered question into a settled answer and permanently suppress its prompt. The `.gitignore` declines-only invariant is encoded in the data rather than left to call sites: `gitignore.section.*` carries a one-value grammar of `false`, so an accepted section is never recorded and a genuinely new template section is still offered next run.

## [2026-08-06] task | TASK-041 bootstrap-prefs.js — four-state preference helper
Created TASK-041 (depends on TASK-040), the second of ROADMAP-005 Phase 1: the zero-dependency Node helper every later phase goes through, reading and writing `~/.claude/bootstrap-prefs.json` and `<project>/.claude/bootstrap-prefs.json`. Modelled on `merge-settings-deny.js` (`warnSkip`, `readTarget`, `detectIndent`, tmp+`renameSync`) but explicitly **not** reusing its `--set-key`, whose never-clobber rule at `:133-145` makes flipping a stored `true`→`false` impossible — correct for a deny list, wrong for preferences where changing your mind is the point. Four reconciliations recorded. **Exit codes encode whose fault it is**: `exit 0` for any unexpected *world* state (missing file, malformed JSON, unset key) so a corrupt prefs file can never abort an install under `set -euo pipefail` or block a commit, and `exit 1` reserved for a wrong *caller* (invalid `--value`, unknown flag, write with no layer selector). **Resolution is the default on `--get`, not a `--resolve` flag** as the plan proposed — forgetting the flag would silently return `unset` and re-prompt, which is the exact failure the roadmap exists to remove; `--target` remains the single-file escape hatch and test seam. **Resolution is `scope`-constrained**, so a `global`-scope key sitting in a project file is not consulted. **Value coercion**: the tokens `true`/`false` become JSON booleans and everything else a string — storing `"false"` as a string would be truthy in every shell test and read back as a settled `true`. A malformed file degrades to empty on read but **blocks writes rather than being overwritten**, since clobbering a hand-edited file would destroy answers to recover a machine convenience. Validation derives entirely from the schema's `values` string, so `gitignore.section.*`'s one-value grammar enforces the `.gitignore` declines-only invariant with no call-site cooperation.

## [2026-08-06] task | TASK-042 test/bootstrap-prefs.test.js — four-state and schema-bijection coverage
Created TASK-042 (depends on TASK-041), closing ROADMAP-005 Phase 1 — which is a strict chain, 040 → 041 → 042, with no parallelism. Modelled on `test/settings-deny.test.js` (`node:test`, `mkdtempSync` fixtures, a `spawnSync` wrapper so `status` and `stderr` are both assertable) and deliberately split into two populations kept separate in the file: behaviour tests that drive the helper as a subprocess against `--target`/`--schema` scratch fixtures, and static schema tests that assert facts about the shipped `bootstrap-prefs-schema.json` without running anything — mixing them is what lets a drift bug hide behind a passing behaviour suite. Three assertions singled out as load-bearing. The **`ask`-reads-back-as-`ask`** case gets its own named test rather than a line inside a round-trip, because `unset` and `ask` both produce a prompt and a helper that conflates them is invisible in normal use. **No `null` anywhere on disk** is asserted after every sequence, since a helper writing `"key": null` would pass a naive round-trip while breaking the absence-is-`unset` contract. **Coercion is a typed assertion** (`strictEqual(v, false)`, never `'false'`), the mistake that would read back as a settled `true` in every shell test. The schema→scripts half of the bijection is written now but `test.skip`'d with a reason naming Phase 2 as the un-skip point, since no script calls the helper yet — writing it as an always-passing no-op would look like coverage without being it; correspondingly the scripts→schema extractor is proven against known-good and known-bad fixture strings so an extractor matching nothing cannot masquerade as a pass. Verification requires **seeing the suite fail on purpose** (break `--get` for a stored `ask`, confirm red, revert) before it counts as evidence.

## [2026-08-06] uat | UAT-040 UAT: Canonical preference key registry — bootstrap-prefs-schema.json
Generated UAT-040 for TASK-040 with 16 test cases covering the schema's parseability and flat shape, the seven-field/two-dynamic contract, the scope and consumer populations (5/9/5 and 14/5), the `.gitignore` declines-only one-value grammar and its non-application to the three-state keys, `script:line` citation accuracy, the `mcp.context7Scope` grammar correction, the absence of `gitignore.review`, the structural no-secrets invariant, `askedBy` resolution, defaults encoding today's behavior with correct JSON types, the three git-adjacent cross-references, the `lib/scripts/README.md` shape contract, `CLAUDE.md` being deliberately untouched, tarball inclusion, and suite greenness. Created 2 new repeatable unit tests in `test/bootstrap-prefs.test.js` — a pinned `script:line` citation check (the task's own verification found and fixed five stale citations, so this is the file's fastest-rotting content) and a cross-reference check on the three git-adjacent keys; both proven to fail by mutation before being accepted. Suite 203 → 205 (204 pass, 0 fail, 1 deliberate skip). The remaining 14 cases map onto assertions TASK-042 had already written, referenced rather than duplicated.

## [2026-08-06] uat | UAT-040 passed (auto) · TASK-040 done
Archived UAT-040 → uat/archive/ and TASK-040 → tasks/archive/. All 16 cases passed: the schema is a flat, comment-free 19-key object (5 global / 9 project / 5 either; 14 installer / 5 skill), every entry carries the seven required fields with `dynamic: true` on exactly `guides.*` and `gitignore.section.*`, the `.gitignore` declines-only invariant survives as the one-token `false` grammar while the three-state keys correctly document their exemption from it, all 11 `script:line` citations still resolve, `gitignore.review` is absent, no key can structurally hold a secret, every `askedBy` resolves, defaults are JSON-typed and encode today's behavior, the three git-adjacent keys cross-reference each other, `lib/scripts/README.md` carries the shape contract, `CLAUDE.md` is untouched as Phase 4 intends, and the template ships in the tarball. Two generated probes were corrected mid-run rather than recorded as product failures, both being measurement bugs: one scanned `prompt_scope` for the bare word `local` and matched the bash `local` **builtin** (`lib.sh:195`) instead of a scope branch — replaced with an assertion that the schema grammar equals the set of values the function actually emits; the other embedded backticks in a `node -e` regex and was correctly blocked by `interpreter-indirection-guard.js`, which cannot distinguish a regex literal from a command substitution. Suite 205: 204 pass, 0 fail, 1 deliberate skip (the schema→scripts bijection, un-skipped at Phase 2). ROADMAP-005 Phase 1 item 1 of 3 checked off.

## [2026-08-06] uat | UAT-041 UAT: bootstrap-prefs.js — four-state preference helper
Generated UAT-041 for TASK-041 with 10 test cases. Deliberately does **not** re-run TASK-042's 55 behavior tests; instead covers what those structurally cannot see, because every one of them spawns the helper from the repo root with an explicit `--schema`: an end-to-end round trip in production order (absence → `true` → `ask` → rejected value → `unset`), a foreign working directory, a redirected `HOME` writing the real `$HOME/.claude/` path shape, malformed-file degradation across all three operations, and the file's own packaging. Created 3 new repeatable unit tests in `test/bootstrap-prefs.test.js` under a new "packaging & invocation contract" section: shebang plus executable bit matching the *named* precedent (`merge-settings-deny.js` at 0755 — the two existing manipulators disagree, so "follow the precedent" only means something because the task names which one), zero runtime dependencies (the helper runs inside installers that may execute before `npm install`), and the default schema path resolving from any cwd. The last is the one production-only failure in the task: `bootstrap-prefs.js:111` must be `__dirname`-relative, and a cwd-relative default would pass all 205 other tests while silently degrading to a no-defaults `warnSkip` in the field — proven by mutation, which the new test caught alone. Suite 205 → 208 (207 pass, 0 fail, 1 deliberate skip).

## [2026-08-06] uat | UAT-041 passed (auto) · TASK-041 done
Archived UAT-041 → uat/archive/ and TASK-041 → tasks/archive/. All 10 cases passed. The end-to-end round trip confirmed the four-state model in production order against a scratch `--target`: absence reads `unset` and a read creates nothing, `true` stores as a JSON **boolean** (a string `"true"` would be truthy in every shell test), a stored `ask` reads back `ask` rather than `unset` (the conflation trap), an out-of-grammar value exits 1 without touching the file, `--unset` removes the key entirely with no `null` left on disk, and no `.tmp-*` residue survives any path. A redirected `HOME` produced the documented `$HOME/.claude/bootstrap-prefs.json` layout with its companion beside it, and the helper resolved its schema correctly when run from `/tmp` — the one production-only failure mode, since every unit test runs from the repo root with an explicit `--schema`. A malformed values file degraded to `unset` on `--get` (exit 0, warning naming the file) and left its garbage bytes byte-identical under both `--set` and `--unset`. Packaging verified: shebang plus 0755 matching the named precedent, and node builtins only. One probe corrected mid-run rather than recorded as a product failure: the slugifier test filter used `em dash` with a space and so missed the test named `em-dash`, selecting 2 of 4 — widened and pinned to the exact count of 4. Real `~/.claude/bootstrap-prefs.json` absent before and after. Suite 208: 207 pass, 0 fail, 1 deliberate skip. ROADMAP-005 Phase 1 item 2 of 3 checked off.

## [2026-08-06] uat | UAT-042 UAT: test/bootstrap-prefs.test.js — four-state and schema-bijection coverage
Generated UAT-042 for TASK-042 with 12 test cases. The deliverable is a test file, so the UAT is deliberately **meta-verification**: the risk in a task like this is a suite that looks like coverage without being it, so the cases target that directly — the runner actually discovers the file, the one skip is the documented one and renders its reason, the skipped bijection is a **real assertion rather than a stub** (proven by un-skipping it in place: it fails reporting all 17 non-dynamic keys, 19 minus the 2 dynamic families, then restores byte-identically), the suite can be **seen failing** (the conflation-trap mutation turns exactly 3 ask-state tests red and no others), and the call-site extractor is proven against known-good and known-bad fixture forms so a matcher that silently found nothing could not masquerade as a passing bijection. Created 1 new repeatable unit test: the **two-population separation** invariant — the file's banner declares that nothing below it spawns the helper or makes scratch dirs, and that separation is precisely what makes the schema assertions trustworthy (the helper is generic, so a wrong schema entry breaks nothing a behavior test can observe), yet it was described in a comment and enforced by nothing; now enforced by reading the file's own source, with failure proven by planting an offending line. Suite 208 → 209 (208 pass, 0 fail, 1 deliberate skip).

## [2026-08-06] uat | UAT-042 passed (auto) · TASK-042 done
Archived UAT-042 → uat/archive/ and TASK-042 → tasks/archive/. All 12 cases passed, **closing ROADMAP-005 Phase 1** — all three items now checked; Phases 2–4 remain, so the roadmap stays `active`. The two proofs that matter both held: un-skipping the `schema -> scripts` bijection in place made it **fail** reporting exactly 17 unreferenced keys (19 schema keys minus the 2 dynamic wildcard families, which are excluded by design because their literals are computed at run time), confirming the skip is a complete assertion and not a stub; and mutating `--get` to conflate a stored `ask` with `unset` turned exactly **3** tests red — the named conflation-trap test with its own message, the `true → false → ask` overwrite, and the project-`ask`-beats-global-`true` resolution case — every test whose subject is the `ask` state and nothing else. Both source files were restored and verified **byte-identical by content**, not by `git diff`, since both are untracked. One probe corrected mid-run: the scratch-dir check counted `os.tmpdir()` entries matching `prefs-`/`bootstrap-prefs-` in absolute terms and found 3, but none were the suite's — the harness uses only `prefs-` and `bootstrap-prefs-slug-`, while the three found were `bootstrap-prefs-layers.*` / `bootstrap-prefs-smoke.*` left by the *shell* smoke scripts of TASK-041's implementation session. Reformulated as a before/after delta (the suite must not *add* a scratch dir), which is the actual claim and is robust to a dirty tmpdir; the suite adds 0. Real `~/.claude/bootstrap-prefs.json` absent throughout. Suite 209: 208 pass, 0 fail, 1 deliberate skip.

## [2026-08-06] task | TASK-043 Sticky prompt helpers in lib.sh
Created TASK-043, opening ROADMAP-005 Phase 2 and gating the other three wiring tasks: `prompt_yn_sticky`, `prompt_choice_sticky`, and the `BOOTSTRAP_ASSUME_TTY` tty seam in `lib/scripts/lib.sh`. Design calls recorded in the task: **one selector argument used for both the read and the write** (correct for every `global`/`project`-scope key because `bootstrap-prefs.js` resolution is already scope-constrained; the single `either` key in Phase 2 is read directly instead); **`prompt_choice_sticky` prints its answer to stdout like `prompt_scope`**, so every diagnostic including the remembered-answer notice must go to stderr or the notice becomes the captured answer; **stored `ask` and `unset` are separate branches** (`ask` prompts and records nothing, `unset` prompts and records) since collapsing them silently overwrites an explicit "keep asking"; and the `--set` is made **unreachable** from the non-interactive branch rather than skipped by a flag, so one CI run can never bake in a permanent decline. The task also repairs the `lib.sh:198` citation its own edits invalidate, in both `bootstrap-prefs-schema.json` and `CITATION_PINS`.

## [2026-08-06] task | TASK-044 Wire the install-mcps.sh prompt sites to the preference store
Created TASK-044 (depends on TASK-043): route all eight MCP preference keys through the sticky helpers. The roadmap's "six prompt sites" is corrected to seven plus the shared `prompt_scope` question — the schema is the authority (seven keys with `askedBy: install-mcps.sh`, plus `mcp.context7Scope` asked in `lib.sh`). Carries TASK-041's hand-off finding as its own step: `mcp.playwrightConflict` stores `shared|alongside|skip` while `install-mcps.sh:415` branches on the digits `1`/`2`/`*`, so a stored name fed into the digit comparison falls through every branch and **silently behaves like `skip`** — the `case` is rewritten to branch on names, with digit→name resolution done at input time so no digit ever reaches a branch or the store. Two other calls recorded: the non-interactive path (`bootstrap install`, which today installs every missing MCP unprompted) now honours a stored decline while still asking and writing nothing; and `register_optional_mcp` grows two optional positional params rather than being forked, with empty meaning "behave as today".

## [2026-08-06] task | TASK-045 Wire the sync-wiki-scaffold.sh, install-global.sh, and update-project.sh prompt sites
Created TASK-045 (depends on TASK-043, parallel-safe with TASK-044/046): the optional-guide offer (`guides.*`), the stale-skill-folder deletion (`skills.pruneOrphans`), and the legacy `.docs/` warning (`update.legacyDocsAck`). Four calls recorded. The `guides.*` key is **built from the loop variable**, never a literal list — enumerating the guides a second time is the drift the wildcard entry exists to prevent, and the bijection scan skips computed keys by design. The store is consulted **before** the `INTERACTIVE` guard, not behind it, or a stored `true` would be ignored on every non-interactive run in contradiction of the schema. `update.legacyDocsAck` records **`true` only**: a recorded `false` would abort every future `update` with no prompt left to change it with, so a decline stays a per-run decision while `false` remains readable for anyone who sets it deliberately via `/bootstrap-config`. And `install-global.sh` must start sourcing `lib.sh` (it never has — `dirname "$0"` plus a hand-rolled `read`), which is also what shifts its cited line. `sync-wiki-scaffold.sh:171` is explicitly left alone: it is the one prompt already sticky in both directions and is the model the roadmap generalises.

## [2026-08-06] task | TASK-046 merge-gitignore.sh — prefs.gitTracking prompt and declines-only wiring
Created TASK-046 (depends on TASK-043, parallel-safe with TASK-044/045): the new three-way `prefs.gitTracking` prompt plus declines-only stickiness for the master review gate, the per-section offer, and the `.git/info/exclude` consent. Five calls recorded. Section keys are computed by shelling out to `bootstrap-prefs.js --section-key <title>`, never slugified in `awk`/`sed` — one template banner title carries an **em dash (U+2014)** and a byte-wise slugifier emits three dashes, producing a key that never matches the schema's, so the remembered decline silently stops matching and the prompt re-asks forever. The declines-only rule stays **in the schema data** (`gitignore.section.*` has a one-value grammar of `false`, so a `--set true` exits 1) and is deliberately not duplicated at call sites. `gitignore.offerSectionUpdates` is the one Phase 2 key with `scope: either`, so it is read directly with a project-dir selector rather than through the single-selector prompt helper — and its schema `default` of `true` (not `ask`) means an unanswered key drops the opening gate while every section is still offered by title, which is what keeps the `:11` invariant true. `prefs.gitTracking` is recorded in all three directions because it is a choice, not an add/skip gate, and is asked **last** so the files it governs already exist. The `.git/info/exclude` layout is called out as load-bearing: the two prefs paths must sit under their own `#` comment and outside the bootstrap sentinel block, or `exclude_is_canonical` breaks and `file-suggestion.sh` stops restoring `@`-autocomplete.

## [2026-08-06] task | TASK-047 test/prompt-stickiness.test.js — sticky-prompt coverage and the bijection un-skip
Created TASK-047 (depends on TASK-044/045/046), closing ROADMAP-005 Phase 2. The two claims in the roadmap item are separated because they fail differently: "a remembered answer suppresses the prompt" fails loudly on the first human run, while "a non-interactive run records nothing" fails **silently and permanently** — one CI run bakes in a decline that leaves no prompt to change it with and nothing in a diff, since the store lives outside the repo — so that case gets the strongest assertion in the file (the values file and its companion must not exist at all). Hermeticity copies `withLayers()`'s read-only redirect probe, which must land before any write because a failed redirect damages the developer's real store before an assertion could notice. **The roadmap's un-skip note is corrected**: UAT-042 measured the skipped bijection failing on 17 keys, but Phase 2 wires only 13 of them — the four `consumer: skill` keys (`gitCommit.versionBump`, `gitCommit.autoPush`, `research.persistToRaw`, `uatGenerate.promoteTests`) get their call sites in Phase 3, so a bare un-skip goes red through no fault of Phase 2. The task therefore measures the residue first, then splits the assertion: hard and live for every `consumer: installer` key, with the four skill keys in a guarded `PHASE_3_PENDING` allowlist that can only hold real `consumer: "skill"` keys and only ones genuinely still unreferenced, so it empties itself when Phase 3 lands instead of becoming a permanent exemption.

## [2026-08-06] uat | UAT-039 UAT: Add extensive inline comments to the hook scripts
Generated UAT-039 for TASK-039 with 13 test cases: 4 behavior-preservation, 6 documentation/standard, 1 syntax, 1 bug-bookkeeping, 1 install-sync. Created 9 repeatable unit tests in one new file: `test/hook-comments.test.js` (suite 209 → 218, 217 pass / 1 skipped / 0 fail). The promotion split was the judgement call: the task's decisive gate — strip comments from `git show HEAD:<path>` and compare byte-for-byte — is deliberately **not** promoted, because once TASK-039 is committed HEAD *is* the commented version and the comparison passes vacuously forever; it stays a one-time acceptance case (UAT-BEHAVIOR-001, re-runnable with `BASE=b85cbe9`, currently 1523 changed `.js` lines / 0 non-comment). In its place the permanent tests assert the two things that must stay true: the comments do not swallow code (`prologue carries no swallowed executable code` — verified against a mutant of `serena-first-read-guard.js` where deleting one `*/` leaves `node --check` **passing**, which is exactly the near-miss that bit Step 2 and the reason a naive `/*`-vs-`*/` count was rejected: regex-dense files like `serena-first-glob-guard.js` read 15 opens / 10 closes while perfectly balanced), and the comments survive (README `## Commenting standard` section, the `CLAUDE.md` exception clause, per-file header blocks, a 25% comment-share floor set below the current 28% minimum so only a stripping pass trips it). Two gaps recorded rather than papered over: `git-protected-ops-block.js` and `serena-edit-guard.js` are blocking hooks with no `Fails:` field (both outside the task's 17-file scope — held as a shrink-only ratchet, not an exemption), and the seven Serena-first hooks remain unit-untestable because `getStateFilePath()` resolves enforcement state from ambient `HOME`/cwd with no injection point.

## [2026-08-06] uat | UAT-043 UAT: Sticky prompt helpers in lib.sh
Generated UAT-043 for TASK-043 with 9 test cases (5 integration, 4 edge). TASK-043 adds no call sites — it adds the choke point — so the UAT verifies the helper contract rather than any installer flow: the bash 3.2 portability gate, the `BOOTSTRAP_ASSUME_TTY` seam being a tty *override* and not a prompt bypass (proven by stdin at EOF still reaching the real EOF path), the load-bearing "a non-interactive run records NOTHING" rule with poisoned stdin, `prompt_choice_sticky`'s stdout purity plus names-not-digits storage, the `ask`/`unset` non-collapse via a byte-level store fingerprint, `prompt_scope`'s inert bare form and first-letter resolver, the two degradation paths, and the repaired `lib.sh:387` citation. Created 6 repeatable unit tests in `test/prompt-stickiness.test.js`, filling the one real gap in TASK-047's coverage: `prompt_scope` and the two `prefs_*` wrappers had no direct tests at all, despite `prompt_scope`'s bare form being a live contract with `bootstrap-serena.sh` and `install-mcps.sh:94`. The zero-prefs-call proof uses an argv-logging spy shim (an empty log is positive evidence, strictly stronger than "no file was created"), and falsifiability was confirmed by mutation — replacing `[pP]*)` with `project)` turned 3 of them red before `lib.sh` was restored and verified byte-identical by SHA-256. Suite 245 → 251 (251 pass, 0 fail, 0 skipped).

## [2026-08-06] uat | UAT-043 passed (auto) · TASK-043 done
Archived UAT-043 → uat/archive/ and TASK-043 → tasks/archive/. All 9 cases passed. The three claims that carry the design held: the `BOOTSTRAP_ASSUME_TTY` seam gates tty *detection* only — with the seam on and stdin at EOF, `read` still runs and the empty reply resolves to the declared default, which is what keeps every stickiness test from passing by not testing anything; an unattended run with stdin poisoned `y` answered no, printed its note exactly once, and left **both** layers with no values file and no companion README, so one CI run cannot bake in a permanent decline; and a stored `ask` was asked again, returned the live reply to the caller, and survived byte-identical, so an explicit "keep asking" is not consumed by the next answer. Also confirmed: a digit reply stores the resolved **name** (`2` → `alongside`), `prompt_choice_sticky`'s capture is exactly 9 bytes with every notice on stderr, `prompt_scope`'s bare form made **zero** prefs calls under an argv-logging spy shim while keeping its first-letter resolver, both degradation paths (missing helper, illegal value) warn without aborting under `set -euo pipefail`, and the repaired `lib.sh:387` citation resolves to the real `Scope for $name` line. Three generated probes were rewritten mid-run rather than recorded as product failures, all three being measurement bugs against this repo's own guards: the bash-3.2 construct scan matched the **constraint banner** naming `${var,,}` until whole-line comments were stripped; `ls` for the store check is blocked by the Serena-first hook and was replaced with a `node` check that also inspects the companion README; and the inline `bash -c '… "$(prompt_choice_sticky …)" …'` probes are correctly refused by `interpreter-indirection-guard.js` (an inline `-c` program containing a command substitution cannot be extracted and re-checked), so those three cases now invoke the test runner — the same behaviour exercised the sanctioned way, since the harness writes a wrapper file and spawns `bash` on it. Real `~/.claude/bootstrap-prefs.json` absent before and after; `lib.sh` verified byte-identical by SHA-256 after the deliberate falsification mutation. Suite 251: 251 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 2 item 1 of 5 checked off; roadmap stays `active`.

## [2026-08-06] uat | UAT-044 UAT: Wire the install-mcps.sh prompt sites to the preference store
Generated UAT-044 for TASK-044 with 10 test cases (6 integration, 4 edge). Created 4 repeatable unit tests in a new `install-mcps.sh e2e` section of `test/prompt-stickiness.test.js`, filling a genuine coverage hole: TASK-047 built e2e coverage for `merge-gitignore.sh` but **none** for `install-mcps.sh`, so the roadmap's single headline bug — the Playwright conflict menu re-asking on every run — and the one real defect found during implementation both had no repeatable guard, having been verified once by hand in step 9. The e2e harness stubs `claude` (logs argv, answers `mcp get` from an env var) and `uname` (reports Linux, so `_add_playwright` takes its one-line stdio branch instead of running `npm install -g` and `launchctl`) onto `PATH`, and builds a scratch git project whose **tracked** `.mcp.json` reaches the team-owned conflict branch; Serena/brave/context7 are pre-declined in the store so stdin means exactly one thing and an off-by-one cannot pass for the wrong reason. Falsifiability proven by reverting the `case` to its pre-fix digit form (`1)`/`2)`): the headline test alone went red with `the shared branch did not register playwright-shared`, confirming a stored NAME would fall through to `*` and silently behave like `skip`; `install-mcps.sh` was restored and verified byte-identical by SHA-256. Two generated probes were corrected before use rather than recorded as failures, both measurement bugs: the selector audit asserted on call-site lines for the three keys that reach their helper as **positional arguments** to `register_optional_mcp` (their `--global` lives in the wrapper), and the menu-order parse kept the trailing `)"` of the command substitution. Suite 251 → 255 (255 pass, 0 fail, 0 skipped).

## [2026-08-06] uat | UAT-044 passed (auto) · TASK-044 done
Archived UAT-044 → uat/archive/ and TASK-044 → tasks/archive/. All 10 cases passed. All eight keys are wired at the layer their schema `scope` mandates — four directly at `"$PROJECT_DIR"`, one directly at `--global`, and three (`mcp.braveSearch`, `mcp.context7`, `mcp.context7Scope`) reaching `--global` inside `register_optional_mcp`, which is where their selector is applied since they arrive as positional `$6`/`$7`. The roadmap's headline bug is closed end to end: answering the conflict menu with the digit `1` stores the **name** `shared`, registers `playwright-shared`, writes `disabledMcpjsonServers: ["playwright"]` into the project's `.claude/settings.local.json`, and leaves the team's committed `.mcp.json` untouched — and a second run driven by **empty stdin** replays the stored answer instead of re-asking, which is the exact re-run the task called "not fixed until this is clean". The name-vs-digit defect is pinned structurally (`case` branches are `shared)`/`alongside)`/`skip|*)`, no digit branch, and the positional name list matches the printed menu order) and behaviourally. A stored `skip` produced `left untouched` with zero `mcp add` calls; a non-interactive run left both layers with no values file and no companion README; and a stored `mcp.braveSearch: false` suppressed the non-interactive auto-install with the store unchanged by bytes and mtime — the silent-reinstall regression. All four moved citations resolve (`:328`, `:341`, `:501`, `:522`), the tty seam is adopted with no bare `-t 0` left in code, and no schema key can structurally hold a secret while both API-key reads stay bare. Real `~/.claude/bootstrap-prefs.json` absent throughout; no real MCP was registered (stubbed `claude` and a `uname` reporting Linux). Suite 255: 255 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 2 item 2 of 5 checked off.

## [2026-08-06] uat | UAT-045 UAT: Wire the sync-wiki-scaffold.sh, install-global.sh, and update-project.sh prompt sites
Generated UAT-045 for TASK-045 with 10 test cases (5 edge, 5 integration). Created 5 repeatable unit tests in `test/prompt-stickiness.test.js` — none of the three prompt sites had **any** stickiness coverage before this: `test/install-global.test.js` drives that script but never touches its orphan-skills prompt, and neither `sync-wiki-scaffold.sh`'s guide ladder nor `update-project.sh`'s acknowledgement ladder was exercised at all. The UAT targets the three decisions that are invisible in normal use: the `guides.*` key literal being **computed** from the loop variable (asserted by answering two guides differently in one run, so a hard-coded key would collapse both under one name), the store being read **ahead of** the `INTERACTIVE` guard (asserted by a fully headless run honouring a stored `true`), and `update.legacyDocsAck` recording `true` **only** while still honouring a stored `false` on read. Both load-bearing claims proven falsifiable by mutation: wrapping the guides lookup in `[ "$INTERACTIVE" = true ]` failed the headless test alone while every interactive test stayed green, and adding `prefs_set … false` to the decline path failed the write-asymmetry test with `an answer was recorded that should not have been`. Both scripts restored and verified byte-identical by SHA-256 — necessary rather than ceremonial, since the work is untracked and `git diff` proves nothing. Suite 255 → 260 (260 pass, 0 fail, 0 skipped).

## [2026-08-06] uat | UAT-045 passed (auto) · TASK-045 done
Archived UAT-045 → uat/archive/ and TASK-045 → tasks/archive/. All 10 cases passed. The three decisions that are invisible in normal use all held: `guides.*` recorded under its exact computed keys `{"guides.evals-framework.md": false, "guides.type-checking-templates": true}` — extension included, both JSON booleans — with the two guides answered differently in one run, so a hard-coded literal would have collapsed them; a fully headless run (no `--interactive`, no tty seam) honoured a stored `true` and delivered the guide, proving the store is read ahead of the `INTERACTIVE` guard rather than behind it, while the unanswered guide stayed both absent and unrecorded; and `update.legacyDocsAck` left **no** values file and no companion README after a decline, while a stored `false` was still honoured on read and aborted with the store unchanged by bytes and mtime. Also confirmed: a `skills.pruneOrphans` decline recorded `false` at `--global` (the only correct layer, since `install-global.sh` has no project dir) and survived a second run fed poisoned `y` with both stale folders intact; the accepted guide came back through the **presence** branch rather than the stored-answer branch; all three citations resolve (`sync-wiki-scaffold.sh:81` genuinely did not move, `install-global.sh:70`, `update-project.sh:76`); no bare `-t 0` remains in any of the three; and `test/install-global.test.js` still passes 7/7 after the script gained its `lib.sh` source. Real `~/.claude/` untouched throughout — which mattered most here, since `install-global.sh` rsyncs into `~/.claude/skills/` and `~/.claude/hooks/`. Suite 260: 260 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 2 item 3 of 5 checked off.

## [2026-08-06] uat | UAT-046 UAT: merge-gitignore.sh — prefs.gitTracking three-way prompt and declines-only wiring
Generated UAT-046 for TASK-046 with 11 test cases (6 edge, 5 integration). This task arrived with the strongest pre-existing coverage in Phase 2 — TASK-047 had already built four `merge-gitignore.sh` e2e tests covering the declines-only invariant, the em-dash slug, the scripted-stdin harness, and all three `prefs.gitTracking` options — so only **3** tests were added, for what was genuinely missing: the master gate's `false` branch (which pins the cross-key independence claim — declining the section pass must leave `.git/info/exclude` fully working), its `ask` branch (discriminated from a remembered skip by the absence of the `(remembered answer …)` suffix), and a schema-layer guard that no `consumer: "installer"` key may carry a non-null default. That last one generalizes defect (b): `--get` resolves *through* the default, so a non-null default on a key that exists to be asked is indistinguishable from an answer the user never gave — the rule is deliberately scoped to installer keys, since the five `consumer: "skill"` keys need a defined starting behaviour and four carry `ask` in their grammar. Defect (b) was also confirmed to be genuinely guarded behaviourally rather than incidentally passing: restoring `"default": "exclude"` made the existing gitTracking test fail with `[gitignore] the digit 1 resolved to the wrong name`. The cross-key claim was proven falsifiable by adding `exit 0` to the master gate's `false` branch — the plausible "simplification" — which failed with `.serena/ did not reach .git/info/exclude`. Both the schema and `merge-gitignore.sh` restored and verified byte-identical by SHA-256. Suite 260 → 263 (263 pass, 0 fail, 0 skipped).

## [2026-08-06] uat | UAT-046 passed (auto) · TASK-046 done
Archived UAT-046 → uat/archive/ and TASK-046 → tasks/archive/. All 11 cases passed. The invariant at `merge-gitignore.sh:11` holds end to end: accepting every section recorded **no** `gitignore.section.*` key at all, and `--set true` on a section key exits 1 with the message `"true" is not a legal value … expected one of: false` while writing no file — the declines-only rule enforced by the schema data rather than by any call site, and no call site attempts to work around it. Declining every section stored exactly the keys `--section-key` computes, including the em-dash title under a key carrying **one** dash where the em dash was; the second run offered nothing and left `.gitignore`, `.git/info/exclude` and the store byte-identical. All three `prefs.gitTracking` options routed correctly in three separate scratch repos, each digit resolving to its **name**. The two newly-covered branches held: a stored `false` master gate skipped the section pass and created no `.gitignore` while `.serena/`, `raw/` and `wiki/` still reached `.git/info/exclude` — the cross-key independence claim — and a stored `ask` restored the opening question, discriminated from a remembered skip by the absence of the `(remembered answer …)` suffix, recording `false` on decline. Both citations resolve (`:171`, `:376`) with `:11` correctly unmoved, and no bare `-t 0` remains. Real `~/.claude/` and this repo's own `.gitignore` / `.git/info/exclude` untouched throughout — hash-guarded around every e2e case. Suite 263: 263 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 2 item 4 of 5 checked off.

## [2026-08-06] uat | UAT-047 UAT: test/prompt-stickiness.test.js — sticky-prompt coverage and the bijection un-skip
Generated UAT-047 for TASK-047 with 8 test cases (5 integration, 3 edge). The deliverable is a test file, so the UAT is deliberately **meta-verification** — the risk in a task like this is a suite that looks like coverage without being it, which is not hypothetical here: un-skipping the bijection cold reported all **seventeen** non-dynamic keys unreferenced rather than the predicted four, because the extractor recognised only the direct `bootstrap-prefs.js --get/--set` form while every Phase 2 call site goes through a `lib.sh` wrapper — meaning the paired `scripts -> schema` direction had been passing on a genuinely empty found-set the whole time. Created 1 repeatable unit test, `suite: no test anywhere in test/ is skipped or marked todo` in `test/bootstrap-prefs.test.js`: TASK-047's Step 9 asserts zero skips as a one-time manual check, but nothing made it permanent, and it is exactly the property most likely to erode — a test skipped "temporarily" reads as a passing one in every summary, and the skip count lives in a trailer no gate reads. Falsifiability proven by planting `test.skip(` in `test/npm-pack-contents.test.js` (caught, naming the file and form) and by parking the installer key `mcp.braveSearch` in `PHASE_3_PENDING` (caught by guard 2: *an installer key unreferenced by Phase 2 is a bug in Phase 2, not something to allowlist*). One implementation wrinkle recorded: the scan covers its own directory, so its offender labels deliberately avoid spelling the option forms literally — a label reading `{ skip: … }` matched its own pattern and failed the test on its own description. All mutated files restored and verified byte-identical by SHA-256. Suite 263 → 264 (264 pass, 0 fail, 0 skipped).

## [2026-08-06] uat | UAT-047 passed (auto) · TASK-047 done — ROADMAP-005 Phase 2 complete
Archived UAT-047 → uat/archive/ and TASK-047 → tasks/archive/. All 8 cases passed, **closing ROADMAP-005 Phase 2** — all five items checked; Phases 3–4 remain, so the roadmap stays `active`. The meta-verification held on every axis. The bijection runs unskipped with `PHASE_3_PENDING` holding exactly the four `consumer: "skill"` keys, and guard 2 was proven to genuinely fire by parking the installer key `mcp.braveSearch` there (`an installer key unreferenced by Phase 2 is a bug in Phase 2, not something to allowlist`). The paired `scripts -> schema` scan pins `found.length > 0`, so it can no longer pass on the empty found-set it silently had before the wrapper-aware extractor landed. **The suite can be seen failing, twice, with the predicted signatures**: breaking `prefs_get` to always answer `unset` turned 22 of 43 prompt-stickiness tests red — every stored-answer claim across all four scripts plus the two `no tty still READS the store` tests, which are by design sensitive to `prefs_get` and are the documented reason "non-interactive stays green" is not a clean binary — while making the non-interactive branch record its auto-answer turned **exactly two** red, both `prompt_yn_sticky`, with `prompt_choice_sticky`'s own no-write tests staying green and thereby proving the two functions' coverage does not cross-contaminate. `lib.sh` was restored and verified byte-identical by SHA-256 after each mutation, line count unchanged at 657 so no citation pin moved. Hermeticity across a full 264-test run: `.gitignore` and `.git/info/exclude` byte-identical, `~/.claude/settings.json` identical in size and mtime, `~/.claude/bootstrap-prefs.json` absent before and after, and zero suite-owned scratch dirs leaked. One probe was corrected mid-run rather than recorded as a failure: the scratch-dir check used a raw `os.tmpdir()` count, which moved 410 → 412 on unrelated OS churn (`uv-*.lock`, `xcrun_db`, `TemporaryItems`); reformulated as a prefix-filtered delta on the suite's own `mkdtemp` names it is 3 → 3, and the 3 are the pre-existing `bootstrap-prefs-layers.*`/`bootstrap-prefs-smoke.*` dirs from TASK-041's shell smoke scripts that UAT-042 already identified. Suite 264: 264 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 2 item 5 of 5 checked off.

## [2026-08-07] task | TASK-048 /bootstrap-config skill — view, edit, and reset stored preferences
Created TASK-048, the second of ROADMAP-005 Phase 3: `lib/skills/bootstrap-config/SKILL.md`, the user-facing front end to the preference store Phase 1 shipped. Depends on nothing outstanding (the store exists) and is `parallel_safe_with::[[TASK-030]]`, which touches `install-global.sh`, `merge-gitignore.sh`, and five other skills while this task creates exactly one new file. Design calls recorded in the task. **`serena-config` is the structural template** — the CRITICAL ORDERING RULE (no `AskUserQuestion` before current state has been read *and printed*), a mandatory "No changes" escape, a confirm-before-write question, and `Edit` never `Write`. **The helper is the only writer**: all reads come from `bootstrap-prefs.js --list` and all writes from `--set`/`--unset`, so value validation, `true`/`false` JSON coercion, atomic writes, and the regenerated `bootstrap-prefs.README.md` companion stay in the one place that implements them; the companion is generated output and is never hand-edited. **The schema supplies every explanation** — `--list` prints only `summary`, so the skill reads the schema file directly for `detail`, `values`, and `scope`, and never offers a value the `values` string omits: `gitCommit.versionBump` expresses "ask me" as `confirm` (there is deliberately no separate `ask`), and `gitignore.section.*` is `false`-only. **The two consumer populations are presented separately**, with a heavier warning on `consumer: skill` keys because changing one changes what a slash command does at run time, not merely whether the installer prompts. All four states must render distinguishably — `unset` (absence), a settled value, `false` (a remembered decline, not the same as unset), and `ask` — plus a `[default]` marker for keys nothing is stored for. Layer offers are `scope`-constrained so an inert global-scope answer can never be parked in a project file, and `--list` always passes `--project` since omitting it silently hides every project answer. Registration in `lib/skills/README.md` and `CLAUDE.md` is explicitly excluded — that is ROADMAP-005 Phase 4's job, and a duplicate edit would collide with it.

## [2026-08-07] uat | UAT-030 UAT: User preferences: stop skills doing consequential things without consent
Generated UAT-030 for TASK-030 with 14 test cases (9 edge, 5 integration) covering the sync-time preferences pass in `install-global.sh` (steps 6-7), its two new `lib.sh` helpers, and the four-state wiring of the five consumers. Created **12 repeatable unit tests** across `test/prompt-stickiness.test.js` (10) and `test/bootstrap-prefs.test.js` (2) — the sync pass had **zero** prior coverage: `prefs_stored_global`, `prompt_letter_choice`, `settle_skill_pref` and the whole step 6/7 block returned no hits in any existing test file. The three trap behaviours are each pinned twice. (1) **`prefs_get` cannot detect `unset`** for a key with a non-null schema default — pinned as a same-key contrast where `prefs_get` answers `true` both before and after a write while `prefs_stored_global` flips, so the two helpers are shown disagreeing at the same moment rather than merely described as different; a companion test pins the `false` direction, where value-based reasoning is genuinely ambiguous because `gitCommit.autoPush`'s schema default is `false` too. (2) **A re-run re-asks only unanswered keys** — the re-run test poisons stdin with `y` on every line and asserts the four settled keys are not merely unchanged but never *printed*, since a re-asked question answered identically is still a re-asked question. (3) **`gitCommit.autoPush` `unset` means DO NOT PUSH** — asserted against the schema default, the Step 6 gate table's `unset` row, and the absence of `--force`/`-u`/`--all`/`--tags` on any constructed push. One harness wrinkle recorded: the `[a]uto / [c]onfirm / [n]ever` prompt string is **unobservable at runtime** in this harness because `read -r -p` displays its prompt only when stdin is a terminal, and every test drives the script through a pipe — so the grammar is asserted against the script source while the behaviour (`c` resolves to `confirm`; the helper exits 1 on `--value ask`) is asserted at runtime where it is observable. Suite 264 → 276 (276 pass, 0 fail, 0 skipped).

## [2026-08-07] uat | UAT-030 passed (auto) · TASK-030 done
Archived UAT-030 → uat/archive/ and TASK-030 → tasks/archive/. All 14 cases passed. The three trap behaviours hold live. **`prefs_stored_global` genuinely separates a stored answer from a schema default**: on the same key at the same moment, `prefs_get uatGenerate.promoteTests` answers `true` before *and* after a write while `prefs_stored_global` flips `no` → `yes` — so the sync pass measures the layer, not the value, and the read-only `--list` probe materialises no values file. A stored `false` counts as **answered** (the direction that stops a decline re-asking forever, and the one where value-based reasoning is genuinely ambiguous because `gitCommit.autoPush`'s own schema default is `false`), and a project-layer answer correctly does **not** satisfy a question the pass would write at `--global`. **Ask-once holds end to end across three consecutive runs**: run 1 settled four keys and `skip`ped the fifth (recording nothing); run 2, with stdin poisoned so every line reads `y`, changed exactly the one unanswered key and never even *printed* the four settled ones — including the stored `false` and the stored `ask`; run 3 printed `All skill preferences already answered — nothing to ask.` and left the store byte-identical, with the recovery pointers correctly suppressed on a run that asked nothing. **`gitCommit.autoPush` `unset` means do not push** — the schema default is `false`, the Step 6 gate table's `unset` row says so explicitly, and no `--force`/`--force-with-lease`/`-u`/`--all`/`--tags` appears on any constructed push. Step 6's helper install puts the schema at `<helper dir>/templates/` and the **installed** copy resolves a default with no `--schema` flag, which is what makes `consumer: skill` keys readable from an arbitrary project at all; it is correctly *not* tty-gated, so a headless run still installs it while writing no preferences file whatsoever. `PHASE_3_PENDING` is `[]` and both bijection directions run unskipped. Hermeticity verified by SHA-256 either side of the whole run: the real `~/.claude/settings.json`, this repo's `.gitignore`, and its `.git/info/exclude` are byte-identical, and `~/.claude/bootstrap-prefs.json` remains absent. Suite 276: 276 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 3 item 1 of 2 checked off.

## [2026-08-07] uat | UAT-048 UAT: /bootstrap-config skill — view, edit, and reset stored preferences
Generated UAT-048 for TASK-048 with 15 test cases (11 edge, 4 integration) and created **14 repeatable unit tests** in a new `test/bootstrap-config-skill.test.js`. The deliverable is a single skill file, and a skill file **is** the program — a model executes the markdown, so a wrong flag, an invented value, or a stale factual claim is a real defect with no compiled artefact to catch it and no type checker for prose. The UAT is therefore built around **the joins rather than the file**: every `--flag` extracted from the skill's `node <helper> …` lines must appear in the helper's usage block; every value the skill offers must trace back to a schema `values` string; the `consumer: skill` population named inline in Step D must match the schema in **both** directions (a key that gained `consumer: skill` and was not added would silently get the lighter "only affects prompting" framing, and a key listed that is not would over-warn and train the user to ignore the banner); and the five `[layer]` annotations the skill promises to pass through verbatim must be ones the helper actually emits. Each of those three inputs drifts independently of the skill and nothing else in the suite noticed. **TASK-048's `--set` scope observation was confirmed real and is worse than reported** — see the closure entry. Two structural claims are also pinned: the `CRITICAL ORDERING RULE` must appear *before* the first `AskUserQuestion` mention, so a reader following the file top-down meets the rule before the thing it forbids; and the Phase 3/4 boundary is asserted as an explicit test that neither `lib/skills/README.md` nor `CLAUDE.md` mentions `bootstrap-config`, which makes the boundary visible when Phase 4 lands (the test fails, and the correct fix is to delete it). The `[DEFERRED-TO-UAT]` step-12 item was run against a **scratch HOME only**. Suite 276 → 290 (290 pass, 0 fail, 0 skipped).

## [2026-08-07] uat | UAT-048 passed (auto) · TASK-048 done — ROADMAP-005 Phase 3 complete
Archived UAT-048 → uat/archive/ and TASK-048 → tasks/archive/. All 15 cases passed, **closing ROADMAP-005 Phase 3** — both items checked; Phase 4's four items remain, so the roadmap stays `active`. Every join holds: all ten helper flags the skill spells (`--list`, `--get`, `--set`, `--unset`, `--value`, `--global`, `--project`, `--target`, `--schema`, `--section-key`) resolve against the helper's usage block; both quoted grammars match the schema exactly and the helper behaviourally rejects the two tempting widenings (`--set gitCommit.versionBump --value ask` and `--set gitignore.section.example --value true` each exit 1); the Step D `consumer: skill` population matches the schema in both directions; and all five `[layer]` annotations the skill promises to pass through verbatim are ones the helper emits. **TASK-048's `--set` scope observation is CONFIRMED, and is worse than the task reported.** `--set mcp.braveSearch --value true --project <dir>` (a `scope: global` key written into a project layer) exits **0** and prints the affirmative success line `project: mcp.braveSearch = true`; the key lands on disk; and `--get` then returns **`unset`**, because resolution *is* scope-constrained even though the write is not. The failure is silent at both surfaces a user would check — `--list --project` renders the row as `mcp.braveSearch = unset  [unset]`, showing no trace of the stored value at all. The **only** surface that reveals it is the generated `bootstrap-prefs.README.md` companion, whose "Unrecognized keys" section explains `scope is global — this layer never consults it`. So the skill's Step E.2 claim that its own layer offer is the sole guard is accurate as written; the defect is in the helper and is **left unfixed here by instruction**, recorded for `/bug-file` routing. The test pinning it is deliberately bidirectional: if `--set` is ever taught to enforce scope, the test fails and points at the skill prose that has become wrong. Hermeticity verified by SHA-256 either side of the run — the real `~/.claude/settings.json`, `.gitignore`, and `.git/info/exclude` are byte-identical and `~/.claude/bootstrap-prefs.json` remains absent. **`/bootstrap-config` is NOT live in the user's real `~/.claude/skills/`**: TASK-048 step 12's deferred install was verified against a scratch `HOME` only, and the user must run `./lib/scripts/install-global.sh --skip-mcps` themselves before the command appears. Suite 290: 290 pass, 0 fail, 0 skipped. ROADMAP-005 Phase 3 item 2 of 2 checked off.

## [2026-08-07] bug | BUG-0009 `--set` reports success writing a key into a layer that never reads it
Filed the defect UAT-048 confirmed and deliberately left unfixed, so it has an ID the eventual fix can cite. `bootstrap-prefs.js` validates a key's **value grammar** on `--set` but never its **`scope`**: `scopePermitsLayer` (`:396-401`) is referenced at exactly one site, `renderCompanion` (`:417`), and the `--set` handler (`:673-713`) writes unconditionally at `:705`. Reproduced end to end against a redirected `HOME` — `--set mcp.braveSearch --value true --project <dir>` (a `scope: global` key into a project layer) exits **0**, prints `project: mcp.braveSearch = true`, and lands `{"mcp.braveSearch": true}` on disk, while `--get` returns `unset` and `--list --project` renders `mcp.braveSearch = unset  [unset]`. **Silent at both surfaces a user would check**; the only surface that reveals it is the generated `bootstrap-prefs.README.md`, under `## Unrecognized keys` with the reason `scope is global — this layer never consults it, so it has no effect here`. Severity `medium`: no data loss or security impact, but the write announces success and the two interrogation surfaces disagree with it. Today the **only** guard is prose — `/bootstrap-config`'s Step E (`lib/skills/bootstrap-config/SKILL.md:115-121`), an instruction to a model, which nothing enforces against a direct CLI call. The bug carries the fix's full blast radius: two tests deliberately depend on the current permissiveness because writing a scope-inert key is how they build their fixtures (`test/bootstrap-prefs.test.js:649-671`, whose comment at `:651` states the contract verbatim, and the two-population companion test at `:1700-1800`), so a fix **must** break them — that failure is the intended pointer to the now-stale skill prose at `SKILL.md:24` and `:120`, which asserts the helper does not enforce scope and frames Step E as the guard. `--target` must stay exempt (`:397` already handles it) and `--unset` should stay permissive, since it is the workaround.

## [2026-08-07] task | TASK-049 Register /bootstrap-config in lib/skills/README.md and the CLAUDE.md Custom Commands table
Created TASK-049, the first of ROADMAP-005 Phase 4's four items — closing the registration gap TASK-048 deliberately left open so a concurrent Phase 4 pass could own the index files without colliding. **The registration surface was verified to be exactly two files**, and the task records the two that look relevant but are not: `lib/scripts/templates/CLAUDE-wiki.md` is wiki-skills-only and is delivered into *target* projects, and the root `README.md` has no command list at all. Both are explicitly out of scope so the task cannot fan out. The skill's frontmatter is pinned as the source of every cell (`description`, and `argument-hint: [view | edit | reset] [--global | --project]`), since the two tables use **different conventions** — `lib/skills/README.md` writes bare backticked names with no slash, `CLAUDE.md` writes the leading slash plus the argument hint — and matching each locally beats making them match each other. `## Misc utility` is the chosen table, next to `serena-config`: the skill's own `category: executing` is Claude Code frontmatter, not a README section name, and the README's sections are topical with no `Executing` among them. Two defects are called out ahead of time: the `argument-hint`'s literal `|` characters must be escaped `\|` or the `CLAUDE.md` row renders with the wrong cell count, and **`test/bootstrap-config-skill.test.js:364-376` will fail on this task's first edit by design** — it asserts neither file mentions `bootstrap-config`, and its own failure message instructs the fixer to delete it. The task requires deleting the whole block plus its orphaned `// Phase boundary` banner, and explicitly forbids skipping or inverting it; the verification step expects the suite to drop 290 → 289 with zero failures, so a non-zero count means the tripwire was left in.

## [2026-08-07] task | TASK-050 Document the helper, the four-state model, and the full key registry in lib/scripts/README.md
Created TASK-050, the second of ROADMAP-005 Phase 4. `lib/scripts/README.md` documents every other script in the directory but was never updated for the preference subsystem: it has a `## Preference-schema notes` section covering the schema *file's shape* and nothing on the helper, the four-state model, the two values files, or what the 19 keys do. **Two of its sentences are now factually wrong** and the task requires correcting them rather than appending around them — line 54 still describes `bootstrap-prefs.js` as *"(not built yet)"* when it shipped in TASK-041 and is now installed into `~/.claude/`, and lines 124-125 state that the per-key table *"is not in this file yet"*, which is exactly what this task adds. The **installed layout is documented as the load-bearing fact**: step 6 puts the helper at `~/.claude/bootstrap-prefs.js` and the schema at `~/.claude/templates/bootstrap-prefs-schema.json` — i.e. `<helper dir>/templates/`, the helper's own default resolution path — which is what lets `consumer: skill` keys be read from an arbitrary project with no `--schema` flag. The `install-global.sh` step list is recorded as it now stands (6 = helper install, 7 = the sync pass, MCPs renumbered 6 → 8). **The stale summary line is this task's call to fix**: `install-global.sh:263` names six steps when there are eight, and the string is pinned twice in `test/install-global.test.js` (`:152`, `:267`) as exact `stdout.includes` matches — the task requires changing all three together, since leaving the line stale keeps a wrong claim and changing it alone turns the suite red. The registry table is specified as transcribed-not-retyped, grouped by `consumer` to match how `--list` and the companion group their output (14 installer / 5 skill), with the pipe-escaping trap called out because nearly every `values` string contains a literal `|`. One **open question is carried in with a required written resolution**: `gitCommit.versionBump` and `gitCommit.autoPush` still declare `askedBy: /git-commit` while the *settling* prompt now lives in step 7's sync pass and the other three skill keys all say `install-global.sh` — the task must decide whether `askedBy` should name both and record the reasoning either way, noting that `test/bootstrap-prefs.test.js:2128` constrains only that the field is a non-empty string, so both answers pass and nothing forces the question. Cross-links `[[BUG-0009]]` so the scope-unenforced `--set` is documented as known-and-filed rather than as intended behaviour.

## [2026-08-07] task | TASK-051 Pin bootstrap-prefs.js and bootstrap-prefs-schema.json into test/npm-pack-contents.test.js
Created TASK-051, the third of ROADMAP-005 Phase 4 — extending the tarball pin that ROADMAP-004 built for the hooks-wiring artifacts to cover the preference subsystem, which has an identical failure mode: `install-global.sh` step 6 installs both files into `~/.claude/`, so a missing one breaks `npx @codewizard-dt/bootstrap install` for every consumer while this repo's own suite stays green. **Verified up front that both files pack today** (`npm pack --dry-run --json` at `b85cbe9`, 191 files) — so this is a regression guard, not a fix, and the task is written around that fact. They pack only *incidentally*, via `package.json`'s blanket `"lib/"` glob (`:23`) with negations confined to `raw/`; nothing names them. The realistic regression is someone narrowing `files` — the same instinct that produced the `raw/` negations in `99f3bba`, which cut 210 → 185 files — and not noticing two runtime-critical files went along. Because a test that passes today would also keep passing under a badly-written version of itself, **the falsifiability step is named as the actual deliverable**: temporarily negate each path in `files` *separately*, confirm each fails independently with a message naming the missing path, then restore `package.json` and verify it byte-identical by SHA-256 — a stray `!lib/...` left behind is precisely the regression the test exists to catch. Design call: extend the existing required-paths loop (`:40-50`) and generalise its name rather than adding a second test, keeping one list of "files whose absence breaks a consumer install" instead of two that drift, and keeping the single module-level `npm pack` (a second would roughly double the file's runtime). `lib/skills/bootstrap-config/SKILL.md` was checked and also packs, but is deliberately excluded: skills ship as a directory tree under the same glob, and pinning one skill file invites the question of why the other ~50 are not.

## [2026-08-07] task | TASK-052 End-to-end verification of the preference store against a scratch project
Created TASK-052, ROADMAP-005 Phase 4's fourth item and the roadmap's release gate — `depends_on::[[TASK-049]] [[TASK-050]] [[TASK-051]]`, since it verifies the state they leave behind. Every phase so far was verified in isolation; nothing has yet exercised the store as a user meets it. Four claims: ask-once across a re-run, all three `prefs.gitTracking` answers, all three `gitCommit.versionBump` values, and a non-interactive run that writes no preferences file. **Hermeticity is written as the hard requirement, with the reason stated rather than assumed**: the task must never run `install-global.sh` against the real `$HOME`, because step 7 would permanently settle the user's own skill preferences and `unset` is not recoverable by re-running — not asking again is the entire feature. Bounded the way UAT-030/046/048 were: redirected `HOME` per command, SHA-256 guards either side on `~/.claude/settings.json` / `.gitignore` / `.git/info/exclude`, and a precondition that the real `~/.claude/bootstrap-prefs.json` is absent before starting. Two harness facts are carried in so they are not rediscovered: `has_tty()` (`lib.sh:191-193`) honours `BOOTSTRAP_ASSUME_TTY=1`, which is the **only** way a piped harness reaches any prompt body — and it must be *unset* for claim 4, which is specifically about the real headless path; and **prompt strings are unobservable through a pipe** because `read -r -p` prints only to a terminal, so every assertion is on stored state and on-disk effects, with grammar checked against the script source. That shapes claim 1 directly: "was not re-asked" is tested as *never printed* with stdin poisoned to answer differently, since unchanged-alone would pass even if the question was asked again and answered the same. Claim 3 proves the `confirm`-is-the-ask-state trap live (`--set --value ask` must exit 1), and claim 4 asserts the deliberate split that step 6 is **not** tty-gated while step 7 is — a headless run installs the helper and schema but writes no answers. Defects found are to be filed via `/bug-file`, never fixed inline, since an inline fix would be unverified by construction. Finally, the task must **recommend but not perform** the user's own `install-global.sh --skip-mcps` run: `/bootstrap-config`, `bootstrap-prefs.js`, and the post-TASK-030 `uat-generate` are not live in their real `~/.claude/` until they run it themselves, and that is the one legitimate real-`HOME` invocation in this area — their call, not the task's.

## [2026-08-07] uat | UAT-049 UAT: Register /bootstrap-config in lib/skills/README.md and the CLAUDE.md Custom Commands table
Generated UAT-049 for TASK-049 with 6 test cases (4 edge, 2 integration) covering the two registration rows, the tripwire's deletion, and the untouched out-of-scope files. **Created 0 repeatable unit tests, deliberately** — cases 1-4 meet every promotion criterion (pure static-text assertions, no harness needed, obvious home in `test/bootstrap-config-skill.test.js`) but TASK-049 step 4 explicitly forbids the promotion: *"Do not replace it with a skip, an inverted assertion, or a 'now it must be listed' test. If a positive registration check is wanted, that is a separate decision — the tripwire's stated resolution is deletion."* Promoting them would recreate, inverted, the very test the task deleted, so the four assertions are recorded as a gap that specifies the replacement should that decision ever be taken. The two conventions are asserted **locally rather than against each other**, because the tables genuinely differ: `lib/skills/README.md` takes a bare backticked name (the test fails on a leading slash) and its Purpose cell is compared against the SKILL.md `description` read at run time rather than hard-coded, so a reworded frontmatter surfaces as drift instead of passing silently. The `CLAUDE.md` case asserts the **rendered** cell count — it masks every `\|` and then splits on the remaining `|`, which is the only formulation that catches a *partially* escaped row; a substring check for the argument hint would pass on a row that renders with five cells. The tripwire case asserts on **absences that a naive check would miss**: not just the test name but the `// Phase boundary` banner, any `{ skip: ... }` option, and any `.skip()` form, since a skipped test reads as a passing one in the summary line. Test counts are pinned at both levels (13/13 for the single file, 289/289/0/0 for the suite) because 14 means the tripwire survived and 12 means the deletion took a neighbour with it.

## [2026-08-07] uat | UAT-049 passed (auto) · TASK-049 done
Archived UAT-049 → uat/archive/ and TASK-049 → tasks/archive/. All 6 cases passed. Both registration rows landed where the task specified and in the *local* convention of each table: `lib/skills/README.md:137` is directly after `serena-config` under `## Misc utility`, bare-named with no leading slash, and its Purpose cell is **byte-equal to the SKILL.md `description`** read at run time rather than compared against a hard-coded copy. `CLAUDE.md:99` carries the full `argument-hint` with all three pipes escaped and **renders as exactly 2 cells** — asserted by masking every `\|` and splitting on what remains, the only formulation that catches a partially-escaped row. The Phase 3/4 tripwire is gone in the strong sense: no test name, no `// Phase boundary` banner, no `{ skip: ... }` option, and no `.skip()` anywhere in the file — the deletion the tripwire's own failure message prescribed, not the neutering that would have read as a pass. Both out-of-scope files are clean (`lib/scripts/templates/CLAUDE-wiki.md`, root `README.md`), and `lib/scripts/README.md` gained no duplicate registration row from this task. Counts confirm the intended delta and nothing more: `test/bootstrap-config-skill.test.js` is 13/13 (14 would mean the tripwire survived, 12 that the cut took a neighbour), and the suite is **289 / 289 pass / 0 fail / 0 skipped**. **Harness note:** all four static-check commands originally used `node -e` with literal backticks and were **blocked by the interpreter-indirection guard**, which reads a backtick as command substitution; they were rewritten to build backticks via `"\x60"` — semantically identical, and the UAT file now stores the runnable form so a re-run does not rediscover it. Zero unit tests were promoted, deliberately: TASK-049 step 4 forbids replacing the tripwire with an inverted "now it must be listed" test, so the four assertions are recorded in the UAT's Gaps section as the ready specification should that separate decision ever be taken. ROADMAP-005 Phase 4 item 1 of 4 checked off; the roadmap stays `active` with three items left.

## [2026-08-07] uat | UAT-050 UAT: Document the helper, the four-state model, and the full key registry in lib/scripts/README.md
Generated UAT-050 for TASK-050 with 7 test cases (4 edge, 3 integration) and created **6 repeatable unit tests** in a new `test/scripts-readme-prefs-docs.test.js`. TASK-050 reads as a documentation task but carries **two non-documentation side effects**, and the UAT is built around those: it edited the schema (`askedBy` for both `gitCommit.*` keys, `/git-commit` → `install-global.sh`) and it edited `install-global.sh`'s summary line, which is pinned twice as exact `stdout.includes(...)` matches. The promoted test is a **README ↔ schema bijection**, written because nothing else in the suite reads `lib/scripts/README.md`: the 19-row registry was transcribed by hand, so a key added, renamed, re-valued, or re-defaulted in the JSON leaves the table silently stale, and the task's own step 8 names the stake — a drifted table is worse than none, because it is the surface a reader trusts *instead of* opening the JSON. It checks both directions (schema key missing from the table; table row for a key that no longer exists), every column against its schema field, the `consumer` grouping (`--list` and the generated companion group the same way, so a misfiled row makes three surfaces disagree), the `(pattern)` markers against `dynamic: true`, the heading counts as derived facts rather than decoration, and a **7-cell render per row** — the pipe-escaping defect the task flagged, where an unescaped `|` shifts every later column and reads as plausible nonsense rather than as breakage. Two comparisons are deliberately normalised: backticks are stripped (the README formats as code what the schema stores bare) and straight single quotes too, because the README renders as `update` what the schema writes as `'update'`; everything else compares literally, so a reworded summary still fails. **The promoted test was proven falsifiable before being trusted** — un-escaping one pipe in the `mcp.context7Scope` row failed two independent tests, and `lib/scripts/README.md` was restored byte-identical (SHA-256 `5a91b86e…` before and after). The summary line is verified twice on purpose: statically as a **three-way** match (script literal vs. both pins, with the pin *count* asserted so a half-finished update is caught), and live by running `test/install-global.test.js`, since the static match proves the strings are equal but not that the line is still reachable — the pins only fail when the script actually executes against a redirected HOME. One gap recorded, not fixed: `STEP_BANNERS` still lists six banners and its test is still named "all six steps" while the script now runs eight, so nothing verifies the two preference steps run or run in order — out of TASK-050's stated scope, and flagged for TASK-052.

## [2026-08-07] uat | UAT-050 passed (auto) · TASK-050 done
Archived UAT-050 → uat/archive/ and TASK-050 → tasks/archive/. All 7 cases passed. **The registry table is not merely present but provably in sync**: all 19 rows match the schema field-for-field in both directions — no schema key missing from the table, no table row for a key that no longer exists — with the `consumer` grouping, the `(pattern)` markers against `dynamic: true`, the heading counts, and a 7-cell render per row all holding. **The `askedBy` change is consistent and legal**: all five `consumer: skill` keys now name `install-global.sh`, that file exists, and no `askedBy` among the 19 carries a comma — the compound form `test/bootstrap-prefs.test.js:2389` forbids, which is what made the decision binary rather than a preference. `test/bootstrap-prefs.test.js` is **70/70 with 0 skipped**, so both bijection directions are still live and the schema edit drifted nothing. **The summary line was verified twice, and the second check is the one that mattered**: statically it is a three-way character-for-character match (script literal vs. exactly two pins — the pin *count* asserted, so a half-finished update fails on count alone), and live it printed on **both** paths a pin covers (happy path and guarded-MCP-failure) when `install-global.sh` actually ran against `fs.mkdtemp` HOMEs. That is the failure mode the task flagged as "green locally, red in a fake-HOME run", and it is closed. Every stale claim is gone (`not built yet`, `is not in this file yet`) and the helper row, six new sections, and the `[[BUG-0009]]` cross-reference are all present. Six repeatable tests were promoted into a new `test/scripts-readme-prefs-docs.test.js` and **proven falsifiable before being trusted** — un-escaping a single pipe in the `mcp.context7Scope` Values cell failed two independent tests, after which `lib/scripts/README.md` was restored byte-identical (SHA-256 `5a91b86e…` either side). Hermeticity confirmed: the real `~/.claude/bootstrap-prefs.json` is still absent after a run that executed the installer seven times. Suite **289 → 295** (295 pass, 0 fail, 0 skipped) — the delta is exactly the six promoted tests. One gap carried forward, deliberately unfixed: `STEP_BANNERS` (`test/install-global.test.js:36-43`) still lists six banners and its test is still named "all six steps" while the script runs eight, so the two preference steps' banners are unverified for presence and order — out of TASK-050's scope, and the natural pickup for TASK-052. ROADMAP-005 Phase 4 item 2 of 4 checked off.

## [2026-08-07] uat | UAT-051 UAT: Pin bootstrap-prefs.js and bootstrap-prefs-schema.json into test/npm-pack-contents.test.js
Generated UAT-051 for TASK-051 with 6 test cases (2 edge, 4 integration). Created **0 repeatable unit tests, and that is the correct outcome** — TASK-051 step 4 requires the suite total to rise by exactly 0 (the two paths join an existing loop; no new `test()` block), so promoting anything would violate the task's own acceptance criterion, and the pin already *is* the repeatable test. The UAT is built around the task's own framing that verifying the two strings are present is nearly worthless: both files pack today only *incidentally*, through `package.json`'s blanket `lib/` glob, so a badly-written pin would pass right now and keep passing under the exact regression it claims to guard. Two cases therefore **re-prove falsifiability live** — each injects one `!lib/...` negation into `files`, runs the pin, and requires a non-zero exit whose output *names that path* — and they are run **independently**, the schema probe starting from a restored `package.json`, because a pin that only fails on its first array element passes a single-path check and is precisely the defect the task warned about. Each probe does mutate → run → restore inside a `try/finally` and asserts the SHA-256 **after** restoration, so a thrown test run cannot leave a negation behind; a stray `!lib/...` is the one regression that would ship silently, since the repo's own suite stays green with all sources present on disk. Two shape assertions guard the *how*: exactly three `test()` blocks and exactly one `spawnSync` (a second `npm pack` would roughly double the file's runtime while passing every content assertion), plus builtin-only imports and the generalised test name. `npm publish` appears nowhere; `npm pack --dry-run` only. Two gaps recorded: the 191-file tarball count is verified here but deliberately **not** pinned in the suite (it changes on every legitimate file addition, so a pinned count would fail for the right reasons and train people to bump it unread), and the cosmetic `99fbba` → `99f3bba` SHA typo at `test/npm-pack-contents.test.js:10` remains out of scope.

## [2026-08-07] uat | UAT-051 passed (auto) · TASK-051 done
Archived UAT-051 → uat/archive/ and TASK-051 → tasks/archive/. All 6 cases passed. **The task's actual deliverable — falsifiability — was re-proven live, for both paths independently.** Injecting `!lib/scripts/bootstrap-prefs.js` into `package.json`'s `files` made the pin exit non-zero with `tarball is missing lib/scripts/bootstrap-prefs.js`; a second probe, started from a **restored** `package.json` so the two are genuinely independent rather than cumulative, did the same for `lib/scripts/templates/bootstrap-prefs-schema.json`. That independence is the point: a pin that only fails on its first array element passes a single-path check, which is the exact defect the task warned about. Each probe mutated → ran → restored inside a `try/finally` and asserted the SHA-256 **after** restoration; `package.json` is byte-identical at `d7fa2645dc4f4c424c0cde53425905759ae09e313eb0c6896e72324ca80bc2f8` and `git status --porcelain` reports it clean, so neither probe left a negation behind — the one regression that would ship silently, since the repo's own suite stays green with every source still on disk. The pin's *shape* also holds as specified: both paths in the existing loop, the test renamed off its hooks-only name, the provenance comment naming `install-global.sh` step 6, exactly **3** `test()` blocks and exactly **1** `spawnSync` (a second `npm pack` would roughly double the file's runtime while passing every content assertion), and 4 imports, all `node:` builtins. Tarball unchanged at **191 files** with both prefs paths packed; `npm pack --dry-run` only, nothing published. Suite **295 / 295 pass / 0 fail / 0 skipped** — TASK-051 added 0 tests exactly as its acceptance criterion required, so the 289 → 295 delta for this session is entirely UAT-050's 6 promoted tests. **ROADMAP-005 Phase 4 item 3 of 4 checked off — the roadmap is now 13/14 with only TASK-052 (the end-to-end release gate) remaining, and stays `active`.**

## [2026-08-07] task | TASK-052 end-to-end verification of the preference store — all four claims pass
Ran the release gate for ROADMAP-005. All four claims were demonstrated against **real captured output** under a redirected `HOME`, never the user's own. **Claim 1 (ask-once)** produced evidence stronger than the plan asked for: run 1 settled four keys and deliberately left one unanswered as a control; run 2 poisoned stdin with answers differing from run 1 on every line, and the control key consumed poison **line 1**, not line 5 — since prompts consume stdin in order, lines 1–4 were consumed by *nothing*, which proves the four settled keys were never asked independently of the stdout-absence check. A separate falsifiability control (`--unset gitCommit.autoPush`, identical poison) re-opened the question and the poison *did* settle it, proving the poison was live and destructive. Run 3 reported `All skill preferences already answered — nothing to ask.` with the store byte-identical. **Claim 2** drove all three `prefs.gitTracking` answers through three separate scratch git repos, storing the resolved **name** (never the digit) at the **project** layer — the scratch *global* file was never created in any of the three — with the three distinct on-disk outcomes and the repo's own `.gitignore` / `.git/info/exclude` hash-guarded **before and after each case**, six readings, all identical. **Claim 3** settled `auto`/`confirm`/`never` plus an uppercase case proving case-insensitivity is real rather than assumed, and confirmed `--set gitCommit.versionBump --value ask` exits 1 *before any write* — no values file is created at all. **Claim 4** confirmed the tty gate is a **whole-pass** gate: the question block sits in the `else` arm, so it is unreached rather than merely unwritten, and a headless run installs the helper and schema while writing no preferences file — absence, not an empty file. Every previously-found trap was re-confirmed closed: the sync pass gates on `prefs_stored_global` (not `prefs_get`, whose `--list` probe was separately proven read-only against a fresh HOME, creating neither the values file nor `~/.claude` itself); stored `false` and stored `ask` are never re-asked; `gitCommit.autoPush`'s schema default is `false`; and `mcp.playwrightConflict` stores names *and rejects digits at the persistence layer*. The folded-in coverage gap is closed — `STEP_BANNERS` (`test/install-global.test.js`) went from six entries to eight with the two preference banners in their real emitted positions, the test renamed off "all six steps", and two assertions added to the `--skip-mcps` case since that flag guards only step 8. The ordering assertion, not merely a presence check, now covers steps 6 and 7: step 6 installs the helper the step-7 pass reads *through*, so an inversion would leave step 7 probing a helper that is not there and re-asking answered questions, which a presence-only check would call a pass. Hermeticity held completely: three baseline SHA-256s unchanged (checked twice), all five prefs artifacts still absent from the real `~/.claude/`, zero scratch residue. Two message-only defects filed as [[BUG-0010]] rather than fixed — a fix inside a verification task would be unverified by construction. Suite held at **295 / 295 / 0 / 0**.

## [2026-08-07] uat | UAT-052 UAT: End-to-end verification of the preference store against a scratch project
Generated UAT-052 for TASK-052 with 11 test cases (6 CLI, 5 edge/doc) and created **2 repeatable unit tests**, taking the suite **295 → 297**. The UAT is deliberately *not* a re-run of TASK-052's ad-hoc scratch scenarios: TASK-052 was itself a verification task, so the honest acceptance question is whether each demonstrated claim is now **pinned** such that a regression fails the suite rather than needing a human to notice. Each case therefore names the automated test that owns its claim and runs it in isolation, and fails if the test fails **or if it does not exist under that name** — a renamed or deleted test is exactly the regression being guarded against, which is why every case requires checking the reported count, since `--test-name-pattern` matching zero tests exits 0 and reads as a pass. A coverage survey found the existing suite already pinned nine of eleven claims (largely TASK-047's work in `test/prompt-stickiness.test.js`) and exactly **two genuine gaps**, both promoted: (1) the **schema half of the headless step-6 contract** — the existing pair covered the interactive direction (schema asserted, `tty:true`) and the headless direction (helper asserted only), so moving step 6 inside step 7's `else` arm, where the *questions* legitimately live, would have passed every existing test while leaving every unattended install with a helper that reads `unset` for every documented default; the new test asserts the schema is byte-identical at `<helper dir>/templates/` **and** that the installed helper invoked with no `--schema` flag still resolves a default, on a `tty:false` run that records zero answers. (2) **the store refusing a raw digit** for `mcp.playwrightConflict` — the menu-input direction was well covered but nothing pinned that a digit cannot be written straight into the store, which is what stops a cosmetic menu reorder silently changing what an already-stored answer means, with no write and no diff; the new test pairs the refusal with the positive contrast that the resolved name `shared` is accepted verbatim. Both new tests carry the local vacuity guard (assert the key's grammar is still what the test assumes, so it fails loudly rather than passing for the wrong reason). Two gaps recorded, not fixed: `test/install-global.test.js`'s `runInstall` inherits the full `process.env` including any ambient `BOOTSTRAP_ASSUME_TTY` (where `prompt-stickiness.test.js` uses `curatedEnv`, which deletes the seam) — test-infrastructure work outside this task's scope, guarded meanwhile by a UAT prerequisite; and [[BUG-0009]] stays pinned by its existing bidirectional test, since that bug's own notes explain why those fixtures must be rewritten as part of the fix rather than ahead of it.

## [2026-08-07] bug | BUG-0010 filed — install-global.sh output overstates what happened
Two **message-only** defects found while asserting on real captured stdout during TASK-052's verification, filed together as one bug per [[BUG-0008]]'s precedent (same file, same root shape: a line printed unconditionally that describes a branch which did not run). (1) `install-global.sh:200` prints `ask (you will be prompted every run; the answer is never stored)` — but `ask` **is** stored, at layer `[global]`, and the next line of output says `Stored in …`. The sentence is technically defensible (`lib.sh:526` defines `ask` as "prompt, and record NOTHING", meaning the per-invocation answer at skill runtime) but placed between `key = ask` and `Stored in …` it reads as "nothing was persisted", inviting the belief that the installer will re-ask — it will not, and `/bootstrap-config` is then the only way to change it. (2) `install-global.sh:264` is a bare unconditional `echo` outside the `if [ "$SKIP_MCPS" = false ]` guard, so the summary claims `MCPs` on runs that skipped step 8 entirely — and since both `setup-project.sh` and `update-project.sh` call `install-global.sh --skip-mcps`, that is **every** setup and every update, the most common path rather than an edge case. Severity `low`: nothing is mis-installed and no state is wrong; the cost is that the completion summary is not usable as evidence of what ran, which is the one thing it exists for. Neither was fixed — TASK-052 is a verification task and a fix applied inside it would be unverified by construction. The fixer's note records that defect 2 has a test consequence: the exact summary string is pinned at three sites in `test/install-global.test.js`, and a conditional summary needs each updated to the variant matching its flags.

## [2026-08-07] uat | UAT-052 passed (auto) · TASK-052 done
Archived UAT-052 → uat/archive/ and TASK-052 → tasks/archive/. **All 11 cases passed**, and all four prerequisites auto-verified. The suite ran **297 / 297 pass / 0 fail / 0 skipped**, the 295 → 297 delta being exactly UAT-052's two promoted tests. Every claim is now pinned by a named automated test run in isolation, with the reported count checked each time — `--test-name-pattern` matching zero tests exits 0 and would otherwise read as a pass, so a renamed or deleted test is caught rather than silently tolerated. **One instrument was corrected mid-run, and the correction is recorded in the UAT file rather than quietly applied**: UAT-DOC-001 originally asserted `git status --porcelain lib/scripts/install-global.sh` prints nothing to prove TASK-052 fixed nothing it found. That compares against **HEAD**, and the file already carried 147 uncommitted insertions from earlier ROADMAP-005 work (steps 6 and 7 themselves), so the check could only ever report `M` — it tested "changed since the last commit", not "changed by TASK-052". The replacement asserts the substantive claim directly and machine-checkably (both reported defect strings still present verbatim → `FILED_NOT_FIXED`); no assertion was dropped, and the case's own step 3 is what is now actually being tested. A second harness note is stored for re-runs: the two backticks in `gitCommit.versionBump`'s test name are written as `.` in the pattern, since `--test-name-pattern` is a regex and a literal backtick is read as command substitution by the interpreter-indirection guard — the same class of finding as UAT-049's. Hermeticity closed clean: real `~/.claude/bootstrap-prefs.json` and its companion both still absent after a session that ran the real installer well over a dozen times, and all three baseline SHA-256s byte-identical. **ROADMAP-005 is now 14/14, flipped to `status: done`, and archived → roadmaps/archive/.**

## [2026-08-13] ingest | Obsidian and Typed Wiki Linking
Ingested from raw/research/obsidian-wiki-linking/index.md. Key claims: this repo's rel::[[target]] typed-link convention is Dataview's full-line inline-field syntax, not a bespoke invention; currently zero plugin dependency (no dataview query blocks exist in the repo); Graph Link Types and Breadcrumbs are optional visualization/navigation layers requiring no authoring changes. 6 entity pages touched/created (5 new: obsidian, dataview, graph-link-types, breadcrumbs-plugin, wikilink-types-plugin; 1 updated: claude-obsidian disambiguation note), 1 concept page created (typed-wiki-links), 1 source summary page created (obsidian-wiki-linking).

## [2026-08-13] ingest | Automating Obsidian and Plugin Setup in the Bootstrap Scripts
Ingested from raw/research/obsidian-setup-automation/index.md. Key claims: the Obsidian app and its three plugins (Dataview, Graph Link Types, Breadcrumbs) are both fully automatable — brew/winget/flatpak for the app; GitHub Releases API + manifest.json-driven plugin id + community-plugins.json for the plugins — fitting this repo's existing guarded/sticky-preference install pattern (documented at the new `bootstrap-guarded-install-pattern` component page); which skills benefit is uneven — `/wiki-lint`'s graph-view guidance improves for free, `/task-audit` does not (bespoke Depends-on blockquote, not `rel::` typed links). 1 new source page, 1 new component page, 4 tool entity pages extended, 1 concept page updated. **Note:** this entry was reconstructed retroactively — the ingesting agent's own log append silently did not land even though it reported success; its other 7 file writes (source, component, 4 entity updates, 1 concept update) were independently verified present on disk before writing this correction.

## [2026-08-13] roadmap | ROADMAP-006 created — Automate Obsidian + Plugin Setup
Created `wiki/work/roadmaps/ROADMAP-006-obsidian-plugin-automation.md` (4 phases, 7 inline items, all upgraded to task-links the same session via `/power-mode`). Phases: installer foundation, wire-up & guide, task-graph compatibility, verification. Derived from both same-day Obsidian research reports. Owner: David Taylor.

## [2026-08-13] task | TASK-053 Add lib/scripts/install-obsidian.sh (app + plugin auto-install)
Created task TASK-053 (ROADMAP-006 Phase 1): guarded, cross-platform installer for the Obsidian app (brew/winget/flatpak) and its three recommended plugins (Dataview, Graph Link Types, Breadcrumbs) via GitHub Releases API, reusing the guarded-install pattern documented at `bootstrap-guarded-install-pattern`. Linked into ROADMAP-006 Phase 1.

## [2026-08-13] task | TASK-054 Add obsidian.installApp + obsidian.plugins keys to bootstrap-prefs-schema.json
Created task TASK-054 (ROADMAP-006 Phase 1, parallel-safe with TASK-053): document the two new sticky preference keys gating the Obsidian installer in `bootstrap-prefs-schema.json`, following the existing per-key documentation shape. Linked into ROADMAP-006 Phase 1.

## [2026-08-13] task | TASK-055 Wire install-obsidian.sh into run_project_sync()
Created task TASK-055 (ROADMAP-006 Phase 2, depends_on TASK-053 + TASK-054): call the new installer from `run_project_sync()` in `lib/scripts/lib.sh`, guarded/non-fatal, mirroring the existing MCP-install call. Linked into ROADMAP-006 Phase 2.

## [2026-08-13] task | TASK-056 Add optional wiki/guides/ Dataview example-queries page
Created task TASK-056 (ROADMAP-006 Phase 2, parallel-safe with the installer tasks): write a new optional guide (`raw/guides/dataview-queries.md` + `wiki/guides/` copy) with 3 example Dataview query blocks, explicitly out of scope for wiring into `sync-wiki-scaffold.sh`'s `OPTIONAL_GUIDES` delivery. Linked into ROADMAP-006 Phase 2.

## [2026-08-13] task | TASK-057 Reconcile /task-audit's Depends-on/Blocks blockquote with rel::[[target]]
Created task TASK-057 (ROADMAP-006 Phase 3, parallel-safe with the installer tasks): add additive `depends_on::`/`blocks::` typed-link lines to task files alongside the existing blockquote, editing `task-audit/SKILL.md` and `task-add/SKILL.md` without touching task-audit's own parser or backfilling existing tasks. Linked into ROADMAP-006 Phase 3.

## [2026-08-13] task | TASK-058 Manually verify guarded Obsidian install end-to-end on at least one platform
Created task TASK-058 (ROADMAP-006 Phase 4, depends_on TASK-053 + TASK-055): real happy-path verification of the installer against this repo's own project root. Linked into ROADMAP-006 Phase 4.

## [2026-08-13] task | TASK-059 Confirm setup/update stay non-fatal on Obsidian install failure and declining leaves everything untouched
Created task TASK-059 (ROADMAP-006 Phase 4, depends_on TASK-053 + TASK-055, parallel-safe with TASK-058): real negative-path verification (simulated failure + declined prompts) confirming no partial state or fatal aborts. Linked into ROADMAP-006 Phase 4.

## [2026-08-13] bug | BUG-0011 install-obsidian.sh never writes manifest.json into the installed plugin directory
Filed while executing TASK-058's real happy-path verification run (`bash lib/scripts/install-obsidian.sh --project-dir "$(pwd)"` against this repo's own `.obsidian/` vault). `_install_obsidian_plugin` (`lib/scripts/install-obsidian.sh:96-171`) downloads each plugin's `manifest.json` to a temp file solely to parse its `id`, then discards it — `main.js`/`styles.css` land in the real plugin directory but `manifest.json` never does. All 3 plugins (dataview, graph-link-types, breadcrumbs) installed cleanly and were enabled in `community-plugins.json`, but none have the manifest Obsidian needs to actually load them. Deterministic, unconditional, confirmed via Serena `find_file` (zero `manifest.json` matches). Severity high, linked to TASK-058.

## [2026-08-13] task | TASK-058 verified — real happy-path Obsidian install run completed
Ran `bash lib/scripts/install-obsidian.sh --project-dir "$(pwd)"` for real against this repo's own project root (exit 0). Obsidian.app was already present (pre-existing, not brew-cask-managed, so the real `brew install --cask obsidian` path was not exercised). All 3 plugin folders (dataview, graph-link-types, breadcrumbs) were created with `main.js`/`styles.css`, and `community-plugins.json` correctly listed all 3 ids — but discovered and filed BUG-0011 (missing `manifest.json` in every plugin folder, so none actually load in Obsidian). No interference observed from the concurrent TASK-059 sibling run against the same directory. Task frontmatter moved to `pending-uat`; findings recorded in the task's `## Notes` section.

## [2026-08-13] task | TASK-059 verified — real failure/decline simulation runs completed
Executed both negative-path checks for real. Failure path: with `brew`/`flatpak` stripped from `PATH`, both the script's own guard (`_install_obsidian_app || echo WARNING...`) and the `run_project_sync()` call-site guard (TASK-055's wiring) stayed non-fatal — exit 0, warning printed, no abort. Decline path: `printf 'n\nn\n' | BOOTSTRAP_ASSUME_TTY=1 bash lib/scripts/install-obsidian.sh --interactive --project-dir "$(pwd)"` left `.obsidian/` byte-for-byte unchanged (SHA-256 verified before/after). One real side effect was caught and reverted: the decline run wrote `obsidian.installApp: false` into the user's actual `~/.claude/bootstrap-prefs.json` and created an untracked `<repo>/.claude/bootstrap-prefs.json` — both cleaned up since they were verification byproducts, not genuine user decisions. No defect found; no bug filed. Task frontmatter moved to `pending-uat`.

## [2026-08-13] uat | UAT-053 through UAT-059 generated for ROADMAP-006
Generated all 7 UATs for ROADMAP-006 in one parallel pass (agents wrote only their own UAT/unit-test files; this index/log update was batched by the orchestrator afterward to avoid the same concurrent-shared-file collision risk already hit once this session). Totals: 46 test cases across the 7 UATs (13/6/6/8/8/2/3), with 6 new/extended unit-test files: `test/install-obsidian.test.js` (new, 13 cases — shared and successfully merged by TASK-058's and TASK-059's agents writing to it concurrently), `test/scripts-readme-prefs-docs.test.js` (extended, 1 new case), `test/run-project-sync.test.js` (extended, 6 new cases — reused the existing file per the "existing test wins" rule rather than creating a sibling), `test/dataview-queries-guide.test.js` (new, 8 cases), `test/task-typed-links.test.js` (new, 6 cases), `test/verify-obsidian-install-evidence.test.js` (new, 4 cases). UAT-058 and UAT-059 were correctly judged already-substantially-verified (both source tasks were themselves manual verification runs) and each ship only 2-3 minimal evidence-preservation cases rather than duplicating that work. One intentional failing test remains by design: the BUG-0011 regression case in `test/install-obsidian.test.js` asserts the *required* contract (manifest.json present) and will auto-pass once that bug is fixed — it is not weakened to match the current buggy behavior.

## [2026-08-13] task | Two regressions from this session's own edits found and fixed
Two failures surfaced by the UAT-generation pass, both caused by this session's own concurrent task work rather than pre-existing drift: (1) TASK-054 added `obsidian.installApp`/`obsidian.plugins` to `bootstrap-prefs-schema.json` without updating `lib/scripts/README.md`'s key registry table, breaking the schema↔README bijection test in `test/scripts-readme-prefs-docs.test.js` (14≠16 rows, 19≠21 total-key mismatch) — fixed by adding the two missing rows and updating the "14/19" counts to "16/21". (2) TASK-055's edit to `run_project_sync()` in `lib.sh` shifted every line after its insertion point, staling `mcp.context7Scope`'s `lib.sh:387` citation (actual content had moved to line 393) — fixed the citation in both `bootstrap-prefs-schema.json`'s `detail` field and its `CITATION_PINS` pin in `test/bootstrap-prefs.test.js`. Full suite confirmed at **338 tests, 337 pass, 1 fail** (the intentional BUG-0011 regression case) after both fixes — the same single failure noted in the UAT-generation entry above, no others.

## [2026-08-13] uat | UAT-053 through UAT-059 auto-judged (headless)
Ran `/uat-auto`-style judging (Steps 1-5 only, headless, fail-closed) across all 7 UATs in parallel — agents wrote only their own file's status markers; Closure was withheld from every agent and run serially by the orchestrator afterward to avoid the same shared-file collision risk as the generation pass (Closure touches both index files, both archive dirs, the roadmap, and this log). Results: UAT-053 12/13 pass (1 expected fail: the BUG-0011 regression case, correct and by design — will auto-pass once that bug is fixed); UAT-054 4/6 pass (2 fail-closed: free-text schema `detail` prose, Manual/not-unit-backed, matches the repo's established UAT-040 precedent); UAT-055 **6/6 all pass**; UAT-056 6/8 pass (2 fail-closed: Manual prose checks); UAT-057 6/8 pass (2 fail-closed: one blocked by this repo's own Serena-first policy on the recorded `**Command**`, one a documented out-of-scope gap-note); UAT-058 **2/2 all pass**; UAT-059 2/3 pass (1 fail-closed: bare Manual doc-completeness check). Only UAT-055 and UAT-058 reached all-pass and proceeded to Closure (see below); the other 5 remain `pending` in `wiki/work/uat/`, each needing a human `/uat-walk` pass for their specific Manual/prose cases — none represent an implementation defect.

## [2026-08-13] uat | UAT-055 passed (auto) · TASK-055 done
Archived UAT-055 → uat/archive/ and TASK-055 → tasks/archive/. All 6 tests passed via unit-backed evidence (`test/run-project-sync.test.js`, extended file): the `install-obsidian.sh` call site sits between `merge-gitignore.sh` and `build-mcp-guide.sh`, invoked with the exact `--interactive --project-dir` flag convention, its banner prints in the right order, a forced failure stays non-fatal with the exact warning text, downstream steps still run after a failure, and success prints no warning. Checked off TASK-055 in ROADMAP-006 Phase 2 (roadmap stays active — 2/7 items now checked).

## [2026-08-13] uat | UAT-058 passed (auto) · TASK-058 done
Archived UAT-058 → uat/archive/ and TASK-058 → tasks/archive/. Both evidence-preservation cases passed via unit-backed evidence (`test/verify-obsidian-install-evidence.test.js`): TASK-058's own Notes section still records the exact real-install command/exit-code/plugin-state, and BUG-0011 remains correctly filed, open, and cross-linked. No live re-run performed — TASK-058's own execution already was the verification. Checked off TASK-058 in ROADMAP-006 Phase 4 (roadmap stays active — 2/7 items now checked).

## [2026-08-13] task | Third regression from Closure itself found and fixed
Archiving TASK-058 (moving it to `wiki/work/tasks/archive/`) broke its own backing unit test — `test/verify-obsidian-install-evidence.test.js` hardcoded the task's path in the *active* `wiki/work/tasks/` directory, which the file no longer occupies once `done`. Fixed by repointing the constant to `wiki/work/tasks/archive/TASK-058-verify-obsidian-install.md`. Full suite re-confirmed at **338 tests, 337 pass, 1 fail** (the intentional BUG-0011 case, unchanged). Noted as a general risk for future closures: any unit test that reads a task/UAT file by its pre-archive path will break the moment that file is archived — worth a `/task-add` follow-up to audit for this pattern across the suite rather than hitting it once per case.

## [2026-08-14] uat | UAT-054 passed (walk) · TASK-054 done
Archived UAT-054 → uat/archive/ and TASK-054 → tasks/archive/. Walked interactively: UAT-EDGE-001/002/003/006 had already auto-passed on machine-checkable evidence; UAT-EDGE-004/005 (the two free-text `detail`-prose checks that fail-closed under `/uat-auto`, matching the repo's UAT-040 precedent) were reviewed by the user and both confirmed pass — both prose entries cover all required elements (mechanism, sticky-prompt semantics, scope rationale, and for `obsidian.plugins`, the Dataview-prerequisite reasoning). BUG-0011 remains unaddressed and out of scope for this task. Checked off TASK-054 in ROADMAP-006 Phase 1 (roadmap stays active — 3/7 items now checked).

## [2026-08-14] uat | UAT-056 passed (walk) · TASK-056 done
Archived UAT-056 → uat/archive/ and TASK-056 → tasks/archive/. Walked interactively: 6 STATIC/unit-backed cases had already auto-passed; the 2 remaining Manual cases (optionality/install-path prose, task↔UAT frontmatter wiring) were reviewed and confirmed pass. User flagged a follow-up during the walk (not fixed now, out of scope for this UAT): `raw/guides/dataview-queries.md` §1's install-path bullet reads "Once TASK-053 lands, run `lib/scripts/install-obsidian.sh`..." — once TASK-053 itself closes, that conditional phrasing should be edited out (the script will unconditionally exist by then), and the link target (`../../wiki/work/tasks/`, a directory, not TASK-053's file) will also need attention once TASK-053 archives, same link-rot pattern already hit with TASK-058. Checked off TASK-056 in ROADMAP-006 Phase 2 (roadmap stays active — 4/7 items now checked).

## [2026-08-14] uat | UAT-057 passed (walk) · TASK-057 done
Archived UAT-057 → uat/archive/ and TASK-057 → tasks/archive/. Walked interactively: 6 STATIC/unit-backed cases had already auto-passed; the 2 remaining cases were re-verified live and confirmed pass — UAT-EDGE-001 (no pre-existing task backfilled with typed links) via `search_for_pattern` against TASK-031/TASK-039 individually (its own recorded `grep` command is blocked by this repo's Serena-first hook, so the equivalent check was re-run instead of skipped), and UAT-MANUAL-001 (the known task-add-never-renders-the-blockquote gap) via a fresh cross-file search confirming task-audit/task-update actually render it while task-add only mentions it in prose. Checked off TASK-057 in ROADMAP-006 Phase 3 (roadmap stays active — 5/7 items now checked).

## [2026-08-14] uat | UAT-059 passed (walk) · TASK-059 done
Archived UAT-059 → uat/archive/ and TASK-059 → tasks/archive/. Walked interactively: the 2 unit-backed EDGE cases had already auto-passed; the remaining Manual doc-completeness case (UAT-DOC-001) was re-verified against TASK-059's `## Notes` section directly (Step 1 findings, Step 2 findings, prefs-cleanup confirmation, overall verdict all present) and confirmed pass. Checked off TASK-059 in ROADMAP-006 Phase 4 (roadmap stays active — 6/7 items now checked; only TASK-053/UAT-053 remains, blocked on BUG-0011).

## [2026-08-14] ingest | Research: Serena MCP — Project Scope vs. Local Scope
Ingested `raw/research/serena-mcp-scope/index.md`. Key claims: Claude Code's three MCP scopes (local/project/user) with local > project > user precedence and a per-teammate approval gate on project-scoped `.mcp.json`; project scope is structurally wrong for Serena because its `--project <path>` argument is machine-specific, the same failure mode as committing a personal credential; no code change indicated — `install-mcps.sh` and `mcp.serenaMigrate` already implement local scope with consent-gated migration. 1 entity page touched ([[serena]]), 1 concept page created ([[mcp-server-scope-model]]).

## [2026-08-14] bug-triage | BUG-0011 install-obsidian.sh never writes manifest.json into the installed plugin directory
Triaged: status open → in-progress. Priority P1 (high severity, breaks the exact feature TASK-053 shipped, but isolated to Obsidian plugin loading — not a security issue and not blocking unrelated work). Assignee David Taylor. Starting work immediately — fix and close follow in the same session.

## [2026-08-14] bug | BUG-0011 verified and closed
Root cause: `_install_obsidian_plugin` downloaded `manifest.json` purely to extract the plugin `id` for directory naming, then discarded the temp file — no code path ever copied it into the real plugin directory, unlike `main.js`/`styles.css` which were written there directly. Fix: copy `manifest_tmp` into `$plugin_dir/manifest.json` immediately after the directory is created, unconditional, matching the existing overwrite-on-refresh semantics of the sibling files — commit `e71ded6` (`lib/scripts/install-obsidian.sh` + `test/install-obsidian.test.js`, both previously untracked). Verified two ways: the pre-existing regression test (`test/install-obsidian.test.js`, "BUG-0011 — a fully successful plugin install must leave manifest.json in the plugin directory") now passes, full suite green at 338/338; and a real re-run of the installer against this repo's own `.obsidian/` vault confirmed `manifest.json` now present in all three plugin directories. Archived BUG-0011 → bugs/archive/. Closes [[TASK-053]].

## [2026-08-14] uat | UAT-053 passed · TASK-053 done — ROADMAP-006 complete
Archived UAT-053 → uat/archive/ and TASK-053 → tasks/archive/. Its one remaining failing case (UAT-EDGE-005, the BUG-0011 regression test) was re-run after the fix and now passes; all 13 cases green. Checked off TASK-053 in ROADMAP-006 Phase 1 — **all 7 items now checked**, so per Closure's roadmap-completion rule the roadmap itself flipped `status: done` and archived → `roadmaps/archive/`. ROADMAP-006 "Automate Obsidian + Plugin Setup" is complete: the bootstrap scripts can now guardedly, opt-in install the Obsidian app and its three recommended plugins, a Dataview example-queries guide exists, and `/task-audit` task files carry additive `depends_on::`/`blocks::` typed links alongside their existing blockquote.

## [2026-08-14] bug | BUG-0009 and BUG-0010 verified and closed together
Both fixed and committed together (`9722eb8`), verified, and archived — user requested both in one pass.

**BUG-0009** — root cause: `scopePermitsLayer` already existed and was correctly used by `resolve()` for reads, but was never called from the `--set` write path, which validated the value grammar and then wrote unconditionally. Fix: call `scopePermitsLayer(entry, targetLayer)` in the `--set` handler and exit 1 (naming the real scope + correct selector) when the layer doesn't permit it; `--target` stays exempt, `--unset` stays permissive by design. Two test fixtures that built a scope-inert key via plain `--set` now use `--target` at the resolved file path instead (still exercises the real write path). Updated `bootstrap-config/SKILL.md` and `lib/scripts/README.md`'s stale "does not enforce scope" claims, and removed a self-aware tripwire test in `test/bootstrap-config-skill.test.js` that explicitly asked to be deleted once fixed. Added 3 new regression tests (both refusal directions + the `--target` exemption).

**BUG-0010** — root cause: two message-only defects, both cases of a line printed unconditionally without accounting for a later-added branch. Fix: reworded the `ask` confirmation to describe the per-run answer rather than reading as "nothing was stored" (it is); made the completion summary's `+ MCPs` token conditional on `SKIP_MCPS`, since both `setup-project.sh` and `update-project.sh` always pass `--skip-mcps` — the most common path was claiming a step that never ran. Added the summary assertion the bug's own notes flagged as missing from the `--skip-mcps` test.

Full suite 341/341 after both fixes. Archived BUG-0009 and BUG-0010 → bugs/archive/.
